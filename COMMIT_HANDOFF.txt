docs(log): 记录 4-bit 载荷的零训练收益（jpeg50 0.879->0.938）+ 交接状态

§24：`diag_ecc_frontier.py --n-bits 4` 在同一载密图 PSNR≈30 dB 下：
- 4bit/ecc16/check16 -> bpb 4 -> N=64：mf clean 1.000 / jpeg50 **0.938** / jpeg75 **0.979**
- 对照 8bit/ecc8/check16 -> N=32：1.000 / 0.879 / 0.934
即同 PSNR 下 jpeg50 +0.059、jpeg75 +0.045，且**零训练**（载荷减半 -> 槽位减半 ->
fit_capacity 把 bpb 顶高 -> N 32->64）。jpeg50 在 N>=56 处饱和于 0.938。
同时给出 N 兑现率的经验标度（每翻倍 ~+0.05 jpeg50），据此外推 64x64 latent 的
N=144 应能到 0.96-0.98 @30 dB —— 这是继续上远程的量化理由。

另加"交接状态"注记：后台任务已结束、GPU 已释放、工作区干净，后续由用户接手。
