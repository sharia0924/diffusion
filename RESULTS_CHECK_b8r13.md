# 结果检查：用户 r_max 环带重定位实验（2026-09-20）

> 检查对象：用户在 9/19–9/20 的三轮解码器训练 + `r_max` 环带重定位实现。
> 数据来源：`results/strength_sweep_v2.md`、`strength_sweep_b8.md`、
> `strength_sweep_b8r13.md`、`robustness_b8r13.md`、`verification_report.md`
> 以及本次补跑的 `b8r13` 全套评测。

## 1. 用户在代码层面做了什么

| 文件 | 改动 | 作用 |
|---|---|---|
| `krd/pattern.py` | `ring_capacity` / `key_params` 增加 `r_max` | 限制环带外半径 = **把图案移到低频段** |
| `krd/stego.py` | `TrajStego(..., r_max=)` | 图案参数生成时带上 r_max |
| `scripts/train_decoder.py` | `--r-max` + 贯通 `fit_capacity` | 预算随 r_max 收窄，bpb 自动下调 |
| `scripts/eval_*.py` / `eval_setup.py` | 从 decoder config 回填 `r_max` | 保证训练/评测同一频段 |

**动机（正确）**：JPEG 对中高频量化最重，把载波移到低频可提升抗 JPEG 能力。
**另外还新增了 `scripts/verify_results.py`**（evaluation-only 复核工具）——
这个工具很有价值，我复核了它输出的 `verification_report.md`：
**R1/R2/R3 的复测值与声明值完全一致**（51.53 / 30.07 / 39.00 / 28.10 / 0.883 / 0.734），
说明之前的数字是可复现的。

## 2. 三个变体的配置

| 变体 | 载荷 | ecc | bpb | slots | inject_at | r_max | S |
|---|---|---|---|---|---|---|---|
| `decoder_latent32` | 16bit | 3 | 2 | 80 | None(1.0) | None | 150 |
| `decoder_latent32_v2` | 16bit | 3 | 4 | 80 | 0.35 | None | 150 |
| `decoder_latent32_b8` | 8bit | 3 | 6 | 56 | 0.35 | None | 150 |
| **`decoder_latent32_b8r13`** | **8bit** | 3 | 4 | 56 | 0.35 | **13** | 150 |

## 3. 关键结果对比（strength=0.3，S=150）

| 变体 | PSNR | dec clean | **dec jpeg50** | mf |
|---|---|---|---|---|
| 16bit, bpb2（原始） | 26.05 dB | 0.916 | 0.758 | 0.984 |
| 16bit, bpb4 (v2) | 26.05 | 0.916 | 0.758 | 0.984 |
| 8bit, bpb6 (b8) | 26.13 | **0.980** | 0.777 | 0.996 |
| **8bit, bpb4, r_max=13** | **27.10** | **0.984** | **0.836** | 0.992 |

### 结论 1：`r_max` 环带重定位是**净收益**
同等工作点下 **jpeg50 从 0.777 → 0.836（+0.059）**，同时 PSNR 还高 1.0 dB、
clean 准确率 0.980 → 0.984。用户这个方向**判断正确、实现正确**。

### 结论 2：降载荷到 8bit 的收益与预期一致（不大）
clean 从 0.916 → 0.980（+0.064），但 jpeg50 只从 0.758 → 0.777（+0.019）。
与我上一轮的分析一致：**降载荷不是主要杠杆**（每比特观测数从 10 → 14，约 +1.5 dB）。

## 4. b8r13 完整前沿（本次补跑，n=16）

| strength | PSNR | SSIM | dec clean | dec jpeg50 |
|---|---|---|---|---|
| 0.5 | 19.43 | 0.582 | **1.000** | **0.984** |
| **0.3** | **27.10** | 0.840 | 0.984 | 0.836 |
| 0.2 | 31.81 | 0.919 | 0.891 | 0.727 |
| 0.15 | 34.23 | 0.945 | 0.797 | 0.695 |
| 0.1 | 36.45 | 0.963 | 0.688 | 0.633 |

## 5. b8r13 全套鲁棒性（strength=0.3, n=16, PSNR 27.71 dB / SSIM 0.844）

| 攻击 | 准确率 | | 攻击 | 准确率 |
|---|---|---|---|---|
| clean | 0.930 | | crop2px / crop4px | 0.906 / 0.906 |
| jpeg30 / 50 / 75 | 0.789 / 0.789 / 0.797 | | **translate2px** | **0.516** |
| noise0.05 / 0.10 | 0.820 / 0.742 | | **rotate5 / 15** | **0.711 / 0.523** |
| blur3x3 / 5x5 | 0.891 / 0.906 | | **cropresize0.8** | **0.508** |
| resize0.5 / 0.7 | 0.891 / 0.852 | | **zoom1.1** | **0.633** |
| bright / contrast | 0.906 / 0.875 | | regen(t=400) | 0.727 |
| | | | wrong-key | 0.492 ✅ |

**新发现（值得注意）**：隐空间载体下 **`crop` 依然几乎无损（0.906）**，
但**平移/旋转/裁剪重采样全部塌到 0.51–0.71**（接近随机）。
这与像素空间阶段"crop 好、重采样差"的结论一致，且**隐空间下更严重**。

## 6. 与目标的差距（诚实结论）

| 指标 | 当前最好 | 论文可用线 | 差距 |
|---|---|---|---|
| PSNR（dec≥0.95 时） | 27.1 dB @s=0.3 | ≥ 35 dB | **−7.9 dB** |
| jpeg50（PSNR≥35 时） | 0.633 @s=0.1 | ≥ 0.95 | **−0.32** |
| clean（PSNR≥35 时） | 0.688 @s=0.1 | ≥ 0.95 | **−0.26** |

**要同时达标，需要把 s=0.3 那一行的 PSNR 从 27.1 抬到 35+（+8 dB），
或在 s=0.1 那一行把 jpeg50 从 0.63 抬到 0.95（+0.32）。两者目前都做不到。**

### 但有一个重要观察
**strength=0.5 时 clean=1.000、jpeg50=0.984** —— 准确率完全够，
只是 PSNR 掉到 19.4 dB。意味着：
> **问题不在"能不能解码"，而在"为了解码要付多少 PSNR"。**
> 这是一个纯信噪比/效率问题，而不是可达性问题。任何能提升"每单位扰动可解码性"的
> 手段（更强模型、更好编码、多步注入）都能直接平移整条前沿。

## 7. 建议的下一步（按预期收益）

1. **多步注入**（沿轨迹在多个 t 处注入）：把"单次注入必须很响"解耦成多次轻注入，
   是把前沿整体左移（同 PSNR 下更高准确率）最有希望的机制改动。
2. **更强的基础模型**：当前 DDPM 只有 5.17M 参数 / 100 epoch / 往返 39 dB@S=150。
   模型越强，采样对注入扰动的非线性畸变越小，整条前沿会左移。
3. **软判决 + BCH/LDPC**：当前是"重复码 + 实部均值"，换真 ECC 可省 3–6 dB。
4. **`r_max` 继续往低调**（如 10、8）：已证明有效，且实现就绪。
   可扫 r_max ∈ {15, 13, 11, 9} 找抗 JPEG 最优频段。
5. 抗几何：隐空间下 translate/rotate/cropresize 是硬伤，
   若论文定位为"抗压缩+抗再生"，需在 limitation 里明确。

## 8. 一处小的显示问题（非功能性）
`eval_robustness` 打印的容量报告仍显示 `r_max=15`，那是 `latent_capacity_report`
用默认环带算的**展示值**；实际图案用的是 checkpoint 里的 `r_max=13`。
建议把实际 `r_max` 传给容量报告，避免误读。
