# 重训后的结果分析（远程 Linux+CUDA 跑，2026-09-10 19:36 同步回本机）

> 来源：`results/pipeline_summary.md` 显示全部路径为 `/root/diffusion/...`，
> 说明这一轮是在另一台机器上执行 `run_pipeline.py --stages all` 后同步回本机的结果目录。
> **本机 `checkpoints/` 仍是 2026-09-10 11:56 的旧权重，未同步**，
> 因此本机无法复现这一轮数字；`python scripts/run_pipeline.py` 在本机会触发
> ddpm/decoder 阶段重训。引用这批结果时请注明环境。

## 0. 一句话结论

**这是一次"没有变量的重训"：架构、轮数、学习率、容量、strength 全部与上一轮相同，
唯一的新变量是几何失真进了训练混合（`--geom-prop 0.25`）。**
结果因此完全落在预期的同一档位：**载密图 PSNR 依旧 9.3–9.5 dB，
P2 的"零失配惩罚"依旧，几何短板依旧是旋转/平移/缩放。**
也就是说：**本次重训没有改变工作点，真正该动的三个杠杆一个都没动**（见 §4）。

## 1. 新旧结果对照

| 指标 | 旧（v1 口径） | 新（v2 口径，本轮） | 判读 |
|---|---|---|---|
| 载密图 PSNR / SSIM @strength=1 | 9.52 dB / 0.1023 | **9.52 dB / 0.1040** | 完全持平 |
| steps_grid @S_hide=50 | 9.53 dB | 9.26 dB | 持平（±0.3dB 属噪声） |
| p1 基线 | 9.31 dB | 9.41 dB | 持平 |
| step_mismatch 基线 | 9.56 dB | 9.29 dB | 持平 |
| 无嵌入 DDIM 往返 PSNR | 17.9 dB (n=48) | 17.4 dB (n=4, diag) | 持平 → **基础模型没变好** |
| clean / jpeg50 准确率 | 0.999 / 0.999 | 0.995 / 0.996 | 持平略降 |
| 错密钥 BER mean | 0.4975 | **0.4996** | 好 |
| 真密钥准确率 | 1.0000 | 0.9961 | 略降（几何失真训练换来） |
| 槽位定位 AUC @N=32（nonce） | 0.790 | 0.794 | 持平 |
| 盲检测 AUC（nonce） | — | **0.812** | 水印仍高度可检测 |

**判读**：三轮独立运行（旧本机 GPU、本机 CPU、本轮远程）在同一配置下给出
9.3–9.5 dB 的载密图质量，离散度 < 0.3 dB —— 这既说明评测已经稳定可复现，
也说明**这个工作点是被配置钉死的，不是随机波动**。

## 2. 本轮唯一真正的新结论：P3 的"攻击者困境"被数据否定

`results/regen.md`（n=32, S_hide=S_rec=50, strength=1.0，改口径后首次跑满 8 个点）：

| t_reg | regen steps | BER | 攻击代价 PSNR **vs cover** | PSNR vs stego |
|---|---|---|---|---|
| 200 | 25 | 0.025 | 8.33 | 12.09 |
| 400 | 25 | 0.119 | 8.93 | 9.59 |
| **600** | 25 | 0.324 | **9.71** | 8.89 |
| 800 | 25 | 0.432 | 9.43 | 8.41 |
| 200 | 50 | 0.031 | 8.42 | 13.34 |
| 400 | 50 | 0.113 | 8.71 | 9.51 |
| **600** | 50 | 0.289 | **10.19** | 8.86 |
| 800 | 50 | 0.418 | 9.58 | 8.23 |

- **载密图基线本身只有 9.44 dB（vs cover）**；
- BER 随 t_reg 单调上升（0.025 → 0.43），这是预期的"攻击有效"；
- **但攻击代价（vs cover）是倒 U 型**：t=200 最低（8.3–8.4），t=600 最高（9.7–10.2），
  也就是说**攻击者把水印抹到 BER≈0.3 时，图像相对原图的质量反而比温和攻击更好**；
- 右侧的 `vs stego` 单调下降（12.1 → 8.4）才是单调的，但它**不是读者关心的代价**。

**机制**：低 t_reg 时扩散模型主要在做"擦除我们注入的扰动"、朝自然图像流形回拉，
移动方向恰好和我们的扰动相反 → 相对 cover 的距离被部分抵消不了反而叠加；
高 t_reg 时内容被重生成，与原图无关，与 cover 的距离趋向"两张无关自然图"的基线。

**对论文的影响**：不能再用"BER↑ 必然伴随 PSNR↓"来论证"攻击者困境"。
正确的表述只能是：
> 攻击者在 BER 与"相对原图的信息保真"之间权衡；且当 t_reg 足够大时，
> 攻击者可以用与原图无关的重生成把 BER 推到 0.42–0.47（≈随机），
> 此时图像已不承载原图语义，攻击"成功"但不具备实用攻击价值。

## 3. 本轮其余结果

**P1（`results/key_security.md`，n=32, wrong keys=256）**

- 错密钥 BER mean 0.4996 / std 0.0236 / min 0.4336 / max 0.5664；真密钥 0.9961；
- matched-filter：真密钥距离 0/32（全部样本精确命中），错密钥 15.93±0.52，FAR@τ=4/8/12 = 0；
- 残差方向一致性：无 nonce **0.191** → 逐图 nonce **0.033**（接近正交）；
  对应槽位定位 AUC 0.972 → 0.794；
- **盲检测 AUC：无 nonce 1.000 → 逐图 nonce 0.812** —— 水印存在性几乎藏不住；
- 密钥：派生参数空间 1354.1 bit，**密钥本身熵 64 bit**（真实强度上限）。

**P2（`results/steps_grid.md` / `step_mismatch.md`）**

- 载密图质量：S_hide=10/25/50/100 → 14.11 / 10.28 / 9.26 / 9.11 dB；
- 准确率：S_hide≥25 时 0.988–0.994；**S_hide=10 掉到 0.730–0.807**（旧口径为 0.85–0.93）；
- 失配：S_rec 20→80 全 0.996–1.000（clean 与 jpeg50）—— **"零失配惩罚"依旧**。

**P3 前沿（`results/frontier.md`）**

| strength | PSNR | clean | jpeg50 | crop2px | rotate5 |
|---|---|---|---|---|---|
| 0.50 | 13.65 | 0.980 | 0.920 | 0.980 | 0.848 |
| 0.75 | 11.74 | 0.998 | 0.996 | 0.998 | 0.893 |
| 1.00 | 9.47 | 0.996 | 0.996 | 0.996 | 0.881 |
| 2.00 | 7.57 | 0.994 | 0.990 | 0.994 | 0.873 |

**关键读法**：把 strength 翻 4 倍（PSNR 13.65 → 7.57 dB），`rotate5` 只从
0.848 抬到 0.873 —— **几何鲁棒性对嵌入强度几乎不敏感**。
说明旋转/平移/缩放不是"信号不够强"，而是**载体（相位）本身不携带这类不变性**。
这一点直接指向 §4 的第 3 条：换载波，而不是继续加功率。

## 4. 诊断：为什么这次重训不可能改善

对照 `NEXT_STEPS.md` §3.1/§5 的清单，本轮与上一轮逐项比对：

| 该动的杠杆 | 是否动了 | 证据 |
|---|---|---|
| 训练更久 / 换预训练权重（目标往返 ≥28 dB） | ❌ 没动 | 仍是 `--epochs 60`；diag 往返 17.4 dB |
| 降低嵌入强度、把工作点移到 30 dB 以上 | ❌ 没动 | strength 仍固定 1.0；PSNR 9.5 dB |
| 提高每比特观测数（换 LDM / 提分辨率） | ❌ 没动 | 仍 32×32、160 对频点、80 槽位 |
| 换载波（相位 → 幅度域） | ❌ 没动 | `inject_pattern` 未改 |
| ECC 升级（重复码 → BCH/LDPC） | ❌ 没动 | `ecc_reps=3` |
| **几何失真进入训练** | ✅ 动了 | `--geom-prob 0.25`（唯一变量） |

所以本轮本质是**用同一个模型配置抽了第二次样**，期望它改善 PSNR 是不成立的。
唯一新变量（几何失真训练）的效果也很有限：`rotate5` 0.887 → 0.881，
`translate2px` 0.377 → 0.369（都还在 0.37–0.49 的"基本失效"区间）。

对照 `NEXT_STEPS.md` §2.3 的悬崖曲线：pertE≈0.09（约 13 dB）时准确率仍 1.000，
pertE≈0.014（约 17 dB）就跌到 0.78。本轮 strength=1.0 对应 pertE≈0.73，
**离悬崖还有 8 倍的能量余量**——所以这里一切"看起来很好"的准确率都不构成证据。

## 5. 下一步：三个必须动的杠杆（按性价比排序）

1. **先把 PSNR 曲线画出来（半天，零训练成本）**
   把 strength 扫到 **0.05 / 0.1 / 0.15 / 0.2 / 0.3 / 0.5**，
   并且**同时报告 `diag_psnr_probe` 的 `rel_perturb_energy`**。
   目的：找到"准确率开始掉"的 PSNR 临界点（预计在 12–16 dB 之间）。
   如果临界点在 15 dB 以下，说明当前配置**任何工作点都到不了 35 dB**，
   必须换配置；如果临界点能被 ECC 增益推到 20 dB+，再谈模型。

2. **提高每比特观测数（最高杠杆，1–2 天）**
   32×32 单通道半平面仅 ~176 个可用频点却要塞 80 个槽位 → 每槽 2 个观测。
   两条路：
   - 降容量：16 bit → 8 bit（每槽 4 个观测，+3 dB 聚合增益）；
   - 升维度：用 SD VAE latent 64×64×4（每槽 ~25 个观测，+11 dB）。
   先把"降容量"跑通，用来**验证增益模型是否成立**（如果 8 bit 能把临界点推高 3 dB，
   就证明聚合增益假设正确，再投入 LDM 迁移）。

3. **换载波：从相位域改到幅度域（1 天，改 `pattern.py`）**
   证据链已经完整：
   - `crop2px` 真裁剪切 0.995，但 `translate2px` 只有 0.369 —— 两者都移动了内容；
   - `rotate5=0.88` / `zoom1.1=0.85` / `cropresize0.8=0.48` —— 凡是**重采样**就崩；
   - strength 翻 4 倍几何准确率只涨 2.5 个百分点 —— 不是功率问题；
   - 训练里已经加了 `geom_prob=0.25`，学完也只有 0.88 —— **可学习性不是瓶颈**。
   结论：接收端"按密钥去旋取实部"这一步对空间对齐过度敏感。
   把判决量换成**幅度比** `|F[k]| / median(|F[ring]|)`（幅度天然对平移不变、
   对裁剪也基本保留），是唯一能同时救 `translate/rotate/cropresize` 的方向。

**顺手要做的一件事**：`pip install lpips`。
现在所有报告的不可见性只有 PSNR/SSIM，而 9.5 dB 下 PSNR 已经失去意义，
没有 LPIPS 无法判断"到底是色彩偏移、结构损坏还是高频噪声"。

## 6. 需要你确认的一件事

**checkpoint 要不要同步回本机？** 本机 `checkpoints/*.pt` 仍是 2026-09-10 11:56 的旧权重，
无法复现本轮结果；任何后续本地调试都会拿旧权重对照，容易得出错误结论。

> 附注：用 PowerShell `Get-Content` 读这些 md/log 会显示为 GBK 乱码，那是终端解码问题，
> 文件本身是 UTF-8（用编辑器的 read 工具读正常）。若在 Linux 侧生成，建议统一
> `PYTHONUTF8=1`（`run_pipeline.py` 已对子进程设置该变量）。
