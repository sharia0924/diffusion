# 本机 300-epoch 重训结果分析（2026-09-11）

> 环境：本机 GTX 1650 4GB，`D:\anaconda3\envs\yolo`（torch 2.5.0+cu118，amp off，batch 128）
> 训练：`run_pipeline.py --stages ddpm --ddpm-epochs 300`，由 60 epoch 断点续训到 300

## 0. 结论速览

| 指标 | 旧模型（60 epoch） | 新模型（300 epoch） | 变化 |
|---|---|---|---|
| 抗噪 MSE（末段平均） | 0.0315 | **0.0297** | −5.7% |
| **DDIM 往返 PSNR（无嵌入上限）** | 17.4–18.3 dB | **18.81 dB**（SSIM 0.6228） | +0.5～1.4 dB |
| 载密图 PSNR @strength=1.0（探针，matched-filter 接收） | 9.48 dB | **10.16 dB** | +0.7 dB |
| 环带能量占比 | 35.7% | 37.5% | 基本不变 |

**核心判断：训练本身完全成功（300 epoch 跑完、loss 正常下降、无 nan、断点续训生效），
但方向被证明是"到此为止"——继续加长训练无法突破 19 dB 的天花板。**

## 1. 训练过程质量检查

| 检查项 | 结果 |
|---|---|
| 目标 epoch | 300，实际完成 300 ✅ |
| 用时 | 289.1 min（4.82 h），平均 72.3 s/epoch（≈185 ms/step） |
| 断点续训 | ✅ 生效：日志首行 `[resume] ... 已完成 140 epoch, 继续训练到 300`，说明中途被打断过一次并成功续训 |
| loss 是否发散 | ✅ 无 nan/inf |
| EMA | ✅ 完整（5.17 M 参数，与 model 一致） |
| checkpoint | `ddpm_cifar.pt`（epochs_done=300）+ `ddpm_cifar.last.pt`（含优化器状态） |

**loss 曲线（每 20 epoch 抽样）**

| epoch | 159 | 179 | 199 | 219 | 239 | 259 | 279 | 299 |
|---|---|---|---|---|---|---|---|---|
| avg loss | 0.0307 | 0.0308 | 0.0305 | 0.0302 | 0.0302 | 0.0304 | 0.0302 | 0.0297 |

对照 60 epoch 时的 0.0315：**240 个 epoch 只换来 5.7% 的 MSE 下降，最近 150 epoch 基本平坦。**
这是典型的**收敛/容量饱和**信号，不是训练不足。

## 2. 决定性数字：往返 PSNR 只有 18.8 dB

`diag_psnr_probe` 在 n=16、S=50 步下的测量：

```
[Q1] DDIM roundtrip PSNR 18.81 dB  SSIM 0.6228      <- 不做任何嵌入的上限
[Q2] A0(median)=0.7665  A0/rms=0.906  ring-energy-share=0.3746
[Q3] replace s=1.0: pertE=0.728  PSNR 10.16  mf clean 1.000
```

**这意味着什么**：即使我们什么都不嵌入（`strength=0`），载密图相对原图的 PSNR
也**不可能超过 18.8 dB**。而论文工作点要求 ≥35 dB。

> **差了 16 dB，而且这 16 dB 与嵌入方案无关，完全由基础 DDPM 的往返重构保真度决定。**

同时，注入本身仍要付出约 8.7 dB（18.81 → 10.16，strength=1.0）。
也就是说当前配置下：

- 最好的可能（完美嵌入）：18.8 dB —— 仍不达标；
- 实际（strength=1.0）：10.2 dB。

## 3. 为什么加长训练救不回来

CIFAR-10 32×32 用 **5.17 M 参数**的 U-Net，训练 300 epoch = 117,000 optimizer step。
参考量级：CIFAR-10 上 FID ≈ 3–5 的 DDPM 通常需要 **~36 M 参数 / 800k step**（Ho 等人的原始设置）。
本模型参数少 **7 倍**、步数少 **7 倍**，且 loss 已完全平坦。

因此：

- ❌ **继续训到 1000 epoch**：loss 已平坦，预计往返 PSNR 最多再涨零点几 dB；
- ❌ **调 lr / 换 EMA 系数**：不是优化问题，是容量/步数问题；
- ✅ **换基础模型**：要么显著加大 U-Net + 步数（本机 4GB 不现实），
  要么直接用预训练的高保真生成模型 → **即 NEXT_STEPS 里的 LDM 迁移**。

## 4. 这如何改变项目的优先级判断

之前 `NEXT_STEPS.md` §5 把 LDM 迁移排在"第 3 步"，并写"真正的杠杆是每比特观测数 ∝ 分辨率²"。
**本次测量把 LDM 迁移从"质量优化"升级为"必要条件"**，理由更硬：

1. 现在这条像素空间路线的 PSNR 上限是 **18.8 dB**，无论怎么改嵌入公式都到不了 35 dB；
2. 隐空间路线之所以能突破，不只是"每比特观测数 ×12"，更是因为
   **VAE 解码器本身就把往返保真度拉到 30 dB+** —— 即先解决上限问题；
3. 在 18.8 dB 上限下继续调嵌入方案、调 ECC、调步数失配，都是在**给一个注定不达标的
   系统做局部优化**，投入产出比很低。

## 5. 建议的下一步

**立刻做（不用再训练）：**

1. 重训解码器 + 重跑全部评测，把结果表刷新到新基础模型上：
   ```bash
   python scripts/run_pipeline.py --stages decoder robust p1 p2 p3 --force
   ```
   预期：几何攻击、失配、安全性结论**基本不变**（因为载体机制没动），
   只有绝对 PSNR 从 9.5 → 10.2 dB 量级。这一步的价值是"让仓库里的结果与新权重一致"。

**接下来（项目方向决策）：**

2. **停止在像素空间继续加训练**。把算力转投隐空间路线：
   - 离线用 SD VAE 把 32×32/256×256 图片编码成 latent（64×64×4）；
   - 用现在这套已经跑通的 `train_ddpm.py`（含续训、大 batch）训一个 latent DDPM；
   - `krd/pattern.py` / `krd/stego.py` 与分辨率无关，可直接平移。
3. 或者，如果目标是尽快有"能看的数字"：直接在**预训练 SD 隐空间**上做
   注入 + 反演提取的实验（不训练生成模型），先把 PSNR/FID 量级确认下来。

**不要做：**

4. 不要再花 5 小时把 DDPM 训到 600/1000 epoch；
5. 不要在 9–19 dB 的工作点上继续深化 P2/P3 的消融，结论会被审稿人质疑"信号过强"。
