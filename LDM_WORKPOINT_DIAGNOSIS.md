# LDM 隐空间工作点诊断（2026-09-12）

> 本文档记录修正「尺寸/步数」类 bug 后暴露出的**机制层面**问题，以及实测证据与选项。

## 1. 已修完的问题（尺寸/配置类）

| # | 问题 | 表现 | 修复 |
|---|---|---|---|
| 1 | `run_pipeline` 漏传 `--resize` | DDPM 在 32² 上编码得 16×16 latent，评测指标全错 | 贯通 `--resize` + `train_ddpm.py` 尺寸一致性校验 |
| 2 | `train_decoder` 用 32² CIFAR | VAE 按 64² 训练 → latent 16×16，与 res=32 的图案网格不匹配 → CUDA gather 越界 | 按 VAE 的 `args.resize` 构造 Resize |
| 3 | `run_stego.py` 硬编码 32² + 绕过 StegoIO | 隐空间下必崩/结果错 | 改走 `eval_setup` + `StegoIO` |
| 4 | 评测步数硬编码 50 | 与 S=150 训练的解码器不匹配 → 准确率崩 | 8 个脚本改为从 decoder config 回填 |
| 5 | 图案网格与张量尺寸不匹配 | CUDA device-side assert（报错不可读） | `pattern._check_grid` 运行时护栏 |

## 2. 修正后的可用上限（无嵌入 DDIM 往返，latent 4×32×32）

| S_hide=S_rec | latent 相对 MSE | 图像 PSNR |
|---|---|---|
| 50 | 0.0498 | 29.40 dB |
| 100 | 0.0133 | 34.86 dB |
| **150** | **0.0061** | **38.00 dB** |
| 200 | 0.0034 | 40.25 dB |
| 250 | 0.0022 | 41.89 dB |

注意与 VAE 自身的 encode→decode（**51.8 dB，不含扩散**）区分——后者不是工作点上限。

**新默认工作点：S=150（38 dB 天花板）。**

## 3. 核心问题：**扩散采样会抹除 x_T 的小扰动**

`scripts/diag_floor2.py` 的对照实验（同一注入后的 `x_T'`，三条链路）：

| strength | A 直接解码 x_T' | B 采样回 x_0 后 | C B+VAE 重编码 | latent 相对 MSE (B) |
|---|---|---|---|---|
| 0.0 | 12.53 dB | **29.40 dB** | 29.22 dB | 0.0498 |
| 0.02 | 12.54 | **29.39** | 29.21 | 0.0513 |
| 0.1 | 12.54 | **29.53** | 29.35 | 0.0530 |
| 0.5 | 12.50 | **22.34** | 22.36 | 0.2244 |

（A 列数值低是正常的：x_T 是纯噪声，直接解码无意义；它是"注入是否改动了 x_T"的指示。）

**判读**：strength 0.02 与 0.1 相差 5 倍，B 列 PSNR 却完全相同（29.39 vs 29.53），
latent 相对 MSE 也几乎不变（0.0513 vs 0.0530）——
即**注入在采样过程中被完全擦除**，与注入幅度无关。
只有 strength ≥ ~0.3 的强注入才能存活（0.5 时相对 MSE 升到 0.224、PSNR 降到 22.3 dB）。

**机制**：DDIM 每步都要经学到的 `ε_θ` 再投影回数据流形，模型学到的低维流形会把
"离流形"的微小扰动投影掉；扰动必须有足够幅度才能留下痕迹。
这与 VAE 的编码-重编码无关（C 列只比 B 低 0.2 dB），也解释了此前观察到的
"PSNR 对 strength 不敏感、卡在 16 或 29 dB"。

## 4. 因此当前的真实权衡

```
strength ≈ 0     -> 29.4 dB（S=50）/ 38.0 dB（S=150），但**图案被抹除**，无法解码
strength ≳ 0.3   -> 图案存活，但 PSNR 掉到 ~22 dB（S=50）
```

也就是说：**"高质量"与"可恢复"目前是互斥的**，中间没有连续过渡
（这正是需要解决的问题，而不是调参能绕过的）。

## 5. 可选路线（按预期收益排序）

1. **在轨迹更晚的时刻注入（t_inject 而非 t=0）**：现在的流程是反演到最末端的 x_T
   再注入，之后要经过全部 S 步采样，放大/投影机会最多。改为反演到中间时刻 t*
   （如 t*=0.5T）注入、只走剩余少数步，可大幅减少被投影的次数。
   预期：同样 strength 下存活率显著提升。
2. **注入到模型敏感的频段**：当前环带 r_min..r_max 是均匀选的；改为按
   `|ε_θ|` 对注入的梯度/敏感度挑选频点（或选低频），让扰动更容易存活。
3. **载体改为"可学习方向"**：用少量样本学习一个在采样下近似不变的子空间
   （类似把载波从固定环带改为学到的低维投影），代价是训练复杂度上升。
4. **接受强注入 + 换指标叙事**：在 strength≈0.3 工作（~22 dB）下做全套实验，
   论文里以"鲁棒性优先"定位，并明确 PSNR 上限受此机制约束。

**建议先做 1**（改动最小、预期收益最大）：在 `TrajStego.hide` 里把
"反演到 x_T → 注入 → 完整采样"改为"反演到 t* → 注入 → 从 t* 采样"，
t* 作为协议参数（隐藏端自由、复原端固定，与现有"步数不对称"叙事天然兼容）。

## 6. 复现命令

```bash
python scripts/diag_floor2.py                 # 本文件 §3 的三链路对照
python scripts/audit_dims.py                  # 尺寸/配置类隐患扫描
python scripts/check_vae_eval.py              # 核对 VAE 真实质量与评测链路配置
python scripts/list_checkpoints.py            # 盘点 checkpoint 架构与进度
```
