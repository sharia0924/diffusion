# KRD-Steg 方案迭代记录

> 本文档是项目的**主迭代日志**：记录方案概述、技术路线、重要迭代、当前结果结论，
> 以及对目标 venue 与创新点强度的自评。每轮重要实验后应追加一节。
>
> 最近更新：2026-09-19

---

## 1. 方案概述

**KRD-Steg = 密钥条件化扩散轨迹隐写（Key-conditioned Reverse-Diffusion Steganography）**

核心思想：把秘密比特写进扩散模型的**初始噪声/潜变量轨迹**（而非像素），
用**闭式 DDIM 公式**做复原，用**密钥**门控比特在频谱上的位置与相位。

```
隐藏端（步数自由）:
  cover --VAE encode--> z --DDIM 反演(S_hide)--> z_T --密钥门控频谱注入--> z_T'
        --DDIM 采样--> 载密 z --VAE decode--> 载密图
复原端（步数固定、公式驱动、密钥门控）:
  载密图 --VAE encode--> z --DDIM 反演(S_rec)--> z_T --按密钥取频点+去旋--> 特征
        --RingDecoder(MLP)--> ECC 合并--> 比特
```

三个拟主张的创新点：

| 编号 | 主张 | 现状 |
|---|---|---|
| **C1** | **非对称步数设计**：隐藏端步数自由、复原端步数固定且由闭式 DDIM 公式驱动 | 机制成立；尚缺"为什么重要"的强论证 |
| **C2** | **密钥门控频谱图案 + 学习式密钥条件解码**：把 Tree-Ring 的固定指纹升级为用户密钥参数化的多比特信道 | 成立且已验证（错密钥 BER≈0.5、FAR=0） |
| **C3** | **失真感知轨迹复原**：可微 JPEG/噪声/几何 + 扩散再生攻击下的统一评测 | 评测协议完备；鲁棒性数字目前偏弱 |

---

## 2. 技术路线（实际走过的路径）

```
① 像素空间原型（CIFAR-10 32×32, U-Net DDPM, 容量 16bit）
   └─ 发现：往返上限仅 17.9 dB，注入后 9.5 dB，不可用
② 修正评测口径（真几何攻击 / nonce 盲提取 / 再生攻击代价 / 安全指标重定义）
   └─ 结论：此前所有"漂亮数字"均不可引用；真裁剪其实几乎无损
③ 加长训练到 300 epoch
   └─ 结论：往返上限只到 18.81 dB，loss 已饱和 —— 像素空间路线到头
④ 迁移 LDM 隐空间（本轮主体工作）
   ├─ VAE（native，f=2，latent 32×32×4）→ 64² 往返 51.5 dB
   ├─ 隐空间 DDPM（100 epoch）→ 往返质量随步数提升（见 §4）
   ├─ 隐空间解码器（S=150, 加性注入, 3 层 ECC + 32 校验位）
   └─ 评测链路全面适配（像素↔潜变量桥接、尺寸一致性）
⑤ 诊断出机制瓶颈："采样投影抹除小扰动" → 提出并在 PoC 中验证"轨迹中点注入"
```

---

## 3. 重要迭代记录

### 迭代 1：像素空间原型 + 评测口径修正
- **问题**：`crop` 攻击实为 `torch.roll`（循环平移）；再生攻击代价参照物是 stego 而非 cover；
  nonce 由 cover 派生导致方案非盲；把"槽位定位 AUC=0.79"当成攻击失败。
- **修复**：真裁剪 + 完整几何攻击矩阵；代价改以 cover 为参照；nonce 改 `H(key‖counter)`；
  安全指标重定义为盲检测 AUC + 残差一致性 + 真实密钥熵。
- **结论**：真裁剪几乎无损（0.992），真正短板是重采样类几何变换；
  嵌入能量占 x_T 的 36% 是质量差的直接原因。

### 迭代 2：像素空间加大训练量（60 → 300 epoch）
- **结果**：往返 17.4 → 18.81 dB；loss 0.0315 → 0.0297（后 150 epoch 基本平坦）。
- **结论**：**18.8 dB 是该配置的硬上限**，继续加训练无效 → 必须换空间。

### 迭代 3：LDM 迁移 + 一批尺寸/配置 bug
修复 5 类会导致结果无效的 bug：
| # | 问题 | 后果 |
|---|---|---|
| 1 | `run_pipeline` 漏传 `--resize` | DDPM 在 16×16 latent 上训练，与 VAE 的 32×32 不符 |
| 2 | `train_decoder` 用 32² CIFAR | latent 16×16 vs res=32 网格 → CUDA gather 越界 |
| 3 | `run_stego.py` 硬编码 32² + 绕过 StegoIO | 隐空间下必崩 |
| 4 | 评测步数硬编码 50 | 与 S=150 训练的解码器不匹配 |
| 5 | 图案网格与张量尺寸不匹配 | CUDA 断言不可读 |

并加了运行时护栏（`pattern._check_grid`）与审计脚本（`audit_dims.py`）。

### 迭代 4：注入方式 A/B —— 覆盖式是质量陷阱
| 模式 | s=0 | 0.02 | 0.05 | 0.1 | 0.3 |
|---|---|---|---|---|---|
| `replace`（原实现） | 35.1 | 15.7 | 15.6 | 15.6 | 15.3 |
| **`add`（新增）** | 35.1 | **34.5** | **33.3** | **31.0** | **23.6** |

`replace` 把选中频点的原系数（含相位）整体覆盖，一加注入就崩且与 strength 无关。
改用加性注入后质量随 strength 平滑可控。**这是隐空间可用的前提。**

### 迭代 5：步数上限扫描 —— 50 步是瓶颈
| S_hide=S_rec | 无嵌入往返 PSNR |
|---|---|
| 50 | 29.40 dB |
| 100 | 34.86 dB |
| **150（新默认）** | **38.00 dB** |
| 200 | 40.25 dB |

### 迭代 6：【核心发现】采样投影会抹除 x_T 的小扰动
三链路对照（同一注入后 `x_T'`）：
| strength | 采样回 x_0 后 PSNR | latent 相对 MSE |
|---|---|---|
| 0.0 | 29.40 | 0.0498 |
| 0.02 | 29.39 | 0.0513 |
| 0.1 | 29.53 | 0.0530 |
| 0.5 | 22.34 | 0.2244 |

strength 差 5 倍结果**完全相同** → 扰动在采样中被完全投影掉；
只有 strength ≳ 0.3 的强注入能存活。这解释了"PSNR 对 strength 不敏感"的全部现象。

### 迭代 7：解法 PoC —— 轨迹中点注入
| 注入时刻 t* | 图像 PSNR | mf 存活率 |
|---|---|---|
| 1.00（原做法，末端） | 9.39 dB | 0.453 |
| **0.50** | **32.40 dB** | **0.625** |
| 0.25 | 34.87 dB | 0.672 |

**PSNR +23 dB 且存活率更高**。原因：注入后剩余采样步数从 ~150 降到 75–112。
**尚未落地到 `TrajStego.hide`（下一步工作）。**

---

## 4. 当前结果结论（2026-09-12）

### 4.1 资产状态

| 组件 | 配置 | 质量 |
|---|---|---|
| VAE | native, f=2, latent **4×32×32**, 64² 输入, 25 epoch | 往返 **51.5 dB** |
| 隐空间 DDPM | U-Net base=64, 100 epoch, 5.17M 参数 | 往返 29.4 dB@50步 / **38.0 dB@150步** |
| 解码器 | S=150, 加性注入, 16bit+ecc3+32校验 | eval clean **0.979** / jpeg50 **0.960** |

### 4.2 端到端工作点（latent 4×32×32, S=150, n=8）

| strength | PSNR | SSIM | dec clean | dec jpeg50 |
|---|---|---|---|---|
| 0.02 | **38.81 dB** | 0.980 | 0.508 | 0.523 |
| 0.05 | 38.26 | 0.976 | 0.562 | 0.531 |
| 0.1 | 36.81 | 0.967 | 0.641 | 0.641 |
| **0.3** | **28.10 dB** | 0.865 | **0.883** | **0.734** |

**当前可用工作点：strength≈0.3 → PSNR 28.1 dB / jpeg50 0.734。**
（对比像素空间：9.5 dB / 0.99 —— 质量提升 18.6 dB）

### 4.3 鲁棒性与安全性（strength=0.3, n=16）

- 几何：`crop2px/crop4px` 良好；`rotate5=0.695`、`zoom1.1=0.621`、`translate2px=0.555`、
  `cropresize0.8=0.484`（**重采样类仍弱**）
- 再生攻击 t=400：0.641（vs cover 代价 7.84 dB）
- 错密钥：0.543（期望 0.5）✅
- 槽位定位 AUC：无 nonce 0.964 → 逐图 nonce **0.662**（去相关有效）
- 盲检测 AUC：无 nonce 1.000 → 逐图 nonce **0.750**（仍偏高）

### 4.4 结论

1. **LDM 迁移成功**：把可用的 PSNR 上限从 18.8 dB 抬到 38 dB（+19 dB），
   载密图质量从 9.5 dB 提升到 28–38 dB 区间——**这是本项目最关键的一步**。
2. **但"质量-可解码性"仍互斥**：38 dB 时解码 0.51（≈随机），
   0.88 准确率要付 PSNR 掉到 28 dB 的代价。
3. **根因已定位**：扩散采样对 x_T 扰动的流形投影（迭代 6），
   **解法已在 PoC 验证但未落地**（迭代 7）。
4. **数字尚未达到论文可用**：目标 PSNR≥35 dB 且 jpeg50≥0.95，目前无法同时满足。

---

## 5. 目标期刊/会议分析

### 5.1 当前成熟度评估

| 维度 | 状态 | 距离达标 |
|---|---|---|
| 机制新颖性 | 有一定新意（密钥门控 + 轨迹注入 + 步数不对称），但**与 Tree-Ring / ZoDiac / MDDM 的差异尚未用实验钉死** | 需补基线对比 |
| 结果强度 | PSNR 28 dB / jpeg50 0.73 | **不达标**（需 ≥35 dB & ≥0.95） |
| 评测完备性 | 高：13 种攻击 + 再生 + 盲检测 + 密钥空间 + 步数失配 | 已达标 |
| 安全性论证 | 中：错密钥/FAR/盲检测都有，但密钥恢复威胁模型只做了一半 | 需补 |
| 数据规模 | 仅 CIFAR-10 64² | **严重不足**（需 256²/512² + ImageNet/COCO） |

### 5.2 推荐 venue（按当前→潜在排序）

| venue | 类型 | 匹配度 | 说明 |
|---|---|---|---|
| **IEEE TIFS** | CCF-A 期刊 | ★★★★☆ | 隐写/水印主战场，**期刊周期长、可边做边补**，最适合当前阶段 |
| **IEEE TIP / TCSVT** | CCF-A 期刊 | ★★★☆☆ | 偏图像处理，需强视觉质量与大规模实验 |
| **ACM MM** | CCF-A 会议 | ★★★☆☆ | 接收面较宽，需在截稿前拿到达标数字 |
| **NeurIPS / ICML** | CCF-A 会议 | ★★☆☆☆ | 需更强理论（如"注入存活性"的解析刻画） |
| **CVPR / ICCV** | CCF-A 会议 | ★★☆☆☆ | 偏好视觉冲击与大模型 demo，需 512² + FID 对比 |
| **IH&MMSec / WIFS** | 专业会议 | ★★★★★ | 隐写领域专门会议，**可作为 workshop/短文先发** |
| **arXiv 预印本** | — | — | 建议先挂，锁定时间戳 |

**建议策略**：以 **TIFS 为主线**（周期容忍迭代），同时投 **IH&MMSec/WIFS** 短文占位；
若迭代 7 落地后数字达到 PSNR≥35 dB & jpeg50≥0.95，再考虑冲 ACM MM / CVPR。

### 5.3 创新点强度自评（诚实版）

**够有力的部分**：
- **C2（密钥门控多比特信道）**：把 Tree-Ring 的 0-bit 固定指纹升级为**用户密钥参数化的
  16-bit 信道**，并给出错密钥 BER≈0.5、FAR=0、盲检测 AUC 的完整量化 —— 这是可辩护的增量。
- **C3 的评测协议**：预算化再生攻击（同时报告攻击代价）+ 盲检测 AUC + 步数失配曲线，
  比现有工作更完整，可作为"评测贡献"。

**不够有力的部分（审稿人会打的点）**：
1. **C1"步数不对称"目前只是工程描述**，缺"为什么必须这样"的理论论证。
   若要立住，需要给出：复原步数与 BER 的解析关系，或证明某类载体对步数不敏感。
2. **与 MDDM / Training-Free Robust Generative Steganography 的差异未用实验证明** ——
   提案自己就把这两篇列为风险项，**投稿前必须复现并同表对比**。
3. **容量仅 16 bit**，而竞品动辄 32–256 bit；16 bit 会被质疑实用性。
4. **抗几何攻击弱**（rotate5=0.70、cropresize=0.48），而这是全局频域载体的通病，
   需要明确作为 limitation 或改用幅度域/同步标记。
5. **"攻击者困境"叙事已被自己的数据否定**（再生攻击代价随攻击强度非单调），
   必须重写为"BER–保真度联合权衡"。

**结论**：**当前创新点强度 = 中等偏上，但尚不足以支撑 CCF-A 主会**。
需要补三件事才能到"够有力"：
(a) 迭代 7 落地并把指标做到 PSNR≥35 dB & jpeg50≥0.95；
(b) 与 Tree-Ring/ZoDiac/MDDM 的同环境对比表；
(c) C1 的理论刻画或改用"注入存活性"这一更有解释力的新主张。

---

## 6. 下一步路线（优先级排序）

> **状态（2026-09-19 晚）**：P0 已完成（§8）；P1 杠杆 1（8bit 载荷）已完成（§9.4，
> 解码 0.95 工作点 +5.2 dB）；杠杆 2（解码器收敛）经诊断剩余空间有限（§9.5）；
> **瓶颈已定位为载体级 JPEG 脆弱性 → 首选杠杆改为环带重定位（§9.5）**，
> 之后重判是否触发 §6 路线转向。

| 优先级 | 任务 | 预期收益 | 预计工时 |
|---|---|---|---|
| **P0** | 在 `TrajStego.hide` 实现 `inject_at`（t*），把"末端注入"改为"轨迹中点注入" | PSNR +23 dB（PoC 已验证） | 2–3 h |
| **P0** | 扫 t* × strength，找 PSNR≥35 dB 且 jpeg50≥0.95 的工作点 | 决定项目能否成立 | 3–4 h |
| **P1** | 用选定工作点重训解码器 + 全套评测 | 论文主表 | 2 h |
| **P1** | 提升容量到 64–256 bit（32×32×4 latent 预算 1368 对，够用） | 回应"容量太小" | 2 h |
| **P2** | 复现 Tree-Ring / ZoDiac 做同环境对比 | 回应 novelty 质疑 | 3–5 天 |
| **P2** | 分辨率升到 256²（CIFAR 上采样或换数据集）+ FID/LPIPS | 回应"规模太小" | 1–2 天 |
| **P3** | C1 的理论刻画（步数-BER 关系）或改主张为"注入存活性" | 提升理论强度 | 1 周 |

**关键判断点**：P0 的两个任务做完后，如果无法同时达到
**PSNR ≥ 35 dB 且 jpeg50 ≥ 0.95**，则当前载体方案不成立，
应转向"幅度域载体"或"同步标记 + 仿射对齐"路线（见 `LDM_WORKPOINT_DIAGNOSIS.md` §5）。

---

## 7. 相关文档索引

| 文档 | 内容 |
|---|---|
| `PROPOSAL.md` | 最初方案与文献综述 |
| `NEXT_STEPS.md` | 像素空间阶段的诊断与路线 |
| `RESULTS_ANALYSIS_local300.md` | 像素空间 300 epoch 结果分析 |
| `RESULTS_ANALYSIS_v2_retrain.md` | 评测口径修正后的结果分析 |
| `LDM_MIGRATION.md` | LDM 迁移实现说明与验收标准 |
| **`LDM_WORKPOINT_DIAGNOSIS.md`** | **尺寸类 bug 清单 + 采样抹除机制 + t\* 解法** |
| `TRAINING_TUNING.md` | 训练/显存/速度配置 |
| 本文档 | 方案概述、技术路线、迭代记录、结论、选刊与创新点自评 |

---

## 8. P0 执行结果（2026-09-12 续）

### 8.1 已完成：`inject_at` 落地并全链路贯通

`TrajStego.hide(..., inject_at=frac)`：把注入时刻从轨迹末端提前到 `frac` 位置
（`k = round(frac × S_hide)` 步反演 → 注入 → k 步采样回 x_0）。
已贯通：`train_decoder --inject-at` → decoder config → `eval_setup` → `StegoIO.inject_at`
→ 全部评测脚本自动继承。回归测试：`tests/inject_at_test.py` 通过。

### 8.2 P0 扫描结果（latent 4×32×32, S=150, n=4, matched-filter 接收）

| inject_at | strength | PSNR | mf 准确率 |
|---|---|---|---|
| 1.00（原做法） | 0.1 | 36.91 dB | 0.766 |
| 1.00 | 0.3 | 26.54 dB | 0.938 |
| 0.50 | 0.3 | 27.33 dB | 0.938 |
| **0.35** | **0.3** | **27.70 dB** | **0.938** |
| 0.25 | 0.3 | 27.51 dB | 0.922 |

**结论（与 PoC 的差异需要说明）**：
- PoC 报的 "inject_at=0.5 → 32.4 dB" 是在 **strength=0.1、S=150、且采样步数也用 150**
  的条件下测的；本轮正式扫描显示：**inject_at 对 PSNR 的收益在强注入区并不显著**
  （0.3 强度下 1.0/0.5/0.35 分别是 26.54/27.33/27.70 dB，仅 +1.2 dB）。
- **更重要**：mf 准确率在 strength≥0.3 处**饱和于 0.938**，把 inject_at 从 1.0 调到 0.25
  也无法突破 —— 说明**瓶颈不是"扰动被抹除"，而是"每比特观测数不足"**。

### 8.3 找到真正的杠杆：增加每比特观测数

容量报告显示当前只用掉 **160/1368** 对频点（8.6 倍余量未用）。提高 `bins_per_bit`：

| bpb | 实际对数 | strength | PSNR | mf 准确率 |
|---|---|---|---|---|
| 2 | 160 | 0.1 | 30.10 dB | 0.625 |
| 2 | 160 | 0.2 | 30.63 dB | 0.766 |
| **4** | **320** | **0.1** | **29.35 dB** | **0.781** |
| **4** | **0.2** | **28.85 dB** | **0.922** |
| 4 | 320 | 0.2 | 28.85 dB | 0.922 |

**在几乎相同的 PSNR（30.6 → 28.9 dB，仅 -1.7 dB）下，准确率从 0.766 提升到 0.922**
（+0.156）。这是目前找到的**性价比最高的杠杆**。

### 8.4 当前最优工作点与差距

| 指标 | 当前最优 | 论文可用线 | 差距 |
|---|---|---|---|
| PSNR | 28.9 dB | ≥ 35 dB | -6.1 dB |
| jpeg50 准确率（训练解码器） | 0.734 @28 dB | ≥ 0.95 | -0.22 |

### 8.5 剩余可选杠杆（按预期收益）

1. **降低载荷**：16 bit → 4–8 bit。每比特观测数翻 2–4 倍，是隐写领域的标准折中，
   预期能把准确率推到 0.95+ 而不牺牲 PSNR。
2. **软判决 + 真 ECC**：当前是"重复码 + 实部均值"，换成 BCH/LDPC 软译码可再省 3–6 dB。
3. **重训解码器用新工作点**：现有解码器是在 `inject_at=1.0, bpb=2, S=150` 下训练的；
   改用 `inject_at=0.35, bpb=4` 重训（约 1 h）应能显著超过当前 0.734。

### 8.6 下一步（P1 修订版）

```powershell
# 用新工作点重训解码器（inject_at=0.35, bpb=4）
python scripts/run_pipeline.py --ldm --stages latent_decoder --force `
    --hide-steps 150 --rec-steps 150
# 注：bpb 由 fit_capacity 自动上调到 4（res=32 预算 342 对）
python scripts/train_decoder.py --ddpm-ckpt checkpoints/ddpm_latent32.pt `
    --out checkpoints/decoder_latent32.pt --steps 300 --batch-size 16 `
    --inject-mode add --inject-at 0.35 --strength-min 0.1 --strength-max 0.3 `
    --hide-steps 150 --rec-steps 150 --eval-every 100
```
然后跑 `eval_ldm_pipeline.py` + `eval_strength_sweep.py` 确认能否达到
PSNR≥35 dB 且 jpeg50≥0.95；若仍不达标，则执行 8.5 第 1 项（降载荷到 8 bit）。

---

## 9. P1 执行与独立复核（2026-09-19）

### 9.1 独立复核：声明数字全部复现（evaluation-only，零训练）

新增 `scripts/verify_results.py`：用现有 checkpoint 独立重验 §4/§8 的关键声明
（VAE 往返 / 无嵌入往返 / 端到端工作点 / 密钥安全 / 投影抹除），报告见
`results/verification_report.md`。

| 复核项 | 声明值 | 复测值 | 判定 |
|---|---|---|---|
| VAE 往返上限 | 51.53 dB | 51.53 dB | ✅ 精确 |
| 端到端 s=0.3: PSNR/dec/jpeg50 | 28.10 / 0.883 / 0.734 | 28.10 / 0.883 / 0.734 | ✅ 逐位一致 |
| 端到端 s=0.1: PSNR/dec | 36.81 / 0.641 | 36.81 / 0.641 | ✅ 逐位一致 |
| 无嵌入往返 @S=50/150 | 29.40 / 38.00 dB | 30.07 / 39.00 dB | ✅ ~1dB（样本集不同） |
| 错密钥 BER | 0.4977±0.040 | 0.4997±0.043 | ✅ |
| 定位 AUC @N=8（无nonce/ours） | 0.964 / 0.662 | 0.965 / 0.658 | ✅ |
| 投影抹除（S=50, s=0.05 vs 0.1） | 应几乎相同 | 30.16 / 30.34（基线 30.07） | ✅ 确认 |

端到端逐位一致源于固定种子（bits 用 seed=0 生成）——结果可复现是好事，但也意味着
**n=8 的单次抽样波动没有被平均掉**。

**复核发现的两个稳健性问题（此前未记录）**：
1. **FAR@τ=12 不稳健**：原 76 错密钥抽样得 0.037，复测（20 键×8 图）得 0.138，
   与理论 Binomial(32,0.5) 的 P(X≤12)≈0.11–0.15 一致。**论文引用 FAR@τ=8（两次均为 0，
   离均值 2.8σ），τ=12 需加大样本或弃用。**
2. **真密钥解码 acc 的密钥抽样方差 ≈±0.05**（0.898 → 0.844）：n=8 = 128 比特，
   置信区间很宽。**正式评测必须 n≥64 并报 mean±std。**

### 9.2 P1 落地：v2 解码器（bpb=4 + inject_at=0.35）重训与扫描

`checkpoints/decoder_latent32_v2.pt`：n_bits=16, ecc=3, **bpb=4（320/342 对）**,
inject_mode=add, **inject_at=0.35**, S=150/150, 1000 步（旧解码器 bpb=2/inject_at=1.0 保留作对照）。
训练内评测（分布外 strength=1.0）：clean=0.988 / jpeg50=0.969。

strength 扫描（n=32，`results/strength_sweep_v2.md`）：

| strength | PSNR | mf | dec clean | dec jpeg50 |
|---|---|---|---|---|
| 0.3 | 26.05 | 0.984 | **0.916** | 0.758 |
| 0.2 | 31.07 | 0.906 | 0.840 | 0.676 |
| 0.15 | 33.95 | 0.818 | 0.768 | 0.633 |
| 0.1 | 36.73 | 0.727 | 0.695 | 0.596 |
| 0.05 | 38.77 | 0.615 | 0.604 | 0.549 |
| 0.02 | 39.32 | 0.545 | 0.523 | 0.512 |

与旧解码器同坐标对比：s=0.1 同 PSNR（36.7dB）clean 0.641→0.695；s=0.3 clean 0.883→0.916
（PSNR 28.10→26.05，因频点对翻倍总能量略增）。**bpb=4 杠杆确认有效但幅度有限。**

**判定点结论：PSNR≥35 dB 且解码≥0.95 仍未达成**——35dB 附近仅 ~0.7，0.9+ 需要 26dB。

### 9.3 新发现：matched-filter 全面优于训练解码器

所有 strength 下 mf > dec（s=0.3: 0.984 vs 0.916）。MLP 训练 1000 步后 loss 仍在
0.57–0.62（80 槽位输出 + strength/失真混合分布），未收敛到位。

两个推论：
1. **解码器侧仍有免费提升空间**（加长训练 / 简化输出头 / 只学消息槽位），优先级低于载荷杠杆；
2. **mf 本身是论文卖点**：免训练、密钥门控的盲接收机在 26dB 处达 0.984——可作为
   "训练无关接收"基线写入方法（接收端无需信任/部署解码器权重）。

### 9.4 杠杆 1 实测：8bit 载荷重训（已完成）

`checkpoints/decoder_latent32_b8.pt`：n_bits=8, ecc=3, check=32 → 56 槽位 × **bpb=6**
（fit_capacity 自动上调，336/342 对），**每消息比特观测数 12→18（+50%）**；其余工作点不变，
1000 步。训练内评测（分布外 strength=1.0）：**clean=1.000 / jpeg50=0.996**。

strength 扫描（n=32，`results/strength_sweep_b8.md`），与 16bit v2 解码器对比：

| strength | PSNR | 16bit dec | **8bit dec** | Δ | 8bit mf | 8bit jpeg50 dec |
|---|---|---|---|---|---|---|
| 0.3 | 26.13 | 0.916 | **0.980** | +0.064 | 0.996 | 0.777 |
| 0.2 | 31.30 | 0.840 | **0.914** | +0.074 | 0.957 | 0.699 |
| 0.15 | 34.09 | 0.768 | **0.859** | +0.091 | 0.906 | 0.641 |
| 0.1 | 36.64 | 0.695 | **0.781** | +0.086 | 0.812 | 0.613 |
| 0.05 | 38.56 | 0.604 | 0.645 | +0.041 | 0.684 | 0.574 |

- **解码 0.95 工作点 PSNR：~23.5 dB（16bit）→ ~28.7 dB（8bit），+5.2 dB**；
- 解码器与 mf 的差距大幅收窄（clean: 0.980 vs 0.996 @s=0.3）——杠杆 2（解码器收敛）
  的剩余空间已不大；
- **判定线仍未达成**：34dB 处 0.859，0.95 需 ~29dB，距 35dB@0.95 还差 ~6dB。

### 9.5 JPEG 瓶颈定位（免训练 mf 失真诊断，2026-09-19）

对 8bit 接收机在失真下测 mf（mf = 无学习接收的 ceiling 估计）：

| strength | clean | jpeg50 | jpeg75 | noise0.05 |
|---|---|---|---|---|
| 0.3 | 0.992 | 0.852 | 0.836 | 0.844 |
| 0.2 | 0.953 | **0.609** | 0.719 | 0.742 |

**结论：JPEG/噪声短板主要是载体层面而非解码器层面**——图案所在环带（res=32, r∈[3,15]）
覆盖的中高频系数在 JPEG 量化下大量归零，mf 上限本身塌陷（s=0.2 时 jpeg50 仅 0.609）。
训练解码器已把该上限补回一部分（jpeg50: dec 0.699 vs mf 0.609 @s=0.2）。

由此，剩余杠杆的优先级重排：
1. **环带重定位（载体级，推荐首选）**：把 r_max 从 15 降到 ~8–10（JPEG 鲁棒频段），
   容量换 JPEG 存活率（隐空间 4 通道预算充足，需重算 `latent_capacity_report`）；
2. **软判决/真 ECC（编码级）**：mf jpeg50=0.852@26dB 对应 BER≈0.15，BCH/LDPC 软译码
   有望拉回 0.95+；
3. 解码器加长训练：与 mf 差距已小，预期收益最低。

### 9.6 当前差距（更新 §8.4/§9.5）

| 工作点 | clean | jpeg50 | 论文可用线（35dB & 0.95） |
|---|---|---|---|
| 8bit @s=0.3（26.1 dB） | 0.980 | 0.777 | clean 达标线差距小；jpeg 是短板 |
| 8bit @s=0.15（34.1 dB） | 0.859 | 0.641 | 未达标 |
| 8bit @s=0.2（31.3 dB） | 0.914 | 0.699 | — |

判定：**"35dB 且全通道 ≥0.95" 在当前环带设计下不可达**（JPEG 载体上限限制）；
执行环带重定位后需重判。若重定位 + 软 ECC 后仍不达标，则触发 §6 路线转向
（幅度域载体 / 同步标记 + 仿射对齐），或把主张改为"clean 通道 35dB@0.95 +
JPEG 通道的显式容量-鲁棒性权衡曲线"。

---

## 9.7 A 方案（环带重定位）训练准备就绪（2026-09-19 深夜，训练由用户启动）

### 已落地的代码支持（`--r-max` 全链路贯通）

`ring_capacity` / `key_params` / `TrajStego` / `fit_capacity` / `load_stego` /
`train_decoder --r-max`（写入 config）/ `eval_setup` / `eval_strength_sweep` /
`eval_ldm_pipeline` 全部支持 r_max 参数；评测端从解码器 config 自动回填（与
inject_at/steps/bpb 同机制）。2 步 dry-run 已验证（bpb 自动 6→4、config 回填正确，
探针产物已清理）。历史行为（r_max=None）逐位不变，冒烟测试通过。

### 频点预算表（res=32 单通道, r_min=3, 8bit: 56 槽位）

| r_max | 可用对 | bpb | 每消息比特观测 | 说明 |
|---|---|---|---|---|
| 15 | 342 | 6 | 18 | 现行基线（JPEG 中高频脆弱） |
| **13** | **252** | **4** | **12** | **推荐**：砍掉最脆弱的最外两环，SNR 等效代价约 -1.7dB |
| 12 | 208 | 3 | 9 | 更保守的备选 |
| 10 | 146 | 2 | 6 | 观测数过低，预期抵消 8bit 收益（原方案 8–10 据此上修） |
| 8 | 86 | 1 | 3 | 仅极低频，容量不足 |

> 注：原 A 方案拟取 r_max=8–10，但精确容量计算显示该区间每比特观测数只剩 3–6
> （现值 18），大概率得不偿失；故推荐 **r_max=13**（必要时 12）作为实验点。

### 明天的启动命令（依次执行）

```powershell
# 1) 主实验: r_max=13 重训 8bit 解码器（约 1.5-2h；bpb 自动 6->4）
python scripts/train_decoder.py --ddpm-ckpt checkpoints/ddpm_latent32.pt `
    --out checkpoints/decoder_latent32_b8r13.pt --steps 1000 --batch-size 16 `
    --n-bits 8 --bins-per-bit 6 --r-max 13 --inject-mode add --inject-at 0.35 `
    --strength-min 0.1 --strength-max 0.3 --hide-steps 150 --rec-steps 150 `
    --eval-every 250 --eval-size 32

# 2) 同口径 strength 扫描（对照基线 = results/strength_sweep_b8.md, r_max=15）
python scripts/eval_strength_sweep.py --ddpm-ckpt checkpoints/ddpm_latent32.pt `
    --decoder-ckpt checkpoints/decoder_latent32_b8r13_best.pt --n 32 --batch 8 `
    --hide-steps 150 --rec-steps 150 --strengths 0.3,0.2,0.15,0.1,0.05,0.02 `
    --out results/strength_sweep_b8r13.md

# 3) mf 失真诊断（与 §9.5 同口径, 检验 JPEG 上限是否抬升）: 把
#    verify 里的 mf 脚本换 decoder_ckpt 即可, 或直接看扫描表中 jpeg50 dec 列

# 4) (可选, 若 r_max=13 达标趋势明显) 更保守点 r_max=12 再训一组做消融
```

### 判定规则（实验后重判）

- **成功**：同 PSNR 下 jpeg50 dec 相比 r_max=15 基线（0.777@26dB / 0.699@31dB）
  提升 ≥0.08，且 clean 通道回落 ≤0.03 → 环带重定位有效，继续叠加软判决 ECC 冲线；
- **失败**：jpeg50 无提升或 clean 大幅回落 → 载体级 JPEG 问题不在环带位置
  （而在 VAE 往返的频谱均衡），触发 §6 路线转向评估。
---

## 10. 多步注入（n_inject）实现与实测（2026-09-20）

> 编号说明：本节与 §9 曾同为"9."（§9 是 P1 那一节，README 引用的也是那一节），
> 现把本节改为 §10 以消除歧义。

### 10.1 实现
`TrajStego.hide(..., inject_at=frac, n_inject=n)`：在轨迹**下坡段**的 k 步里等距选 n 个时刻，
每处注入一次相同图案。**不增加反演/采样步数**（仍是 k 步采样），计算开销与 n=1 相同。
贯通：`train_decoder --n-inject`（内部按 `strength/n` 保持总能量守恒）→ decoder config
→ `eval_setup` → `StegoIO.n_inject` → 全部评测脚本自动继承。

### 10.2 实测（latent 4×32×32, S=150, inject_at=0.35, n=8 图, matched-filter 接收）

| 总能量 E | n=1 PSNR / mf | n=4 | n=8 |
|---|---|---|---|
| 0.20 | 30.69 / 0.820 | 31.12 / 0.820 | **31.14 / 0.836** |
| 0.30 | 28.97 / 0.938 | 30.49 / 0.945 | **30.65 / 0.961** |
| 0.40 | 25.39 / 0.992 | 27.51 / 0.992 | **27.73 / 0.992** |

### 10.3 结论（含对上一轮预测的更正）

1. **准确率主要由总注入能量决定**，把同样的能量拆成 n 次注入**不会**显著提高准确率
   （E=0.2/0.4 时 mf 完全不变；E=0.3 时 +0.023）。
   —— 我上一轮预测"多步注入能把前沿整体左移"**过于乐观**，实测只左移了约 0.02 准确率。
2. **但 PSNR 稳定提升 0.5–2.1 dB**（E=0.4 时 25.39 → 27.73 dB），
   因为峰值扰动更小、能量沿轨迹摊得更匀。**这是零额外计算换来的净收益**，
   属于 Pareto 改进（同准确率下质量更好）。
3. **首次出现 mf ≥ 0.95**：E=0.30, n=8 时 mf=0.961（对应 PSNR 30.65 dB）。
   这比之前的 b8r13 前沿（s=0.3 → 27.7 dB / jpeg50 0.836）更好，
   但仍是 **30.65 dB，距 35 dB 还差 4.4 dB**。

### 10.4 当前最优工作点（更新）
| 目标 | 配置 | PSNR | mf |
|---|---|---|---|
| mf ≈ 0.95 | 原有设计 + n_inject=8, E=0.3, inject_at=0.35 | **30.65 dB** | 0.961 |

### 10.5 下一步
- 把 `n_inject=8` 与已有的 `r_max=13`、8bit 载荷组合，重训解码器（预期 PSNR 再 +1~2 dB）；
- 若仍需 4 dB：**换更强的基础 DDPM**（当前 5.17M/100 epoch）是唯一剩下的结构性杠杆；
- 或者按 P3 重新定位：以"鲁棒性优先"（30 dB + jpeg50≥0.9）作为卖点，
  把 PSNR 上限写入 limitation。

---

## 11. 三机制组合（8bit + r_max=13 + n_inject=8）与评测口径 bug 修复（2026-09-20）

本轮的目标只有一个：把三个**各自独立验证有效**的机制叠到同一个解码器上——
8bit 载荷（clean +0.064）、`r_max=13`（jpeg50 +0.059、PSNR +1.0 dB）、
`n_inject=8`（同准确率 PSNR +0.5~2.1 dB）。三者作用在不同环节，理应可叠加。

训练配置（`decoder_latent32_combo.pt`）：

```
--n-bits 8 --ecc-reps 3 --r-max 13 --inject-mode add --inject-at 0.35 --n-inject 8
--strength-min 0.15 --strength-max 0.4 --sched-noise-prob 0.3 --geom-prob 0.25
--hide-steps 150 --rec-steps 150 --steps 600 --batch-size 16
```

### 11.1 发现并修复了一个会"吞掉"全部多步注入收益的评测口径 bug

组合模型训练的第一次 strength 扫描结果与 `b8r13` 基线**逐位相同**
（PSNR 27.10、mf 0.953、jpeg50 0.781），这本身是不可能的巧合，顺藤摸到两个叠加的错误：

1. `train_decoder` 落盘 decoder config 时**漏写 `n_inject`** → 评测端
   `eval_setup` 回填成默认值 1，于是"用 8 次注入训出来的解码器"在评测时只注入 1 次；
2. 修好回填后 PSNR 反而崩到 **6.02 dB**：`krd/stego.py` 的 `n_inject=n` 是把一次注入
   拆成 n 次、每次 `strength/n`（**总能量守恒**），训练端自己除了 n，
   而评测端 `StegoIO.hide` 把 `strength` 原样传下去，**总能量被放大 8 倍**。

修复（commit `95d8b87`）：`StegoIO.hide` 明确把 `strength` 定义为**总注入能量**并按
`n_inject` 均摊；`n_inject=1` 时与历史行为逐位一致（老 checkpoint 不受影响）；
`train_decoder` 补写 `n_inject`。同时加了 `tests/strength_semantics_test.py`
（假 stego 记录传参，不需要 GPU，覆盖均摊/总能量守恒/n=1 兼容/张量强度/显式覆盖五种情形）。

修复后同口径对照（`scripts/check_ninject.py`，n=4, S=150, s=0.3）：

| 解码器 | n_inject | bpb | r_max | 载密图 PSNR |
|---|---|---|---|---|
| `decoder_latent32_b8r13_best.pt` | 1 | 4 | 13 | 27.88 dB |
| `decoder_latent32_combo_best.pt`（同参数 + n_inject=8） | 8 | 4 | 13 | **28.85 dB** |

**+0.97 dB 是纯白拿的**（零额外计算），且这正是 §10 在 matched-filter 接收端测到的
"多步注入 PSNR 增益"在**训练解码器端**的复现——两个独立口径互相印证。

> 教训（已写进 `tests/strength_semantics_test.py` docstring）：这类"训练/评测口径漂移"
> 的 bug 表现很隐蔽——**准确率看着正常，只有 PSNR 崩**，极易被误读成"这个机制没用"。
> 凡是"训练端做了一次变换、评测端没做"的参数（`strength/n`、`inject_at`、`r_max`、
> `hide_steps`），都必须有一条"训练与评测同源"的断言或回归测试兜底。
> 顺带把同一类隐患的第三处也堵了（commit `c26e15f`）：`run_pipeline.py --ldm` 的
> `latent_decoder` 阶段此前不传 `inject_at/n_inject/r_max`，会用默认值训出一个
> **不是最好那个**的解码器，现在改成 CLI 贯通 + 打印工作点 + 写入 summary。

### 11.2 组合模型评测（S=150, 8bit, bpb=4, r_max=13, inject_at=0.35, n_inject=8）

训练在 16:02 结束（600 步，`_best.pt` 与最后一步权重一致——已用状态字典哈希核对）。
注意：**该 checkpoint 的 config 缺 `n_inject`（训练进程用的是修复前的代码）**，
训练本身用了 `--n-inject 8`（`hide_space` 的除 n 逻辑在旧代码里已存在），
只是落盘时漏了字段，故已用 `scripts/patch_decoder_cfg.py` 补写
`n_inject=8 / eval_strength=1.0 / strength∈[0.15,0.4]`，并用 `check_ninject.py` 复核。

strength 扫描（`results/strength_sweep_combo.md`，n=24，S=150，corrected 评测路径）：

| 总能量 E | PSNR | SSIM | mf | dec clean | dec jpeg50 |
|---|---|---|---|---|---|
| 0.15 | 30.69 | 0.9140 | 0.802 | 0.792 | 0.615 |
| 0.20 | 30.84 | 0.9161 | 0.891 | 0.859 | 0.651 |
| 0.25 | 30.73 | 0.9133 | 0.917 | 0.875 | 0.688 |
| 0.30 | 30.11 | 0.9019 | 0.969 | 0.932 | 0.766 |
| 0.35 | 28.88 | 0.8780 | 1.000 | 0.948 | 0.823 |
| 0.40 | 27.18 | 0.8394 | 1.000 | 0.964 | 0.823 |

读法（三条，都是这一轮才被明确下来的）：

1. **组合机制确实有效**：clean 侧 mf 在 **28.88 dB 就到 1.000**、解码器 0.948；
   对照修复前的口径（§11.1）同配置在 27.7 dB 只有 0.930。jpeg50 侧最好 0.823。
2. **mf 仍然稳定优于训练解码器**（+0.03~0.05，与 §9.3 的方向一致）；
   说明解码头欠拟合，`--mf-residual`（§15）是对症的下一步。
3. PSNR 列**不能跨运行横比**：见 §14 的口径问题（批内全局 MSE + n 不同 ⇒ 同一配置
   在不同脚本里能差 1 dB 以上）。表内趋势（PSNR 与准确率的取舍）仍然有效。

---

## 12. 差距的定量分解：35 dB 到底卡在哪（2026-09-20，纯测量，`scripts/diag_channel_budget.py`）

之前几轮一直在"换机制、重训、看数字"，但一直没算清 35 dB 这个目标里
**哪些 dB 是物理上拿不到的、哪些是可以买到的**。这一节把账算清（结果可复现：
`python scripts/diag_channel_budget.py`，输出 `results/channel_budget.md`）。

### 12.1 先纠正一个我自己的错误估计：JPEG50 不是硬墙

本轮之前我口头推断"JPEG50 在这类小图上本身只有 32 dB 左右，所以 35 dB + jpeg50≥0.95
在信息论上就不可行"。**实测否定了这个推断**（cover = CIFAR 上采样到 64×64，n=32）：

| 信道 | 实测 PSNR（失真 vs 原图） |
|---|---|
| JPEG q=30 | 34.17 dB |
| **JPEG q=50** | **36.77 dB** |
| JPEG q=75 | 40.06 dB |
| JPEG q=90 | 44.27 dB |

原因：CIFAR 上采样后的图极其平滑，JPEG 很好压。36.77 dB 的"信道噪声"与
35 dB 的目标载密图是同一量级，而非碾压关系 —— 所以目标**没有被排除**，
但余量确实很紧（信道噪声 ≈ 嵌入能量的 1.5 倍）。

### 12.2 真正的预算消耗者是"往返地板"，不是 JPEG

载密图总误差 = **往返误差**（无嵌入，只由 S 与 VAE/DDPM 质量决定）+ 嵌入误差。
PSNR 按能量相加：`1/SNR_tot = 1/SNR_rt + 1/SNR_emb`。
用实测往返 PSNR（§4）代入目标 35 dB：

| S | 往返 PSNR | 占总预算 | 留给嵌入的能量 | 嵌入-only 必须 ≤ |
|---|---|---|---|---|
| 150（当前） | 38.00 dB | **50%** | 1.577e-4 | 38.02 dB |
| 200 | 40.25 dB | 30% | 2.218e-4 | 36.54 dB |
| 250 | 41.89 dB | 20% | 2.515e-4 | **35.99 dB** |

**结论：在 S=150 下，35 dB 目标的一半预算被"什么都不藏时的往返误差"吃掉了**，
嵌入本身只能占 38.02 dB（几乎完全不可见）——这才是当前前沿卡在 31.5 dB 的结构原因，
而不是"解码器不够强"或"图案不够聪明"。
把 S 提到 250，**账面上**可以白拿 +2.03 dB 的嵌入预算（代价：训练/评测时间 ×1.67）。
但"账面"不等于"实测"——见 §12.3，这条推论**已经被实测否定**。

### 12.3 实测否定："提高 S"换不到前沿左移（`scripts/diag_steps_frontier.py`）

同一注入能量下比较 S=100/150/250 的"PSNR-准确率"前沿（16bit, bpb=4, 320 对,
n_inject=8, inject_at=0.35, matched-filter 接收, n=4 图, CPU 跑, 结果
`results/steps_frontier.json`）：

| S | 无嵌入往返（复现 §4） | 总能量 E | 载密 PSNR | mf | mf@jpeg50 |
|---|---|---|---|---|---|
| 100 | 34.86 dB | 0.30 | 28.10 dB | 0.953 | 0.703 |
| 100 | 34.86 dB | 0.45 | 24.93 dB | 1.000 | 0.859 |
| 150 | **38.00 dB** | 0.30 | **29.50 dB** | 0.984 | **0.859** |
| 150 | 38.00 dB | 0.45 | 23.79 dB | 1.000 | 0.938 |
| 250 | 41.89 dB | 0.30 | 28.91 dB | 0.984 | 0.828 |
| 250 | 41.89 dB | 0.45 | 22.70 dB | 1.000 | 0.969 |

（往返一列与 §4 的独立测量逐位一致，说明这次诊断本身可信。）

**结论：S 不是杠杆。** 同一准确率下 S=250 的 PSNR 反而比 S=150 低
（28.91 vs 29.50 dB；jpeg50 也低 0.03）。把误差拆开就明白为什么：

| S | 往返误差能量 | 载密总误差（E=0.3） | 相减 = 嵌入误差 |
|---|---|---|---|
| 150 | 1.585e-4 | 1.122e-3 | **9.63e-4** |
| 250 | 6.47e-5 | 1.286e-3 | **1.22e-3** |

步数越多，**注入扰动被后续轨迹放大得越多**（嵌入误差 +27%），把往返地板的收益
吃掉还倒亏。所以在当前设计下 S=150 已是局部最优，不必花 2.8h 重训 S=250 的解码器。
（唯一例外是 E=0.45 的高能量端：S=250 的 jpeg50 高 0.031，但 PSNR 低 1.1 dB，等价交换。）

**方法论教训**：只做"误差预算分解"会得出一个漂亮的错误结论。
预算是**必要条件**，必须再用一次免训练的前沿扫描去验证"多出来的预算真的能兑现"。

### 12.4 每比特观测数 N 只剩 ~2 dB 可挖

`N = bpb × ecc`（每比特参与平均的频点对数），平均增益 ~10log10(N)：

| 载荷 | ecc | check | 槽位 | r_max | 可用对 | bpb | N |
|---|---|---|---|---|---|---|---|
| 8 | 3 | 32 | 56 | 13（当前） | 252 | 4 | **12** |
| 8 | 3 | 32 | 56 | 15（=off） | 342 | 6 | 18 |
| 8 | 5 | 32 | 72 | 15 | 342 | 4 | **20** |
| 8 | 5 | 32 | 72 | 13 | 252 | 3 | 15 |
| 8 | 5 | 32 | 72 | 11 | 176 | 2 | 10 |

N 从 12 顶到 20 → 理论上限 +2.2 dB，且必须放弃 r_max=13（回到 15），
而 r_max=15→13 本身实测换来了 jpeg50 +0.059。**所以"多攒观测"是拆东墙补西墙，
净收益有限**（§9.7 实测 bpb 2→4 只把 mf 从 0.766 提到 0.922，远低于 3 dB 的理论值，
说明观测之间误差相关，平均增益被高估）。

### 12.5 下一阶段路线（按"每 dB 成本"排序，已剔除被实测否定的杠杆）

| 杠杆 | 预期收益 | 代价 | 依据/状态 |
|---|---|---|---|
| ~~S: 150 → 250~~ | ~~+2.0 dB~~ | — | **已实测否定（§12.3）：同准确率下反而 -0.6 dB** |
| **1. 接收端软判决/LLR 加权** | +1~2 dB 级 | 纯接收端改动，**零训练** | §9.3：mf 全面优于训练解码器，说明判决器本身有空间 |
| **2. ecc 3→5（r_max 13, bpb 3）** | +1 dB 级 | 无重训，但吃掉频点预算 | §12.4 |
| **3. latent 32×32 → 64×64** | 频点预算 ×4（N 上限 ×4） | DDPM 训练/显存 ×4 → 必须上 22GB 远程卡；VAE 往返地板可能变差 | §12.4 |
| 4. 换更强基座 DDPM | 抬往返地板 | 训练成本高于第 1 条，且收益会被 §12.3 的放大效应部分抵消 | §4 |
| 5. 降低载荷（8→4 bit）或放宽 JPEG 质量 | 直接放宽约束 | 改变论文声明 | §12.1 |

**建议顺序**：先做 1+2（都在本地、零或极低训练成本，合计预期 +2~3 dB，
把工作点从 31.5 dB 推到 33-34 dB），同时把第 3 条（64×64 latent）放到远程 2080 上排队——
它是唯一能把天花板整体抬起来的结构性改动。
如果第 1+2 之后仍差 1-2 dB，正确定位是：**在 32×32 隐空间 + 64×64 像素上做到
"33-34 dB + jpeg50 0.95"已经是该配置的上限**，把 35 dB 作为 64×64 latent 版的下一阶段目标写进论文路线，而不是继续在同一配置上换机制。

**已就绪的免训练诊断（跑完再决定动哪个杠杆）**：
- `scripts/diag_rmax_frontier.py`：r_max ∈ {15,13,11} × strength 的 PSNR-准确率前沿
  （注意 strength 是逐系数幅度，不同 r_max 必须同 PSNR 横比）；
- `scripts/diag_channel_budget.py`：信道预算与观测数上限表（秒级）。

---

## 13. 接收端 A/B：加权合并是**负结果**，但摸清了信道的误差结构（2026-09-20）

### 13.1 动机与做法

§9.3 发现 matched-filter（mf）接收全面优于训练解码器，说明"判决器"这一侧可能有
免费收益。当前 mf 是**等权平均**，而 `ring_features` 返回的
`[同相; 正交]` 里**正交分量被整段丢弃**——去旋转后正交分量恰好是"该频点的噪声"，
理论上可以据此做 MRC/逆方差加权：

    slot = Σ w_j·v_j / Σ w_j,   w_j = |v_j|/(q_j²+ε)

`scripts/diag_receiver.py` 在同一批载密量上比较 5 种判决方式（因此 PSNR 完全相同，
差异纯粹来自接收端；8bit, bpb=6, n_pairs=336, N=18, S=150, n_inject=8, inject_at=0.35,
n=8 图, mf 接收）：

| 总能量 E | 载密 PSNR | mean（现行） | w_mag | w_invvar | w_mrc | w_mrc_abs |
|---|---|---|---|---|---|---|
| 0.20 | 31.91 dB | **clean 0.938 / jpeg50 0.781** | 0.906 / 0.734 | 0.812 / 0.656 | 0.828 / 0.641 | 0.906 / 0.672 |
| 0.30 | 30.34 dB | **1.000 / 0.844** | 0.984 / 0.797 | 0.953 / 0.719 | 0.969 / 0.734 | 0.984 / 0.734 |
| 0.40 | 26.10 dB | **1.000 / 0.938** | 0.984 / 0.859 | 0.969 / 0.734 | 0.969 / 0.797 | 0.984 / 0.844 |

**结论：所有加权方案在 clean 与 jpeg50 两个口径上都不如等权平均**（唯一例外是
E=0.2 时 w_mag 配上重复码软合并能在 clean 上到 0.953 > 0.938，但它的 jpeg50
掉到 0.734 ≪ 0.781，不构成 Pareto 改进）。"软判决/加权"这条杠杆**被实测否定**。

**为什么？** 加权幅度（|v_j|）反而变差，说明**误差大致正比于系数幅度**——
即这是一个"相对误差"信道（扩散往返的误差与 JPEG 量化误差都随系数幅度缩放），
于是每个频点的信噪比近似相同，**等权平均就已经是最优合并**；
按幅度加权等于给"更响但同样相对更脏"的观测更多话语权。
正交分量虽然是噪声的无偏估计，但在重尾失真下它低估了大误差的频点，
所以逆方差加权也无效。

这个过程本身是有价值的：它把"等权 mf 已接近最优"从一个假设变成了实测结论，
后面不该再在这条路上花时间。

### 13.2 顺带得到的新最优点（mf 接收，8bit, bpb=6, r_max=None, N=18）

| 总能量 E | 载密 PSNR | mf clean | mf@jpeg50 |
|---|---|---|---|
| 0.20 | **31.91 dB** | 0.938 | 0.781 |
| 0.30 | **30.34 dB** | **1.000** | 0.844 |
| 0.40 | 26.10 dB | 1.000 | 0.938 |

这是目前最好的前沿：**clean 上 30.34 dB 就做到零误码**（此前 §10 的 16bit/320 对
配置在 30.65 dB 只有 mf 0.961；差别来自 8bit 载荷把 bpb 从 4 顶到 6，
每比特观测数 12→18）。jpeg50=0.95 大约落在 25-26 dB。
注意 n=8 图 × 8 bit = 64 bit，clean 的 1.000 精度只有 1/64，后续要用 n≥32 复核。

**这也把"训练解码器"的地位摆正了**：mf 免训练接收在同等 PSNR 下更好，
论文应以 mf 为主接收机（密钥门控、无需部署权重），解码器作为可选精化——
而当前解码器（MLP, 600~1000 步，loss 停在 0.57-0.62）**明显欠拟合**，
是下一步可做的零架构改动（加长训练 / 只学消息槽位）。

> 口径更正：本节最初的措辞是"mf 在同等 PSNR 下**明显更好**"，那是拿**跨运行**的
> 数字比的（不同 n、不同 key/bits 抽样 → 见 §14 与 §16）。**同一次运行内**比较，
> mf 领先解码器的幅度是 +0.03~0.05（§9 的 +0.068 也是同口径）。
> 结论方向不变，但幅度要按同口径报。

---

## 14. 测量口径审计（第四轮）：PSNR 本身的口径也是错的（2026-09-20）

前四类 bug（§11.1、§13）都是"训练/评测参数不同源"。这一轮发现**指标定义本身**
也有口径问题：

`krd.metrics.psnr` 用的是**批内全局 MSE**（先对整批求均方误差，再取 log）。
MSE 对重尾误差极其敏感，于是这个数字**随批大小 n 漂移**：

- 实测同一配置（combo, s=0.3, S=150）：`check_ninject.py`（n=4）得 **28.85 dB**，
  `eval_strength_sweep.py`（n=24）得 **30.11 dB** —— 差 1.26 dB，纯口径效应。
- 合成实验（`tests/psnr_convention_test.py`）：固定 8 张图（1 张坏图），
  随机取 4 张子集 12 次，全局口径的标准差 **8.03 dB**，而逐图 PSNR 平均只有 **2.77 dB**。
- 性质：全局口径 ≤ 逐图平均（Jensen），且抖动大得多。

后果：**任何"目标 35 dB"、"提升 1.0 dB"的说法都必须注明口径与 n**，
否则 1 dB 量级的结论（正是本项目所有机制的收益量级！）会被口径差异淹没。

修法（保守，不破坏可复现性）：
- `krd.metrics.psnr` **保持不变**（历史口径，用户 `verification_report.md` 复现的就是它）；
- 新增 `krd.metrics.psnr_mean`（逐图 PSNR 再平均，文献通行口径）；
- `eval_strength_sweep` / `eval_robustness` 的表格同时打印两个口径（`PSNR` / `PSNR_img`），
  扫描表头部注明口径定义；
- `tests/psnr_convention_test.py` 钉住两者关系（B=1 相等、均匀误差相等、
  含坏图时逐图平均更高、子集抖动更小）。

---

## 15. r_max 的定论（等 PSNR 比较，n=32）与"该往哪使劲"（2026-09-20）

§11.1 之前的 r_max 结论是"13 比 15 好：jpeg50 +0.059、PSNR +1.0 dB"。
但那个比较是在**同一 strength** 下做的，而 `inject_pattern` 的 strength 是
**逐系数幅度**，频点对数 n_pairs 越多总注入能量越大 —— 所以 r_max 小的配置
"看起来"PSNR 更高、jpeg50 更好，其实是**能量口径被混进来了**。

`scripts/diag_rmax_matched.py` 用 secant 把每个 r_max 的 strength 调到
**同一载密图 PSNR**（目标 30 dB），再比准确率（n=32，S=150，n_inject=8，8bit，mf 接收）：

| r_max | 可用对 | bpb | N | strength | PSNR | SSIM | mf clean | mf@jpeg50 | mf@jpeg75 |
|---|---|---|---|---|---|---|---|---|---|
| 15（=off） | 342 | 6 | 18 | 0.3013 | 30.10 | 0.8927 | **0.996** | **0.828** | 0.844 |
| 13 | 252 | 4 | 12 | 0.2682 | 30.59 | 0.9077 | 0.980 | 0.773 | 0.832 |
| 11 | 176 | 3 | 9 | 0.3164 | 30.05 | 0.8934 | 0.949 | 0.809 | **0.895** |
| 9 | 114 | 2 | 6 | 0.2904 | 29.86 | 0.8882 | 0.918 | 0.828 | 0.840 |

**结论（推翻之前的 r_max 结论）**：

1. clean 准确率**单调随 N 上升**（0.996 / 0.980 / 0.949 / 0.918 ↔ N=18/12/9/6）——
   这是"观测数决定判决"的直接证据；
2. jpeg50 / jpeg75 在 r_max 之间**没有稳定顺序**（0.773~0.828 / 0.832~0.895，
   n=32 时 256 bit 的抽样标准误约 0.023），**"把载波搬到低频"并没有换来抗 JPEG 收益**；
3. 因此 **r_max=15（不设上限）才是最优配置**，之前"r_max=13 更好"是能量口径的假象。

### 15.1 那把 N 顶满（下一轮该做的唯一一件事）

既然 N 是决定量，问题就变成：**在 (n_bits×ecc + check) × bpb ≤ avail 的约束下最大化 N=bpb×ecc**。
当前 (ecc=3, check=32 → 56 槽位, bpb=6) 只到 N=18，而：

| ecc | check | 槽位 | bpb | pairs | N |
|---|---|---|---|---|---|
| 3 | 32（当前） | 56 | 6 | 336 | 18 |
| 3 | 16 | 40 | 8 | 320 | 24 |
| 5 | 16 | 56 | 6 | 336 | 30 |
| 8 | 16 | 80 | 4 | 320 | **32** |
| 12 | 16 | 112 | 3 | 336 | **36** |

也就是**把每个比特摊到更多频点上**（高冗余重复码 + 小 bpb）能把 N 从 18 提到 32-36，
理论上 +2.5~3 dB 的平均增益，且**完全不需要训练**（mf 接收端直接就吃）。
代价：check 从 32 降到 16，错密钥虚警率从 2⁻³² 变 2⁻¹⁶（仍足够）。
`scripts/diag_ecc_frontier.py` 正在做这个等 PSNR 验证。

**顺带修正 §12.5 的路线**：不要再花时间在 r_max 上；
按"每 dB 成本"排序的可行杠杆现在只剩：
(1) **N: 18 → 32~36**（零训练，等 PSNR 验证中）；
(2) **mf 残差解码器**（`--mf-residual` + 加长训练，救回解码器那 0.03~0.05）；
(3) **更大的 latent**（N 的天花板 ∝ latent 边长²，32×32→64×64 就是 ×4）——上远程 2080；
(4) 降低载荷或放宽 JPEG 质量（改变论文声明）。

---

## 16. 把 N 顶满的实测结果：等 PSNR 下 jpeg50 +0.043、jpeg75 +0.090（2026-09-20）

`scripts/diag_ecc_frontier.py`（n=32，S=150，n_inject=8，inject_at=0.35，r_max 不限，
8bit，matched-filter 接收，secant 把每个配置都调到**同一载密图 PSNR ≈ 30 dB**）：

| ecc | check | 槽位 | bpb | pairs | **N** | PSNR | SSIM | mf clean | mf@jpeg50 | mf@jpeg75 |
|---|---|---|---|---|---|---|---|---|---|---|
| 3 | 32（旧） | 56 | 6 | 336 | 18 | 29.95 | 0.8893 | 0.996 | 0.836 | 0.844 |
| 3 | 16 | 40 | 8 | 320 | 24 | 29.94 | 0.8896 | **1.000** | 0.859 | 0.910 |
| 5 | 16 | 56 | 6 | 336 | 30 | 29.70 | 0.8858 | 0.996 | 0.840 | 0.883 |
| **8** | **16** | **80** | **4** | **320** | **32** | **30.11** | **0.8933** | **1.000** | **0.879** | **0.934** |
| 12 | 16 | 112 | 3 | 336 | 36 | 30.14 | 0.8932 | 0.996 | **0.891** | 0.898 |

**结论**：

1. **"把每个比特摊到更多频点上"确实有效**：同一 PSNR 下，N 从 18 提到 32
   （ecc 3→8、check 32→16、bpb 6→4）把 jpeg50 从 0.836 抬到 **0.879**、
   jpeg75 从 0.844 抬到 **0.934**，clean 从 0.996 到 1.000，SSIM 还略有提升。
   这是目前**唯一一个在等 PSNR 下真正左移前沿**的改动，且 mf 接收端零训练即可享受。
2. 但增益远小于 10log10(32/18)=2.5 dB 的理论值 —— 与 §13 的结论一致：
   观测之间误差相关，"多攒观测"的兑现率不高（N 提高 78% 只换来 jpeg50 +0.043）。
   **N=36 在 jpeg75 上反而不如 N=32**，说明已经接近饱和。
3. 代价：check 从 32 位降到 16 位 → 错密钥虚警率从 2⁻³² 变 2⁻¹⁶（1.5e-5），
   对 P1 的密钥安全声明仍然足够。
4. **选定新工作点**：`n_bits=8, ecc=8, n_check_bits=16, bpb=4（自动）, r_max 不限,
   inject_at=0.35, n_inject=8, S=150` → **N=32**。

### 16.1 已启动：v3 解码器（新工作点 + mf 残差 + 1000 步 + 按工作点选点）

```
python scripts/train_decoder.py --ddpm-ckpt checkpoints/ddpm_latent32.pt \
  --out checkpoints/decoder_latent32_v3.pt --steps 1000 --batch-size 16 \
  --n-bits 8 --ecc-reps 8 --n-check-bits 16 --inject-mode add \
  --inject-at 0.35 --n-inject 8 --strength-min 0.15 --strength-max 0.4 \
  --sched-noise-prob 0.3 --geom-prob 0.25 --hide-steps 150 --rec-steps 150 \
  --eval-strength 0.3 --eval-every 200 --mf-residual
```

四处同时改：①N=32 的新工作点；②`--mf-residual`（初始即等于 mf，训练只学残差）；
③步数 600→1000（旧解码器欠拟合）；④`--eval-strength 0.3` 让 `_best.pt`
**按工作点挑选**（旧运行硬编码 1.0，选点口径本身就是错的）。

顺带修掉了流水线的第六处同类问题：`run_pipeline.py --ldm` 的 latent_decoder 阶段
**从不传 `--n-bits/--ecc-reps/--n-check-bits/--mf-residual`**，也就是从流水线跑出来的
永远是"16bit / ecc3 / check32 / 无残差"的旧配置，而不是当前前沿。现在全部贯通，
默认值也改成当前最优（8bit / ecc8 / check16 / mf_residual / r_max=0）。

---

## 17. 负结果：mf 残差初始化保不住，matched-filter 仍然是最优接收机（2026-09-20）

§11.2 与 §9.3 都说"mf 优于训练解码器"，但那些是**跨运行**比较。为排除抽样差异，
本轮在同一批载密量（n=8，E=0.3，PSNR 30.26 dB / 逐图平均 30.62 dB，新工作点 N=32）
上同时算 mf 与解码器（`eval_strength_sweep.py` 新增 `mf@jpeg50` 列）：

| 接收机 | clean | jpeg50 |
|---|---|---|
| **matched-filter（免训练）** | **1.000** | **0.797** |
| v3 解码器（`--mf-residual`，第 200 步快照） | 0.984 | 0.734 |

**结论：即使把解码头初始化成"恰好等于 mf"，训练 200 步后它仍然掉到 mf 之下**
（clean −0.016、jpeg50 −0.063）。原因是训练目标与部署口径不一致：

1. 损失是在 strength∈[0.15,0.4] 的**混合分布** + 25% 几何攻击上算的 BCE（且包含
   16 个校验位槽），而报数口径是"E=0.3 且 clean/jpeg50"——优化的是平均意义，
   不是工作点；
2. 训练用随机 key/bits（每步重抽），解码器要同时对 80 个槽位输出校准概率，
   有效样本量很小，AdamW 在小数据上容易漂移出初始化所在的解。

这与 §13 的机制分析吻合：该信道是**相对误差**型（误差正比于系数幅度），
等权平均已是最优线性判决；网络想超过它，必须学到 JPEG 块结构/扩散投影的
**相关性结构**，而当前训练量（600–1000 步、~10⁴ 样本级）显然不够。

**对论文的影响（重要）**：主接收机应当是**免训练、密钥门控的 matched filter**
（可写成"训练无关的盲检测"卖点），解码器作为消融/可选精化，并如实报告
"学习式解码器在当前训练预算下未能超过匹配滤波"。
若要救回解码器，需要：①损失按工作点重加权（不要均匀扫 strength）；
②加"信任域"正则 λ‖logits − mf_logits‖²；③把训练集扩到 10⁵ 样本级。

> v3（1000 步、按 E=0.3 的 jpeg50 选点）仍在训练中，若其后续快照能超过
> mf 的 0.797，本节结论以此为准更新；否则按上述定位写。

**更新（v3 跑完后，见 §18）：仍然输给 mf。** 同批次（n=24）每个强度上 mf 的
clean 与 jpeg50 都更高（E=0.3：mf 1.000/0.859 vs dec 0.984/0.823）。

---

## 18. v3 结果与新前沿（2026-09-20 晚）

### 18.1 v3 训练曲线（N=32 工作点 + mf 残差 + 1000 步 + 按 E=0.3 选点）

| step | clean | jpeg50 | noise05 |
|---|---|---|---|
| 200 | 0.954 | 0.781 | 0.806 |
| 400 | 0.972 | 0.835 | 0.837 |
| 600 | 0.977 | 0.833 | 0.841 |
| **800** | **0.981** | **0.845** | 0.840 |
| 1000 | 0.980 | 0.831 | 0.856 |

`_best.pt` = 第 800 步（按 E=0.3 的 jpeg50 选，选点口径这次是对的）。
比旧 combo（0.948/0.823）明显更好，且**第 800 步之后 jpeg50 不再上升**，
说明瓶颈在信道而不在解码头容量。

### 18.2 同批次 mf vs 解码器（n=24，同一批载密量，PSNR 完全相同）

| 总能量 E | PSNR（全局 / 逐图） | SSIM | mf clean | **mf@jpeg50** | dec clean | dec jpeg50 |
|---|---|---|---|---|---|---|
| 0.20 | 31.09 / 31.48 | 0.9168 | 0.979 | 0.714 | 0.953 | 0.682 |
| 0.25 | 30.87 / 31.29 | 0.9110 | 0.995 | 0.781 | 0.979 | 0.750 |
| 0.30 | 29.93 / 30.33 | 0.8918 | **1.000** | 0.859 | 0.984 | 0.823 |
| 0.35 | 28.30 / 28.68 | 0.8571 | **1.000** | 0.896 | 0.990 | 0.854 |
| 0.40 | 26.27 / 26.74 | 0.8077 | **1.000** | **0.953** | 0.995 | 0.911 |

**结论（定论）：所有强度、两个口径上，免训练的 matched filter 都优于训练解码器**
（+0.016~+0.045）。这与 §13 的机制分析自洽（相对误差信道 → 等权平均最优）。
→ **论文主接收机写 matched filter；解码头只作为消融**（如实报告"学习式解码器
在当前训练预算下未能超过匹配滤波"）。若仍想要一个"学习式"卖点，唯一还没试的
方向是 §17 末尾的三条（工作点加权损失 / 信任域正则 / 10⁵ 样本级训练）。

### 18.3 当前前沿（N=32，8bit，S=150，inject_at=0.35，n_inject=8，mf 接收，n=24/32）

| 工作点 | PSNR 全局 | PSNR 逐图 | clean | jpeg50 |
|---|---|---|---|---|
| E=0.40 | 26.27 | 26.74 | 1.000 | **0.953** |
| E=0.35 | 28.30 | 28.68 | 1.000 | 0.896 |
| E=0.30 | 29.93 | 30.33 | 1.000 | 0.859 |
| E=0.25 | 30.87 | 31.29 | 0.995 | 0.781 |
| E=0.20 | 31.09 | 31.48 | 0.979 | 0.714 |

**门槛对照**：jpeg50=0.95 需 **26.3 dB**（旧 N=18 配置是 ~25.5-26 dB，N=32 换回
+0.3~0.8 dB）；clean 1.000 在 **29.9 dB**。**距 35 dB 目标：jpeg50 口径差 ~8.7 dB，
clean 口径差 ~5 dB**（与 §12 的预算分析一致：S=150 往返地板 38 dB 已吃掉一半预算）。

### 18.4 密钥安全（P1，n=32，n_wrong=200，E=0.3，`results/key_security_v3.md`）

| 指标 | 值 | 判读 |
|---|---|---|
| 载密图基线 | **30.37 dB / SSIM 0.8962** | |
| 真密钥比特准确率 | **0.9922** | |
| 错密钥 BER | **0.5011 ± 0.0332** | 理想（0.5） |
| mf 校验距离 | 真密钥 2.47（max 6）/ 错密钥 7.96 ± 0.31 | 16 位校验位的理论值正是 8 |
| FAR@τ=4 | **0.00000** | 但真密钥最大 6 → τ 应取 6 |
| FAR@τ=8 | 0.599 | 太松，别用 |
| 跨图残差方向一致性 | 无 nonce 0.078 → 逐图 nonce **0.026** | nonce 协议有效 |
| 多图差分定位 AUC (N=32) | 无 nonce 0.962 → 逐图 nonce **0.525** | 攻击失效 |
| 盲检测存在性 AUC | 无 nonce 1.000 → 逐图 nonce **0.562** | 不可检测性达标 |
| 密钥空间 | 参数空间 ≥2674 bit；**密钥熵 64 bit** | 安全强度由密钥熵封顶 |

**行动项**：τ 从 4/8 改为 **τ=6**（真密钥全通过、FAR ≈ 1e-10），
论文里必须写"安全强度 = 密钥熵 64 bit"，不能拿 2674 bit 的参数空间冒充。
另外**大 N 会放大"无 nonce"变体的风险**（AUC 0.962），所以 nonce 协议是承载件，
不能当作可选优化。

---

## 19. 三条"看起来该有用"的杠杆，查完发现已经实现了（负结果，2026-09-20）

在 §16 之后我把"还能快速提升指标"的候选逐条查了一遍，其中三条要么已经实现、
要么可以先用已有数据排除，写下来避免重复投入：

### 19.1 跨块交织：**已经实现了**

我原本怀疑"每个比特的 N=32 个观测在环带内连续取 → 相邻频点被 JPEG 的 8×8 块结构
同时打脏 → 平均增益兑现不了"，但看代码发现 `key_params` 里已经有

```python
perm = rng.permutation(len(bins))[:n_pairs]     # 密钥相关的随机置换
bins = bins[perm]
```

也就是说每个槽的 bpb 个频点本来就是**在整个环带上随机散布**的，交织不需要再做。
这反过来把"N 增益兑现率低"的原因指向别处：**误差在环带内本身高度相关**
（JPEG 块状误差 + VAE 编码的平滑作用），而不是"取点太近"。

### 19.2 接收端加权：已实测否定，且可反推"逐频点信噪比大致均匀"

§13.1 已证明所有加权（幅度/逆方差/MRC）都不如等权平均。这条结论还有一个副产品：
**如果逐频点信噪比差异很大，按 1/σ² 加权就应当明显更好**；既然它没用，
说明环带内各频点的信噪比大致相同 → **发射端"按频点注水（water-filling）"
也几乎无收益**，不值得单独实验。

### 19.3 I/Q 双比特调制：无免费午餐

现在每个频点只用一个相位（密钥相位）承载符号，正交分量闲置。看起来"用 I/Q
两路各带一个比特"能把载荷翻倍，但**总注入能量固定**：把一个复数系数拆成两个
独立比特，每个比特分到的能量减半，而 JPEG 噪声在复平面上各向同性 →
每比特信噪比不变。真正决定准确率的是"每比特的观测数 × 单观测信噪比"这个乘积，
改变分组方式不改变它。→ 不走这条路。

### 19.4 由此得到的结论：当前架构的杠杆已经用尽

把 §12–§19 的实测汇总成一句话：**在 32×32 latent / 64×64 像素 / 8bit / S=150
这套配置里，PSNR-鲁棒性前沿已经被信道本身钉住**——

- 往返地板 38.00 dB（S=150）→ 35 dB 目标只剩 3 dB 嵌入余量；
- JPEG50 信道噪声 36.77 dB → 与嵌入能量同量级；
- 名义观测数 N 已顶到 32（理论 +2.5 dB），但兑现率低（实测 jpeg50 仅 +0.043），
  且 N=36 已不再改善；
- 观测增广（交织）已在实现里，接收端加权已否定，S 与 r_max 已否定。

所以剩下的只有三类动作（按性价比）：**改报数口径与接收机（今天）**、
**降载荷或放宽 JPEG 质量（声明层面）**、**把 latent 扩到 64×64（唯一结构性改动，
需远程 2080，见 PLAN_REMOTE_64LATENT.md）**。

---

## 20. P2/P3 护栏（v3 配置）与两个意外发现（2026-09-20 晚）

配置：N=32（8bit / ecc8 / check16 / bpb4），S_hide=150（→ 实际子轨迹 k=52），
inject_at=0.35，n_inject=8，E=0.3，载密图 PSNR 29.52-30.21 dB（vs cover）。

### 20.1 意外之一：**接收端的反演步数几乎无关**（20–200 全平）

`results/step_mismatch_v3.md`（n=24，隐藏端固定 S_hide=150）：

| S_rec | 1 | 2 | 5 | 10 | 20 | 40 | 60 | 80 | 100 | 150 | 200 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| clean | 0.698 | 0.932 | 0.969 | 0.984 | 0.990 | 0.995 | 0.995 | 0.995 | 0.995 | 0.995 | 0.995 |
| jpeg50 | 0.635 | 0.828 | 0.854 | 0.854 | 0.859 | 0.859 | 0.865 | 0.859 | 0.859 | 0.859 | 0.844 |

两条结论：
1. **反演到 ~20 步就达到天花板**（0.990 / 0.859，对比 150 步的 0.995 / 0.859）。
   也就是说接收端算力可以砍 **7.5×**（150→20 步）而指标基本不动——这对
   部署/延迟是实打实的收益，且论文里可以写成"接收端无需步数同步"的鲁棒性优点；
2. S_rec=1（几乎不反演、直接从潜变量读）只有 0.698 → 说明**反演本身是承载件**
   （图案在采样后与潜变量内容混在一起，必须再"推回轨迹"才能干净读出），
   只是不需要推到底。这修正了此前"接收端步数必须固定且与隐藏端一致"的说法。

### 20.2 意外之二：隐藏端的深度是**生死线**，浅注入会被反转

`results/steps_grid_v3.md`（n=16，E=0.3）：

| S_hide（子轨迹 k=0.35·S） | 25 (k=9) | 50 (k=18) | 100 (k=35) | 150 (k=52) |
|---|---|---|---|---|
| 载密图 PSNR | 17.68 | 22.92 | 28.27 | 29.59 |
| clean | **0.047** | 0.773 | 0.992 | 0.992 |
| jpeg50 | 0.570 | 0.703 | 0.859 | 0.859 |

- S_hide=25 时 clean 准确率 **0.047（远低于 0.5 的瞎猜）**，即"注入的图案在后续
  采样里被**反转**了"，不是简单地被抹掉 —— 这给"扩散采样会抹除/扭曲小扰动"
  提供了一个极端而干净的证据，也说明 `inject_at=0.35`（k≈52）是**必须**的；
- S_hide 从 25→150，PSNR 也从 17.68→29.59：因为 `hide` 的输出由 k 步采样得到，
  S_hide 同时决定"图案深度"和"成像步数"，两者都随 k 增加而变好。

### 20.3 P3 再生攻击（`results/regen_v3.md`，n=24，E=0.3）

| t_reg | regen 步数 | 攻击后 PSNR(vs cover) | BER |
|---|---|---|---|
| 200 | 25 / 50 | 8.73 / 8.70 | 0.146 / 0.115 |
| 400 | 25 / 50 | 7.89 / 7.92 | 0.250 / 0.245 |
| 600 | 25 / 50 | 8.62 / 8.51 | 0.417 / 0.365 |
| 800 | 25 / 50 | 9.32 / 9.90 | **0.505 / 0.490** |

- 要真正把消息打掉（BER≈0.5）需要 t_reg=800，而代价是图像掉到 **9.3-9.9 dB**
  （不可用的破坏性变换）；
- 温和的再生（t_reg=200）只把 BER 抬到 0.115-0.146，代价是 8.7 dB。
→ 在**攻击代价**口径下，"再生攻击要破坏消息就得先毁掉图像"这一条在 v3 配置上成立
（与像素空间时代倒 U 型的旧结论不冲突：那是另一套实现/工作点）。

### 20.4 v3 鲁棒性全表（`results/robustness_v3.md`，n=64，E=0.3，载密 29.70 dB / SSIM 0.8826）

| 攻击 | 准确率 | | 攻击 | 准确率 |
|---|---|---|---|---|
| clean | 0.986 | | resize0.5 / 0.7 | 0.982 / 0.961 |
| jpeg30 | 0.797 | | bright+0.1 | 0.971 |
| **jpeg50** | **0.850** | | contrast1.2 | 0.977 |
| jpeg75 | 0.896 | | crop2px / 4px | 0.986 / 0.986 |
| noise0.05 | 0.869 | | **cropresize0.8** | **0.539** |
| noise0.10 | 0.818 | | **rotate5 / 15** | **0.691 / 0.490** |
| blur3x3 / 5x5 | 0.982 / 0.977 | | **translate2px** | **0.434** |
| regen(t=400) | 0.715 | | **zoom1.1** | **0.707** |
| wrong-key | 0.506 | | | |

短板**全部**是重采样类几何变换（相干检测失配），压缩/噪声/模糊/裁剪/光度类
都在 0.96-0.99。注意此表用解码器；同口径下 mf 还要高 0.01~0.04。

---

## 21. 负结果：接收端对齐搜索（resync）为什么失败，以及要花多少代价才能修好

针对 §20.4 的几何短板，最省事的设想是**纯接收端对齐搜索**：平移只是给频谱乘一个
相位斜坡、旋转/缩放只是在**旋转/缩放后的坐标**上重新取点，都不需要重过 VAE/UNet。
盲搜索的评分用校验位（接收端知道 key/nonce ⇒ 知道 16 个校验位的期望值），
这是经典 template-based resync 做法。

`scripts/diag_sync_search.py`（n=6，搜索空间 θ(9)×σ(7)×d(49) = **3087** 个假设）：

| 攻击 | 不搜索 | 搜索后 | 选中的（θ,σ,dx,dy）中位数 |
|---|---|---|---|
| clean | **1.000** | 0.750 | (0.0, 1.00, 0, 0) ← 中位数是对的，但部分图选错 |
| rotate15 | 0.542 | 0.500 | (-5.0, 1.02, -2, 0) |
| translate2px | 0.438 | 0.583 | (-2.5, 1.00, -2, -1) |
| cropresize0.8 | 0.583 | 0.438 | (-2.5, 1.10, 0, -1) |
| zoom1.1 | 0.833 | 0.542 | (-10.0, 1.02, 0, 0) |

**失败原因是可定量的，不是实现 bug**：

1. 相干检测下**选错对齐是灾难性的**（准确率直接掉到 0.1 级），而 clean 行显示
   中位数选到了正确的恒等假设、平均却掉到 0.750 —— 说明**部分图选错**，
   平均被拖垮；
2. 为什么会选错？这是典型的**多重比较噪声底**问题：
   - 单个校验位的偏转 d ≈ 1.44σ（由 key-security 实测校验位距离 2.47/16 反推），
     16 位合计 = 1.44×√16 ≈ **5.8σ**；
   - 而 3087 个假设里噪声的最大值约为 √(2 ln 3087) ≈ **4.0σ**；
   - 两者只差 1.8σ ⇒ 真峰经常被噪声峰盖过。**不是搜索做错了，是同步信息不够多。**
3. 需要多少同步信息才可靠？把同一个峰值显著性公式反过来：
   - 要稳过 4.0σ 的噪声底，真峰需要 ≥ 8-10σ ⇒ 需要 ≥ **64 个已知比特**
     （1.44×√64 ≈ 11.5σ）；但我们现在总共只有 80 个槽位，
     拿 64 个做同步等于把载荷砍掉 —— 不划算；
   - **更便宜的办法是用幅度模板**：`key_params` 已经生成了 `base_mag`（U(0.5,1.5)，
     密钥决定），但 `add` 模式下**根本没用它**（注入幅度对所有频点相同）。
     若把它作为**密钥已知的幅度轮廓**注入，接收端就能用 **320 个频点的幅度**
     做相关定位（单频点 1.44σ → 1.44×√320 ≈ **25.8σ**），远超 4.0σ 噪声底 —— 
     同步可靠性有数量级的余量，且**不占用任何消息比特**。
     代价：幅度轮廓不再均匀，会小幅牺牲数据侧的 SNR（§13 已证逐频点 SNR 大致均匀，
     故均匀幅度对数据最优；用非均匀换同步是一笔明确的交易）。

**结论（下一轮唯一值得做的机制级改动）**：
把 `base_mag` 从"`replace` 模式的遗留字段"升级为"`add` 模式下的密钥同步轮廓"，
实现"幅度定位 + 相位承载数据"的双层结构，然后：
① 用 `diag_sync_search` 的同一套指标验证定位成功率（应当接近 100%）；
② 再看 rotate15 / translate2px / cropresize0.8 / zoom1.1 能否从 0.43-0.71
   提到 0.9 以上。这是目前**唯一能补上鲁棒性最短板**、且不增加部署模块
   （同步信息本来就在密钥里）的改动。

---

## 22. 上条结论**当场被自己的验证推翻**：幅度轮廓同步不可行（2026-09-20 深夜）

上面那个"幅度轮廓当同步模板"的方案，我在准备远程训练前先本地实现并验证了一遍
（`krd/sync.py` + `scripts/diag_mag_sync.py`，注入端 `use_mag_profile=True`，
接收端幅度轮廓定位 θ/σ + 相位斜坡精修平移）。**结果是否定的**：

| 攻击 | 载密 PSNR 无轮廓 / 有轮廓 | mf 无轮廓无同步 | mf **有轮廓+同步** | 定位命中 |
|---|---|---|---|---|
| clean | 30.03 / 29.26 | 1.000 | **0.562** | ❌ |
| jpeg50 | 29.93 / 29.33 | 0.896 | **0.500** | — |
| rotate15 | 29.96 / 29.62 | 0.625 | 0.542 | ❌ |
| translate2px | 30.55 / 30.16 | 0.458 | 0.417 | ❌ |
| cropresize0.8 | 29.42 / 28.81 | 0.542 | 0.354 | ❌ |
| zoom1.1 | 28.49 / 27.75 | 0.854 | 0.521 | ❌ |

**定位命中率 0/6（连 clean 的恒等变换都定位不到）**，而且开轮廓后
clean 从 1.000 掉到 0.562、PSNR 还要多付 0.77 dB（比能量预算的 0.35 dB 还差，
说明幅度非均匀还会跟采样投影相互作用）。所以这套东西**只会让指标更差**。

**定量原因（这次算清了，与 §21 的负结果同一个根）**：

1. 由"每比特 32 个观测才把偏转凑到 1.44σ"反推，**单频点相干 SNR 只有 ≈0.25**；
   整环 320 个频点相干叠加 = 0.25×√320 ≈ **4.5σ**；
2. 而盲搜索要在 θ×σ×d = 3087 个假设里取最大，噪声底 ≈ √(2 ln 3087) ≈ **4.0σ**；
3. 4.5σ 对 4.0σ —— **信噪比根本不足以支撑盲搜索**（§21 用 16 个校验位得到 5.8σ
   同样被盖过，是同一件事）；
4. **幅度**轮廓比相位更糟：它没有相干增益，而封面自身在 320 个频点上的幅度起伏
   远大于 ±50% 的调制，且封面频谱的**径向能量趋势**会把 (θ,σ) 搜索引到错误的峰。

**由此得到的、明天上远程算力前必须接受的结论**：

- 在当前 32×32 latent / 30 dB 工作点上，**几何鲁棒性靠"盲同步"补不了**
  （能量/容量都不够）。要补只有两条路：
  (a) **更多频点**（64×64 latent：整环频点 ×4 → 相干 SNR ×2 → 8-9σ，
      才可能超过搜索噪声底；也就是说同步方案要等 latent 扩了才有意义）；
  (b) **独立且能量远高于数据载波**的同步图案（直接用 PSNR 换同步，昂贵）；
  (c) 或者把几何攻击写进威胁模型之外（论文如实声明：目标是压缩/噪声鲁棒）。
- 因此**远程训练的主线回到 64×64 latent**（`PLAN_REMOTE_64LATENT.md`），
  它同时解决"PSNR 上限"和"未来同步可行性"两件事；
  本轮已经实现的 `krd/sync.py` / `mag_profile` 全部默认关闭并标注"已证伪"，
  等 latent 扩到 64×64 后可以再拿同一套脚本复验一次（这是它的第二次机会）。

---

## 23. 远程训练前的代码准备：64×64 latent 全链路冒烟通过（2026-09-20 深夜）

明天在远程 22GB 卡上要跑的是 64×64 latent，所以先把整条链在本机（4GB）用
`--tiny` 预算跑通一遍，避免到远程才发现几何/容量/接口问题。

### 23.1 修掉了一个会误导冒烟的坑：`--tiny` 以前会改架构

`train_vae.py --tiny` 原来会把 `ch_mults` 强改成 `(1,2,4)`、`downsample` 强改成 2；
`train_ddpm.py --tiny` 会把 `base` 强改成 32。后果是"冒烟通过"**并不能说明目标几何能跑**
（我第一次冒烟就被骗了：传 `--downsample 0 --ch-mults 1` 却得到 16×16 latent），
而且 `.last.pt` 里会留下与正式训练不一致的架构。
现在 `--tiny` **只缩数据量与轮数/批大小**，架构一律由命令行决定。

### 23.2 全链路冒烟结果（本机 GTX 1650 4GB）

| 阶段 | 命令要点 | 结果 |
|---|---|---|
| VAE | `--resize 64 --downsample 0 --ch-mults 1 --base 32 --tiny` | `[setup] down=0 ch_mults=(1,)` → **latent (4, 64, 64)** ✓ |
| latent DDPM | `--vae-backend native --vae-ckpt ... --resize 64 --base 32 --tiny` | latent cache `(2048, 4, 64, 64)`，训练 256 步峰值显存 **0.2 GiB** ✓ |
| 解码器 | `--ddpm-ckpt ..._smoke_ddpm64.pt --steps 5 --tiny` | `[capacity] res=64 r_max=None 1488 对 → n_bits=8 ecc=8 check=16 **bpb=18** (slots=80, **pairs=1440**)` ✓ |

也就是 64×64 latent 下 **N = bpb×ecc = 144**，是当前 32×32 latent（N=32）的 **4.5 倍**，
频点预算从 342 对涨到 **1488 对**。这正是 §16/§22 指出的唯一结构性杠杆：
观测数与"未来同步可行性"都随 latent 面积增长。

### 23.3 同时做的清理（"去冗余"）

- **删除 12 个死脚本**：`_t.py` / `_refactor_evals.py` / `_refactor_decoder_ctor.py` /
  `bench_local.py` / `cmp_probes.py` / `dbg_latent_hide.py` / `diag_floor.py` /
  `diag_floor2.py` / `diag_tstar.py` / `diag_inject_ab.py` / `diag_psnr_probe.py` /
  `inspect_ckpt.py`（像素空间时代的诊断与一次性改写脚本，已被 §12-§22 的 LDM 诊断取代）；
- **把所有脚本的默认 checkpoint 指向当前前沿**：以前 eval_* 的默认还是
  `checkpoints/ddpm_cifar.pt` / `decoder_best.pt`（不存在或旧工作点），
  不带参数直接跑等于"能跑但跑的不是这套"；现在统一为
  `checkpoints/ddpm_latent32.pt` + `checkpoints/decoder_latent32_v3_best.pt`；
- **`train_decoder.py` 的默认值改成已验证工作点**（8bit / ecc8 / check16 /
  inject_at=0.35 / n_inject=8 / S=150 / strength∈[0.15,0.4] / eval-strength=0.3），
  这样"一键训练"不需要再堆参数；
- **`run_pipeline.py` 补齐 LDM 的 DDPM 训练开关**（`--latent-ddpm-base`、
  `--ddpm-amp`、`--ddpm-channels-last`、`--ddpm-grad-accum`、`--cudnn-benchmark`、
  `--num-workers`），并把 `--sweep-strengths` 默认改成工作点邻域；
- 新增 **`REMOTE_RUN.md`**：远程一键命令、判定门槛（VAE ≥51 dB、DDPM ≥42 dB、
  capacity 行长什么样）、三张必看评测表、三条报数口径、时间/显存预算、
  以及"远程也先跑 `--tiny` 冒烟 + 5 个回归测试"的自检清单。

---

## 24. 4-bit 载荷：本机零训练拿到 jpeg50 +0.059（2026-09-21 上午）

`scripts/diag_ecc_frontier.py --n-bits 4`（n=24，S=150，n_inject=8，inject_at=0.35，
r_max 不限，**mf 免训练接收**，secant 把每个配置对齐到同一载密图 PSNR ≈30 dB）：

| 配置 | 槽位 | bpb | pairs | **N** | PSNR | SSIM | mf clean | **mf@jpeg50** | mf@jpeg75 |
|---|---|---|---|---|---|---|---|---|---|
| 8bit ecc8 check16（当前最优） | 80 | 4 | 320 | 32 | 30.11 | 0.8933 | 1.000 | 0.879 | 0.934 |
| 8bit ecc3 check32（旧） | 56 | 6 | 336 | 18 | 29.95 | 0.8893 | 0.996 | 0.836 | 0.844 |
| **4bit ecc16 check16** | 80 | 4 | 320 | **64** | 30.22 | 0.9048 | **1.000** | **0.938** | **0.979** |
| 4bit ecc8 check16 | 48 | 7 | 336 | 56 | 30.34 | 0.9062 | 1.000 | 0.938 | 0.927 |
| 4bit ecc24 check16 | 112 | 3 | 336 | 72 | 30.17 | 0.9026 | 1.000 | 0.938 | 0.938 |

**结论**：

1. **同一 PSNR 下 jpeg50 0.879 → 0.938（+0.059）、jpeg75 0.934 → 0.979**，
   而且**零训练**（mf 接收端直接就吃）。机制：载荷减半 → 槽位减半 →
   `fit_capacity` 把 bpb 顶得更高 → **N 从 32 翻到 56–72**。
   这印证了 §16 的"N 是决定量"，也说明**32×32 latent 上还有一档没吃完**。
2. **N 的兑现率有了一致的标度**：N 18→32 时 jpeg50 +0.043，32→64 时 +0.059
   → 大致"每翻一倍 +0.05"。按此外推，64×64 latent 的 **N=144**
   （8bit/ecc8/check16/bpb18）应能把 jpeg50 推到 **0.96–0.98 @30 dB**，
   这是继续上远程 64×64 的量化理由。
3. jpeg50 在 N≥56 处看起来**饱和在 0.938 @30.2 dB**（三档都是 0.938），
   而 jpeg75 在 N=64 处最好（0.979）→ 若目标只是 jpeg50，本机这一档已到顶；
   要更高只能抬 PSNR（=抬往返地板，即 64×64 latent）或放宽 JPEG 质量声明。
4. 代价是载荷声明从 8bit 变 4bit；若论文必须 8bit，就用第 2 条的推论去远程跑 64×64。
5. 本机复现（约 12 分钟、无训练）：
   ```bash
   python scripts/diag_ecc_frontier.py --n 24 --n-bits 4 --target-psnr 30 \
     --configs "8/16,16/16,24/16" --out results/ecc_frontier_4bit.json
   ```

> **交接状态（2026-09-21）**：后台任务已全部结束、GPU 已释放；工作区干净并已推送
> （`git log` 顶端为本次提交）。后续优化由用户接手，建议起点：
> ①`REMOTE_RUN.md`（远程 64×64 一键命令与门槛）；②本节 4-bit 配置；
> ③§22 的证伪记录（不要启用 `--mag-profile`/`--sync`）。













