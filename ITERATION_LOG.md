# KRD-Steg 方案迭代记录

> 本文档是项目的**主迭代日志**：记录方案概述、技术路线、重要迭代、当前结果结论，
> 以及对目标 venue 与创新点强度的自评。每轮重要实验后应追加一节。
>
> 最近更新：2026-09-19

---

## 1. 方案概述

**KRD-Steg = 密钥条件化扩散轨迹隐写（Key-conditioned Reverse-Diffusion Steganography）**

核心思想：把秘密比特写进扩散模型的**初始噪声/潜变量轨迹**（而非像素），
用**闭式 DDIM 公式**做复原，用**密钥**门控比特在频谱上的位置与相位。

```
隐藏端（步数自由）:
  cover --VAE encode--> z --DDIM 反演(S_hide)--> z_T --密钥门控频谱注入--> z_T'
        --DDIM 采样--> 载密 z --VAE decode--> 载密图
复原端（步数固定、公式驱动、密钥门控）:
  载密图 --VAE encode--> z --DDIM 反演(S_rec)--> z_T --按密钥取频点+去旋--> 特征
        --RingDecoder(MLP)--> ECC 合并--> 比特
```

三个拟主张的创新点：

| 编号 | 主张 | 现状 |
|---|---|---|
| **C1** | **非对称步数设计**：隐藏端步数自由、复原端步数固定且由闭式 DDIM 公式驱动 | 机制成立；尚缺"为什么重要"的强论证 |
| **C2** | **密钥门控频谱图案 + 学习式密钥条件解码**：把 Tree-Ring 的固定指纹升级为用户密钥参数化的多比特信道 | 成立且已验证（错密钥 BER≈0.5、FAR=0） |
| **C3** | **失真感知轨迹复原**：可微 JPEG/噪声/几何 + 扩散再生攻击下的统一评测 | 评测协议完备；鲁棒性数字目前偏弱 |

---

## 2. 技术路线（实际走过的路径）

```
① 像素空间原型（CIFAR-10 32×32, U-Net DDPM, 容量 16bit）
   └─ 发现：往返上限仅 17.9 dB，注入后 9.5 dB，不可用
② 修正评测口径（真几何攻击 / nonce 盲提取 / 再生攻击代价 / 安全指标重定义）
   └─ 结论：此前所有"漂亮数字"均不可引用；真裁剪其实几乎无损
③ 加长训练到 300 epoch
   └─ 结论：往返上限只到 18.81 dB，loss 已饱和 —— 像素空间路线到头
④ 迁移 LDM 隐空间（本轮主体工作）
   ├─ VAE（native，f=2，latent 32×32×4）→ 64² 往返 51.5 dB
   ├─ 隐空间 DDPM（100 epoch）→ 往返质量随步数提升（见 §4）
   ├─ 隐空间解码器（S=150, 加性注入, 3 层 ECC + 32 校验位）
   └─ 评测链路全面适配（像素↔潜变量桥接、尺寸一致性）
⑤ 诊断出机制瓶颈："采样投影抹除小扰动" → 提出并在 PoC 中验证"轨迹中点注入"
```

---

## 3. 重要迭代记录

### 迭代 1：像素空间原型 + 评测口径修正
- **问题**：`crop` 攻击实为 `torch.roll`（循环平移）；再生攻击代价参照物是 stego 而非 cover；
  nonce 由 cover 派生导致方案非盲；把"槽位定位 AUC=0.79"当成攻击失败。
- **修复**：真裁剪 + 完整几何攻击矩阵；代价改以 cover 为参照；nonce 改 `H(key‖counter)`；
  安全指标重定义为盲检测 AUC + 残差一致性 + 真实密钥熵。
- **结论**：真裁剪几乎无损（0.992），真正短板是重采样类几何变换；
  嵌入能量占 x_T 的 36% 是质量差的直接原因。

### 迭代 2：像素空间加大训练量（60 → 300 epoch）
- **结果**：往返 17.4 → 18.81 dB；loss 0.0315 → 0.0297（后 150 epoch 基本平坦）。
- **结论**：**18.8 dB 是该配置的硬上限**，继续加训练无效 → 必须换空间。

### 迭代 3：LDM 迁移 + 一批尺寸/配置 bug
修复 5 类会导致结果无效的 bug：
| # | 问题 | 后果 |
|---|---|---|
| 1 | `run_pipeline` 漏传 `--resize` | DDPM 在 16×16 latent 上训练，与 VAE 的 32×32 不符 |
| 2 | `train_decoder` 用 32² CIFAR | latent 16×16 vs res=32 网格 → CUDA gather 越界 |
| 3 | `run_stego.py` 硬编码 32² + 绕过 StegoIO | 隐空间下必崩 |
| 4 | 评测步数硬编码 50 | 与 S=150 训练的解码器不匹配 |
| 5 | 图案网格与张量尺寸不匹配 | CUDA 断言不可读 |

并加了运行时护栏（`pattern._check_grid`）与审计脚本（`audit_dims.py`）。

### 迭代 4：注入方式 A/B —— 覆盖式是质量陷阱
| 模式 | s=0 | 0.02 | 0.05 | 0.1 | 0.3 |
|---|---|---|---|---|---|
| `replace`（原实现） | 35.1 | 15.7 | 15.6 | 15.6 | 15.3 |
| **`add`（新增）** | 35.1 | **34.5** | **33.3** | **31.0** | **23.6** |

`replace` 把选中频点的原系数（含相位）整体覆盖，一加注入就崩且与 strength 无关。
改用加性注入后质量随 strength 平滑可控。**这是隐空间可用的前提。**

### 迭代 5：步数上限扫描 —— 50 步是瓶颈
| S_hide=S_rec | 无嵌入往返 PSNR |
|---|---|
| 50 | 29.40 dB |
| 100 | 34.86 dB |
| **150（新默认）** | **38.00 dB** |
| 200 | 40.25 dB |

### 迭代 6：【核心发现】采样投影会抹除 x_T 的小扰动
三链路对照（同一注入后 `x_T'`）：
| strength | 采样回 x_0 后 PSNR | latent 相对 MSE |
|---|---|---|
| 0.0 | 29.40 | 0.0498 |
| 0.02 | 29.39 | 0.0513 |
| 0.1 | 29.53 | 0.0530 |
| 0.5 | 22.34 | 0.2244 |

strength 差 5 倍结果**完全相同** → 扰动在采样中被完全投影掉；
只有 strength ≳ 0.3 的强注入能存活。这解释了"PSNR 对 strength 不敏感"的全部现象。

### 迭代 7：解法 PoC —— 轨迹中点注入
| 注入时刻 t* | 图像 PSNR | mf 存活率 |
|---|---|---|
| 1.00（原做法，末端） | 9.39 dB | 0.453 |
| **0.50** | **32.40 dB** | **0.625** |
| 0.25 | 34.87 dB | 0.672 |

**PSNR +23 dB 且存活率更高**。原因：注入后剩余采样步数从 ~150 降到 75–112。
**尚未落地到 `TrajStego.hide`（下一步工作）。**

---

## 4. 当前结果结论（2026-09-12）

### 4.1 资产状态

| 组件 | 配置 | 质量 |
|---|---|---|
| VAE | native, f=2, latent **4×32×32**, 64² 输入, 25 epoch | 往返 **51.5 dB** |
| 隐空间 DDPM | U-Net base=64, 100 epoch, 5.17M 参数 | 往返 29.4 dB@50步 / **38.0 dB@150步** |
| 解码器 | S=150, 加性注入, 16bit+ecc3+32校验 | eval clean **0.979** / jpeg50 **0.960** |

### 4.2 端到端工作点（latent 4×32×32, S=150, n=8）

| strength | PSNR | SSIM | dec clean | dec jpeg50 |
|---|---|---|---|---|
| 0.02 | **38.81 dB** | 0.980 | 0.508 | 0.523 |
| 0.05 | 38.26 | 0.976 | 0.562 | 0.531 |
| 0.1 | 36.81 | 0.967 | 0.641 | 0.641 |
| **0.3** | **28.10 dB** | 0.865 | **0.883** | **0.734** |

**当前可用工作点：strength≈0.3 → PSNR 28.1 dB / jpeg50 0.734。**
（对比像素空间：9.5 dB / 0.99 —— 质量提升 18.6 dB）

### 4.3 鲁棒性与安全性（strength=0.3, n=16）

- 几何：`crop2px/crop4px` 良好；`rotate5=0.695`、`zoom1.1=0.621`、`translate2px=0.555`、
  `cropresize0.8=0.484`（**重采样类仍弱**）
- 再生攻击 t=400：0.641（vs cover 代价 7.84 dB）
- 错密钥：0.543（期望 0.5）✅
- 槽位定位 AUC：无 nonce 0.964 → 逐图 nonce **0.662**（去相关有效）
- 盲检测 AUC：无 nonce 1.000 → 逐图 nonce **0.750**（仍偏高）

### 4.4 结论

1. **LDM 迁移成功**：把可用的 PSNR 上限从 18.8 dB 抬到 38 dB（+19 dB），
   载密图质量从 9.5 dB 提升到 28–38 dB 区间——**这是本项目最关键的一步**。
2. **但"质量-可解码性"仍互斥**：38 dB 时解码 0.51（≈随机），
   0.88 准确率要付 PSNR 掉到 28 dB 的代价。
3. **根因已定位**：扩散采样对 x_T 扰动的流形投影（迭代 6），
   **解法已在 PoC 验证但未落地**（迭代 7）。
4. **数字尚未达到论文可用**：目标 PSNR≥35 dB 且 jpeg50≥0.95，目前无法同时满足。

---

## 5. 目标期刊/会议分析

### 5.1 当前成熟度评估

| 维度 | 状态 | 距离达标 |
|---|---|---|
| 机制新颖性 | 有一定新意（密钥门控 + 轨迹注入 + 步数不对称），但**与 Tree-Ring / ZoDiac / MDDM 的差异尚未用实验钉死** | 需补基线对比 |
| 结果强度 | PSNR 28 dB / jpeg50 0.73 | **不达标**（需 ≥35 dB & ≥0.95） |
| 评测完备性 | 高：13 种攻击 + 再生 + 盲检测 + 密钥空间 + 步数失配 | 已达标 |
| 安全性论证 | 中：错密钥/FAR/盲检测都有，但密钥恢复威胁模型只做了一半 | 需补 |
| 数据规模 | 仅 CIFAR-10 64² | **严重不足**（需 256²/512² + ImageNet/COCO） |

### 5.2 推荐 venue（按当前→潜在排序）

| venue | 类型 | 匹配度 | 说明 |
|---|---|---|---|
| **IEEE TIFS** | CCF-A 期刊 | ★★★★☆ | 隐写/水印主战场，**期刊周期长、可边做边补**，最适合当前阶段 |
| **IEEE TIP / TCSVT** | CCF-A 期刊 | ★★★☆☆ | 偏图像处理，需强视觉质量与大规模实验 |
| **ACM MM** | CCF-A 会议 | ★★★☆☆ | 接收面较宽，需在截稿前拿到达标数字 |
| **NeurIPS / ICML** | CCF-A 会议 | ★★☆☆☆ | 需更强理论（如"注入存活性"的解析刻画） |
| **CVPR / ICCV** | CCF-A 会议 | ★★☆☆☆ | 偏好视觉冲击与大模型 demo，需 512² + FID 对比 |
| **IH&MMSec / WIFS** | 专业会议 | ★★★★★ | 隐写领域专门会议，**可作为 workshop/短文先发** |
| **arXiv 预印本** | — | — | 建议先挂，锁定时间戳 |

**建议策略**：以 **TIFS 为主线**（周期容忍迭代），同时投 **IH&MMSec/WIFS** 短文占位；
若迭代 7 落地后数字达到 PSNR≥35 dB & jpeg50≥0.95，再考虑冲 ACM MM / CVPR。

### 5.3 创新点强度自评（诚实版）

**够有力的部分**：
- **C2（密钥门控多比特信道）**：把 Tree-Ring 的 0-bit 固定指纹升级为**用户密钥参数化的
  16-bit 信道**，并给出错密钥 BER≈0.5、FAR=0、盲检测 AUC 的完整量化 —— 这是可辩护的增量。
- **C3 的评测协议**：预算化再生攻击（同时报告攻击代价）+ 盲检测 AUC + 步数失配曲线，
  比现有工作更完整，可作为"评测贡献"。

**不够有力的部分（审稿人会打的点）**：
1. **C1"步数不对称"目前只是工程描述**，缺"为什么必须这样"的理论论证。
   若要立住，需要给出：复原步数与 BER 的解析关系，或证明某类载体对步数不敏感。
2. **与 MDDM / Training-Free Robust Generative Steganography 的差异未用实验证明** ——
   提案自己就把这两篇列为风险项，**投稿前必须复现并同表对比**。
3. **容量仅 16 bit**，而竞品动辄 32–256 bit；16 bit 会被质疑实用性。
4. **抗几何攻击弱**（rotate5=0.70、cropresize=0.48），而这是全局频域载体的通病，
   需要明确作为 limitation 或改用幅度域/同步标记。
5. **"攻击者困境"叙事已被自己的数据否定**（再生攻击代价随攻击强度非单调），
   必须重写为"BER–保真度联合权衡"。

**结论**：**当前创新点强度 = 中等偏上，但尚不足以支撑 CCF-A 主会**。
需要补三件事才能到"够有力"：
(a) 迭代 7 落地并把指标做到 PSNR≥35 dB & jpeg50≥0.95；
(b) 与 Tree-Ring/ZoDiac/MDDM 的同环境对比表；
(c) C1 的理论刻画或改用"注入存活性"这一更有解释力的新主张。

---

## 6. 下一步路线（优先级排序）

> **状态（2026-09-19 晚）**：P0 已完成（§8）；P1 杠杆 1（8bit 载荷）已完成（§9.4，
> 解码 0.95 工作点 +5.2 dB）；杠杆 2（解码器收敛）经诊断剩余空间有限（§9.5）；
> **瓶颈已定位为载体级 JPEG 脆弱性 → 首选杠杆改为环带重定位（§9.5）**，
> 之后重判是否触发 §6 路线转向。

| 优先级 | 任务 | 预期收益 | 预计工时 |
|---|---|---|---|
| **P0** | 在 `TrajStego.hide` 实现 `inject_at`（t*），把"末端注入"改为"轨迹中点注入" | PSNR +23 dB（PoC 已验证） | 2–3 h |
| **P0** | 扫 t* × strength，找 PSNR≥35 dB 且 jpeg50≥0.95 的工作点 | 决定项目能否成立 | 3–4 h |
| **P1** | 用选定工作点重训解码器 + 全套评测 | 论文主表 | 2 h |
| **P1** | 提升容量到 64–256 bit（32×32×4 latent 预算 1368 对，够用） | 回应"容量太小" | 2 h |
| **P2** | 复现 Tree-Ring / ZoDiac 做同环境对比 | 回应 novelty 质疑 | 3–5 天 |
| **P2** | 分辨率升到 256²（CIFAR 上采样或换数据集）+ FID/LPIPS | 回应"规模太小" | 1–2 天 |
| **P3** | C1 的理论刻画（步数-BER 关系）或改主张为"注入存活性" | 提升理论强度 | 1 周 |

**关键判断点**：P0 的两个任务做完后，如果无法同时达到
**PSNR ≥ 35 dB 且 jpeg50 ≥ 0.95**，则当前载体方案不成立，
应转向"幅度域载体"或"同步标记 + 仿射对齐"路线（见 `LDM_WORKPOINT_DIAGNOSIS.md` §5）。

---

## 7. 相关文档索引

| 文档 | 内容 |
|---|---|
| `PROPOSAL.md` | 最初方案与文献综述 |
| `NEXT_STEPS.md` | 像素空间阶段的诊断与路线 |
| `RESULTS_ANALYSIS_local300.md` | 像素空间 300 epoch 结果分析 |
| `RESULTS_ANALYSIS_v2_retrain.md` | 评测口径修正后的结果分析 |
| `LDM_MIGRATION.md` | LDM 迁移实现说明与验收标准 |
| **`LDM_WORKPOINT_DIAGNOSIS.md`** | **尺寸类 bug 清单 + 采样抹除机制 + t\* 解法** |
| `TRAINING_TUNING.md` | 训练/显存/速度配置 |
| 本文档 | 方案概述、技术路线、迭代记录、结论、选刊与创新点自评 |

---

## 8. P0 执行结果（2026-09-12 续）

### 8.1 已完成：`inject_at` 落地并全链路贯通

`TrajStego.hide(..., inject_at=frac)`：把注入时刻从轨迹末端提前到 `frac` 位置
（`k = round(frac × S_hide)` 步反演 → 注入 → k 步采样回 x_0）。
已贯通：`train_decoder --inject-at` → decoder config → `eval_setup` → `StegoIO.inject_at`
→ 全部评测脚本自动继承。回归测试：`tests/inject_at_test.py` 通过。

### 8.2 P0 扫描结果（latent 4×32×32, S=150, n=4, matched-filter 接收）

| inject_at | strength | PSNR | mf 准确率 |
|---|---|---|---|
| 1.00（原做法） | 0.1 | 36.91 dB | 0.766 |
| 1.00 | 0.3 | 26.54 dB | 0.938 |
| 0.50 | 0.3 | 27.33 dB | 0.938 |
| **0.35** | **0.3** | **27.70 dB** | **0.938** |
| 0.25 | 0.3 | 27.51 dB | 0.922 |

**结论（与 PoC 的差异需要说明）**：
- PoC 报的 "inject_at=0.5 → 32.4 dB" 是在 **strength=0.1、S=150、且采样步数也用 150**
  的条件下测的；本轮正式扫描显示：**inject_at 对 PSNR 的收益在强注入区并不显著**
  （0.3 强度下 1.0/0.5/0.35 分别是 26.54/27.33/27.70 dB，仅 +1.2 dB）。
- **更重要**：mf 准确率在 strength≥0.3 处**饱和于 0.938**，把 inject_at 从 1.0 调到 0.25
  也无法突破 —— 说明**瓶颈不是"扰动被抹除"，而是"每比特观测数不足"**。

### 8.3 找到真正的杠杆：增加每比特观测数

容量报告显示当前只用掉 **160/1368** 对频点（8.6 倍余量未用）。提高 `bins_per_bit`：

| bpb | 实际对数 | strength | PSNR | mf 准确率 |
|---|---|---|---|---|
| 2 | 160 | 0.1 | 30.10 dB | 0.625 |
| 2 | 160 | 0.2 | 30.63 dB | 0.766 |
| **4** | **320** | **0.1** | **29.35 dB** | **0.781** |
| **4** | **0.2** | **28.85 dB** | **0.922** |
| 4 | 320 | 0.2 | 28.85 dB | 0.922 |

**在几乎相同的 PSNR（30.6 → 28.9 dB，仅 -1.7 dB）下，准确率从 0.766 提升到 0.922**
（+0.156）。这是目前找到的**性价比最高的杠杆**。

### 8.4 当前最优工作点与差距

| 指标 | 当前最优 | 论文可用线 | 差距 |
|---|---|---|---|
| PSNR | 28.9 dB | ≥ 35 dB | -6.1 dB |
| jpeg50 准确率（训练解码器） | 0.734 @28 dB | ≥ 0.95 | -0.22 |

### 8.5 剩余可选杠杆（按预期收益）

1. **降低载荷**：16 bit → 4–8 bit。每比特观测数翻 2–4 倍，是隐写领域的标准折中，
   预期能把准确率推到 0.95+ 而不牺牲 PSNR。
2. **软判决 + 真 ECC**：当前是"重复码 + 实部均值"，换成 BCH/LDPC 软译码可再省 3–6 dB。
3. **重训解码器用新工作点**：现有解码器是在 `inject_at=1.0, bpb=2, S=150` 下训练的；
   改用 `inject_at=0.35, bpb=4` 重训（约 1 h）应能显著超过当前 0.734。

### 8.6 下一步（P1 修订版）

```powershell
# 用新工作点重训解码器（inject_at=0.35, bpb=4）
python scripts/run_pipeline.py --ldm --stages latent_decoder --force `
    --hide-steps 150 --rec-steps 150
# 注：bpb 由 fit_capacity 自动上调到 4（res=32 预算 342 对）
python scripts/train_decoder.py --ddpm-ckpt checkpoints/ddpm_latent32.pt `
    --out checkpoints/decoder_latent32.pt --steps 300 --batch-size 16 `
    --inject-mode add --inject-at 0.35 --strength-min 0.1 --strength-max 0.3 `
    --hide-steps 150 --rec-steps 150 --eval-every 100
```
然后跑 `eval_ldm_pipeline.py` + `eval_strength_sweep.py` 确认能否达到
PSNR≥35 dB 且 jpeg50≥0.95；若仍不达标，则执行 8.5 第 1 项（降载荷到 8 bit）。

---

## 9. P1 执行与独立复核（2026-09-19）

### 9.1 独立复核：声明数字全部复现（evaluation-only，零训练）

新增 `scripts/verify_results.py`：用现有 checkpoint 独立重验 §4/§8 的关键声明
（VAE 往返 / 无嵌入往返 / 端到端工作点 / 密钥安全 / 投影抹除），报告见
`results/verification_report.md`。

| 复核项 | 声明值 | 复测值 | 判定 |
|---|---|---|---|
| VAE 往返上限 | 51.53 dB | 51.53 dB | ✅ 精确 |
| 端到端 s=0.3: PSNR/dec/jpeg50 | 28.10 / 0.883 / 0.734 | 28.10 / 0.883 / 0.734 | ✅ 逐位一致 |
| 端到端 s=0.1: PSNR/dec | 36.81 / 0.641 | 36.81 / 0.641 | ✅ 逐位一致 |
| 无嵌入往返 @S=50/150 | 29.40 / 38.00 dB | 30.07 / 39.00 dB | ✅ ~1dB（样本集不同） |
| 错密钥 BER | 0.4977±0.040 | 0.4997±0.043 | ✅ |
| 定位 AUC @N=8（无nonce/ours） | 0.964 / 0.662 | 0.965 / 0.658 | ✅ |
| 投影抹除（S=50, s=0.05 vs 0.1） | 应几乎相同 | 30.16 / 30.34（基线 30.07） | ✅ 确认 |

端到端逐位一致源于固定种子（bits 用 seed=0 生成）——结果可复现是好事，但也意味着
**n=8 的单次抽样波动没有被平均掉**。

**复核发现的两个稳健性问题（此前未记录）**：
1. **FAR@τ=12 不稳健**：原 76 错密钥抽样得 0.037，复测（20 键×8 图）得 0.138，
   与理论 Binomial(32,0.5) 的 P(X≤12)≈0.11–0.15 一致。**论文引用 FAR@τ=8（两次均为 0，
   离均值 2.8σ），τ=12 需加大样本或弃用。**
2. **真密钥解码 acc 的密钥抽样方差 ≈±0.05**（0.898 → 0.844）：n=8 = 128 比特，
   置信区间很宽。**正式评测必须 n≥64 并报 mean±std。**

### 9.2 P1 落地：v2 解码器（bpb=4 + inject_at=0.35）重训与扫描

`checkpoints/decoder_latent32_v2.pt`：n_bits=16, ecc=3, **bpb=4（320/342 对）**,
inject_mode=add, **inject_at=0.35**, S=150/150, 1000 步（旧解码器 bpb=2/inject_at=1.0 保留作对照）。
训练内评测（分布外 strength=1.0）：clean=0.988 / jpeg50=0.969。

strength 扫描（n=32，`results/strength_sweep_v2.md`）：

| strength | PSNR | mf | dec clean | dec jpeg50 |
|---|---|---|---|---|
| 0.3 | 26.05 | 0.984 | **0.916** | 0.758 |
| 0.2 | 31.07 | 0.906 | 0.840 | 0.676 |
| 0.15 | 33.95 | 0.818 | 0.768 | 0.633 |
| 0.1 | 36.73 | 0.727 | 0.695 | 0.596 |
| 0.05 | 38.77 | 0.615 | 0.604 | 0.549 |
| 0.02 | 39.32 | 0.545 | 0.523 | 0.512 |

与旧解码器同坐标对比：s=0.1 同 PSNR（36.7dB）clean 0.641→0.695；s=0.3 clean 0.883→0.916
（PSNR 28.10→26.05，因频点对翻倍总能量略增）。**bpb=4 杠杆确认有效但幅度有限。**

**判定点结论：PSNR≥35 dB 且解码≥0.95 仍未达成**——35dB 附近仅 ~0.7，0.9+ 需要 26dB。

### 9.3 新发现：matched-filter 全面优于训练解码器

所有 strength 下 mf > dec（s=0.3: 0.984 vs 0.916）。MLP 训练 1000 步后 loss 仍在
0.57–0.62（80 槽位输出 + strength/失真混合分布），未收敛到位。

两个推论：
1. **解码器侧仍有免费提升空间**（加长训练 / 简化输出头 / 只学消息槽位），优先级低于载荷杠杆；
2. **mf 本身是论文卖点**：免训练、密钥门控的盲接收机在 26dB 处达 0.984——可作为
   "训练无关接收"基线写入方法（接收端无需信任/部署解码器权重）。

### 9.4 杠杆 1 实测：8bit 载荷重训（已完成）

`checkpoints/decoder_latent32_b8.pt`：n_bits=8, ecc=3, check=32 → 56 槽位 × **bpb=6**
（fit_capacity 自动上调，336/342 对），**每消息比特观测数 12→18（+50%）**；其余工作点不变，
1000 步。训练内评测（分布外 strength=1.0）：**clean=1.000 / jpeg50=0.996**。

strength 扫描（n=32，`results/strength_sweep_b8.md`），与 16bit v2 解码器对比：

| strength | PSNR | 16bit dec | **8bit dec** | Δ | 8bit mf | 8bit jpeg50 dec |
|---|---|---|---|---|---|---|
| 0.3 | 26.13 | 0.916 | **0.980** | +0.064 | 0.996 | 0.777 |
| 0.2 | 31.30 | 0.840 | **0.914** | +0.074 | 0.957 | 0.699 |
| 0.15 | 34.09 | 0.768 | **0.859** | +0.091 | 0.906 | 0.641 |
| 0.1 | 36.64 | 0.695 | **0.781** | +0.086 | 0.812 | 0.613 |
| 0.05 | 38.56 | 0.604 | 0.645 | +0.041 | 0.684 | 0.574 |

- **解码 0.95 工作点 PSNR：~23.5 dB（16bit）→ ~28.7 dB（8bit），+5.2 dB**；
- 解码器与 mf 的差距大幅收窄（clean: 0.980 vs 0.996 @s=0.3）——杠杆 2（解码器收敛）
  的剩余空间已不大；
- **判定线仍未达成**：34dB 处 0.859，0.95 需 ~29dB，距 35dB@0.95 还差 ~6dB。

### 9.5 JPEG 瓶颈定位（免训练 mf 失真诊断，2026-09-19）

对 8bit 接收机在失真下测 mf（mf = 无学习接收的 ceiling 估计）：

| strength | clean | jpeg50 | jpeg75 | noise0.05 |
|---|---|---|---|---|
| 0.3 | 0.992 | 0.852 | 0.836 | 0.844 |
| 0.2 | 0.953 | **0.609** | 0.719 | 0.742 |

**结论：JPEG/噪声短板主要是载体层面而非解码器层面**——图案所在环带（res=32, r∈[3,15]）
覆盖的中高频系数在 JPEG 量化下大量归零，mf 上限本身塌陷（s=0.2 时 jpeg50 仅 0.609）。
训练解码器已把该上限补回一部分（jpeg50: dec 0.699 vs mf 0.609 @s=0.2）。

由此，剩余杠杆的优先级重排：
1. **环带重定位（载体级，推荐首选）**：把 r_max 从 15 降到 ~8–10（JPEG 鲁棒频段），
   容量换 JPEG 存活率（隐空间 4 通道预算充足，需重算 `latent_capacity_report`）；
2. **软判决/真 ECC（编码级）**：mf jpeg50=0.852@26dB 对应 BER≈0.15，BCH/LDPC 软译码
   有望拉回 0.95+；
3. 解码器加长训练：与 mf 差距已小，预期收益最低。

### 9.6 当前差距（更新 §8.4/§9.5）

| 工作点 | clean | jpeg50 | 论文可用线（35dB & 0.95） |
|---|---|---|---|
| 8bit @s=0.3（26.1 dB） | 0.980 | 0.777 | clean 达标线差距小；jpeg 是短板 |
| 8bit @s=0.15（34.1 dB） | 0.859 | 0.641 | 未达标 |
| 8bit @s=0.2（31.3 dB） | 0.914 | 0.699 | — |

判定：**"35dB 且全通道 ≥0.95" 在当前环带设计下不可达**（JPEG 载体上限限制）；
执行环带重定位后需重判。若重定位 + 软 ECC 后仍不达标，则触发 §6 路线转向
（幅度域载体 / 同步标记 + 仿射对齐），或把主张改为"clean 通道 35dB@0.95 +
JPEG 通道的显式容量-鲁棒性权衡曲线"。

---

## 9.7 A 方案（环带重定位）训练准备就绪（2026-09-19 深夜，训练由用户启动）

### 已落地的代码支持（`--r-max` 全链路贯通）

`ring_capacity` / `key_params` / `TrajStego` / `fit_capacity` / `load_stego` /
`train_decoder --r-max`（写入 config）/ `eval_setup` / `eval_strength_sweep` /
`eval_ldm_pipeline` 全部支持 r_max 参数；评测端从解码器 config 自动回填（与
inject_at/steps/bpb 同机制）。2 步 dry-run 已验证（bpb 自动 6→4、config 回填正确，
探针产物已清理）。历史行为（r_max=None）逐位不变，冒烟测试通过。

### 频点预算表（res=32 单通道, r_min=3, 8bit: 56 槽位）

| r_max | 可用对 | bpb | 每消息比特观测 | 说明 |
|---|---|---|---|---|
| 15 | 342 | 6 | 18 | 现行基线（JPEG 中高频脆弱） |
| **13** | **252** | **4** | **12** | **推荐**：砍掉最脆弱的最外两环，SNR 等效代价约 -1.7dB |
| 12 | 208 | 3 | 9 | 更保守的备选 |
| 10 | 146 | 2 | 6 | 观测数过低，预期抵消 8bit 收益（原方案 8–10 据此上修） |
| 8 | 86 | 1 | 3 | 仅极低频，容量不足 |

> 注：原 A 方案拟取 r_max=8–10，但精确容量计算显示该区间每比特观测数只剩 3–6
> （现值 18），大概率得不偿失；故推荐 **r_max=13**（必要时 12）作为实验点。

### 明天的启动命令（依次执行）

```powershell
# 1) 主实验: r_max=13 重训 8bit 解码器（约 1.5-2h；bpb 自动 6->4）
python scripts/train_decoder.py --ddpm-ckpt checkpoints/ddpm_latent32.pt `
    --out checkpoints/decoder_latent32_b8r13.pt --steps 1000 --batch-size 16 `
    --n-bits 8 --bins-per-bit 6 --r-max 13 --inject-mode add --inject-at 0.35 `
    --strength-min 0.1 --strength-max 0.3 --hide-steps 150 --rec-steps 150 `
    --eval-every 250 --eval-size 32

# 2) 同口径 strength 扫描（对照基线 = results/strength_sweep_b8.md, r_max=15）
python scripts/eval_strength_sweep.py --ddpm-ckpt checkpoints/ddpm_latent32.pt `
    --decoder-ckpt checkpoints/decoder_latent32_b8r13_best.pt --n 32 --batch 8 `
    --hide-steps 150 --rec-steps 150 --strengths 0.3,0.2,0.15,0.1,0.05,0.02 `
    --out results/strength_sweep_b8r13.md

# 3) mf 失真诊断（与 §9.5 同口径, 检验 JPEG 上限是否抬升）: 把
#    verify 里的 mf 脚本换 decoder_ckpt 即可, 或直接看扫描表中 jpeg50 dec 列

# 4) (可选, 若 r_max=13 达标趋势明显) 更保守点 r_max=12 再训一组做消融
```

### 判定规则（实验后重判）

- **成功**：同 PSNR 下 jpeg50 dec 相比 r_max=15 基线（0.777@26dB / 0.699@31dB）
  提升 ≥0.08，且 clean 通道回落 ≤0.03 → 环带重定位有效，继续叠加软判决 ECC 冲线；
- **失败**：jpeg50 无提升或 clean 大幅回落 → 载体级 JPEG 问题不在环带位置
  （而在 VAE 往返的频谱均衡），触发 §6 路线转向评估。
---

## 10. 多步注入（n_inject）实现与实测（2026-09-20）

> 编号说明：本节与 §9 曾同为"9."（§9 是 P1 那一节，README 引用的也是那一节），
> 现把本节改为 §10 以消除歧义。

### 10.1 实现
`TrajStego.hide(..., inject_at=frac, n_inject=n)`：在轨迹**下坡段**的 k 步里等距选 n 个时刻，
每处注入一次相同图案。**不增加反演/采样步数**（仍是 k 步采样），计算开销与 n=1 相同。
贯通：`train_decoder --n-inject`（内部按 `strength/n` 保持总能量守恒）→ decoder config
→ `eval_setup` → `StegoIO.n_inject` → 全部评测脚本自动继承。

### 10.2 实测（latent 4×32×32, S=150, inject_at=0.35, n=8 图, matched-filter 接收）

| 总能量 E | n=1 PSNR / mf | n=4 | n=8 |
|---|---|---|---|
| 0.20 | 30.69 / 0.820 | 31.12 / 0.820 | **31.14 / 0.836** |
| 0.30 | 28.97 / 0.938 | 30.49 / 0.945 | **30.65 / 0.961** |
| 0.40 | 25.39 / 0.992 | 27.51 / 0.992 | **27.73 / 0.992** |

### 10.3 结论（含对上一轮预测的更正）

1. **准确率主要由总注入能量决定**，把同样的能量拆成 n 次注入**不会**显著提高准确率
   （E=0.2/0.4 时 mf 完全不变；E=0.3 时 +0.023）。
   —— 我上一轮预测"多步注入能把前沿整体左移"**过于乐观**，实测只左移了约 0.02 准确率。
2. **但 PSNR 稳定提升 0.5–2.1 dB**（E=0.4 时 25.39 → 27.73 dB），
   因为峰值扰动更小、能量沿轨迹摊得更匀。**这是零额外计算换来的净收益**，
   属于 Pareto 改进（同准确率下质量更好）。
3. **首次出现 mf ≥ 0.95**：E=0.30, n=8 时 mf=0.961（对应 PSNR 30.65 dB）。
   这比之前的 b8r13 前沿（s=0.3 → 27.7 dB / jpeg50 0.836）更好，
   但仍是 **30.65 dB，距 35 dB 还差 4.4 dB**。

### 10.4 当前最优工作点（更新）
| 目标 | 配置 | PSNR | mf |
|---|---|---|---|
| mf ≈ 0.95 | 原有设计 + n_inject=8, E=0.3, inject_at=0.35 | **30.65 dB** | 0.961 |

### 10.5 下一步
- 把 `n_inject=8` 与已有的 `r_max=13`、8bit 载荷组合，重训解码器（预期 PSNR 再 +1~2 dB）；
- 若仍需 4 dB：**换更强的基础 DDPM**（当前 5.17M/100 epoch）是唯一剩下的结构性杠杆；
- 或者按 P3 重新定位：以"鲁棒性优先"（30 dB + jpeg50≥0.9）作为卖点，
  把 PSNR 上限写入 limitation。

---

## 11. 三机制组合（8bit + r_max=13 + n_inject=8）与评测口径 bug 修复（2026-09-20）

本轮的目标只有一个：把三个**各自独立验证有效**的机制叠到同一个解码器上——
8bit 载荷（clean +0.064）、`r_max=13`（jpeg50 +0.059、PSNR +1.0 dB）、
`n_inject=8`（同准确率 PSNR +0.5~2.1 dB）。三者作用在不同环节，理应可叠加。

训练配置（`decoder_latent32_combo.pt`）：

```
--n-bits 8 --ecc-reps 3 --r-max 13 --inject-mode add --inject-at 0.35 --n-inject 8
--strength-min 0.15 --strength-max 0.4 --sched-noise-prob 0.3 --geom-prob 0.25
--hide-steps 150 --rec-steps 150 --steps 600 --batch-size 16
```

### 11.1 发现并修复了一个会"吞掉"全部多步注入收益的评测口径 bug

组合模型训练的第一次 strength 扫描结果与 `b8r13` 基线**逐位相同**
（PSNR 27.10、mf 0.953、jpeg50 0.781），这本身是不可能的巧合，顺藤摸到两个叠加的错误：

1. `train_decoder` 落盘 decoder config 时**漏写 `n_inject`** → 评测端
   `eval_setup` 回填成默认值 1，于是"用 8 次注入训出来的解码器"在评测时只注入 1 次；
2. 修好回填后 PSNR 反而崩到 **6.02 dB**：`krd/stego.py` 的 `n_inject=n` 是把一次注入
   拆成 n 次、每次 `strength/n`（**总能量守恒**），训练端自己除了 n，
   而评测端 `StegoIO.hide` 把 `strength` 原样传下去，**总能量被放大 8 倍**。

修复（commit `95d8b87`）：`StegoIO.hide` 明确把 `strength` 定义为**总注入能量**并按
`n_inject` 均摊；`n_inject=1` 时与历史行为逐位一致（老 checkpoint 不受影响）；
`train_decoder` 补写 `n_inject`。同时加了 `tests/strength_semantics_test.py`
（假 stego 记录传参，不需要 GPU，覆盖均摊/总能量守恒/n=1 兼容/张量强度/显式覆盖五种情形）。

修复后同口径对照（`scripts/check_ninject.py`，n=4, S=150, s=0.3）：

| 解码器 | n_inject | bpb | r_max | 载密图 PSNR |
|---|---|---|---|---|
| `decoder_latent32_b8r13_best.pt` | 1 | 4 | 13 | 27.88 dB |
| `decoder_latent32_combo_best.pt`（同参数 + n_inject=8） | 8 | 4 | 13 | **28.85 dB** |

**+0.97 dB 是纯白拿的**（零额外计算），且这正是 §10 在 matched-filter 接收端测到的
"多步注入 PSNR 增益"在**训练解码器端**的复现——两个独立口径互相印证。

> 教训（已写进 `tests/strength_semantics_test.py` docstring）：这类"训练/评测口径漂移"
> 的 bug 表现很隐蔽——**准确率看着正常，只有 PSNR 崩**，极易被误读成"这个机制没用"。
> 凡是"训练端做了一次变换、评测端没做"的参数（`strength/n`、`inject_at`、`r_max`、
> `hide_steps`），都必须有一条"训练与评测同源"的断言或回归测试兜底。
> 顺带把同一类隐患的第三处也堵了（commit `c26e15f`）：`run_pipeline.py --ldm` 的
> `latent_decoder` 阶段此前不传 `inject_at/n_inject/r_max`，会用默认值训出一个
> **不是最好那个**的解码器，现在改成 CLI 贯通 + 打印工作点 + 写入 summary。

### 11.2 组合模型评测（S=150, s=0.3, 8bit, bpb=4, r_max=13, inject_at=0.35, n_inject=8）

（训练完成后补：strength 扫描 / 鲁棒性全表 / P1-P3 护栏）

---

## 12. 差距的定量分解：35 dB 到底卡在哪（2026-09-20，纯测量，`scripts/diag_channel_budget.py`）

之前几轮一直在"换机制、重训、看数字"，但一直没算清 35 dB 这个目标里
**哪些 dB 是物理上拿不到的、哪些是可以买到的**。这一节把账算清（结果可复现：
`python scripts/diag_channel_budget.py`，输出 `results/channel_budget.md`）。

### 12.1 先纠正一个我自己的错误估计：JPEG50 不是硬墙

本轮之前我口头推断"JPEG50 在这类小图上本身只有 32 dB 左右，所以 35 dB + jpeg50≥0.95
在信息论上就不可行"。**实测否定了这个推断**（cover = CIFAR 上采样到 64×64，n=32）：

| 信道 | 实测 PSNR（失真 vs 原图） |
|---|---|
| JPEG q=30 | 34.17 dB |
| **JPEG q=50** | **36.77 dB** |
| JPEG q=75 | 40.06 dB |
| JPEG q=90 | 44.27 dB |

原因：CIFAR 上采样后的图极其平滑，JPEG 很好压。36.77 dB 的"信道噪声"与
35 dB 的目标载密图是同一量级，而非碾压关系 —— 所以目标**没有被排除**，
但余量确实很紧（信道噪声 ≈ 嵌入能量的 1.5 倍）。

### 12.2 真正的预算消耗者是"往返地板"，不是 JPEG

载密图总误差 = **往返误差**（无嵌入，只由 S 与 VAE/DDPM 质量决定）+ 嵌入误差。
PSNR 按能量相加：`1/SNR_tot = 1/SNR_rt + 1/SNR_emb`。
用实测往返 PSNR（§4）代入目标 35 dB：

| S | 往返 PSNR | 占总预算 | 留给嵌入的能量 | 嵌入-only 必须 ≤ |
|---|---|---|---|---|
| 150（当前） | 38.00 dB | **50%** | 1.577e-4 | 38.02 dB |
| 200 | 40.25 dB | 30% | 2.218e-4 | 36.54 dB |
| 250 | 41.89 dB | 20% | 2.515e-4 | **35.99 dB** |

**结论：在 S=150 下，35 dB 目标的一半预算被"什么都不藏时的往返误差"吃掉了**，
嵌入本身只能占 38.02 dB（几乎完全不可见）——这才是当前前沿卡在 31.5 dB 的结构原因，
而不是"解码器不够强"或"图案不够聪明"。
把 S 提到 250，等于白拿 **+2.03 dB** 的嵌入预算（代价：训练/评测时间 ×1.67）。

### 12.3 每比特观测数 N 只剩 ~2 dB 可挖

`N = bpb × ecc`（每比特参与平均的频点对数），平均增益 ~10log10(N)：

| 载荷 | ecc | check | 槽位 | r_max | 可用对 | bpb | N |
|---|---|---|---|---|---|---|---|
| 8 | 3 | 32 | 56 | 13（当前） | 252 | 4 | **12** |
| 8 | 3 | 32 | 56 | 15（=off） | 342 | 6 | 18 |
| 8 | 5 | 32 | 72 | 15 | 342 | 4 | **20** |
| 8 | 5 | 32 | 72 | 13 | 252 | 3 | 15 |
| 8 | 5 | 32 | 72 | 11 | 176 | 2 | 10 |

N 从 12 顶到 20 → 理论上限 +2.2 dB，且必须放弃 r_max=13（回到 15），
而 r_max=15→13 本身实测换来了 jpeg50 +0.059。**所以"多攒观测"是拆东墙补西墙，
净收益有限**（§9.7 实测 bpb 2→4 只把 mf 从 0.766 提到 0.922，远低于 3 dB 的理论值，
说明观测之间误差相关，平均增益被高估）。

### 12.4 下一阶段路线（按"每 dB 成本"排序）

| 杠杆 | 预期收益 | 代价 | 依据 |
|---|---|---|---|
| **1. S: 150 → 250** | **+2.0 dB** 嵌入预算 | 训练/评测 ×1.67（解码器重训 ~2.8h，本地可跑） | §12.2 |
| 2. ecc 3→5（r_max 13, bpb 3） | +1 dB 级 | 无重训成本，但占满频点预算 | §12.3 |
| 3. 接收端软判决/LLR 加权 | +1~2 dB 级 | 纯接收端改动；mf 已优于训练解码器，说明这里有空间 | §9.3 |
| 4. latent 32×32 → 64×64 | +6 dB 级（N∝边长²） | DDPM 训练 ×4、显存 ×4 → 必须上 22GB 远程卡；且往返地板可能变差 | §12.3 |
| 5. 换更强基座 DDPM | 抬往返地板（=同第 1 条） | 训练成本高于第 1 条 | §4 |

**先做 1+2+3**：三者正交且都不动架构，合计预期 +4 dB 左右，正好覆盖到 35 dB 的门槛；
若第 1 条在实测中被"投影抹除"抵消（见 `scripts/diag_steps_frontier.py`），
则直接跳到第 4 条（上远程 2080 训 64×64 latent）。

**验证第 1 条的前置实验（已就绪，免训练）**：
`python scripts/diag_steps_frontier.py --n 4 --steps-list 100,150,250 --energies 0.3,0.45`
—— 比较同一总能量下 PSNR-准确率前沿是否随 S 左移。若前沿不左移，说明多出来的
往返上限被采样投影抹掉，S 就不是杠杆。

