# KRD-Steg 现状诊断与下一步改进方案

> 本文档基于对本仓库 `PROPOSAL.md` / `README.md` / `krd/*` / `scripts/*` 与 `results/*`
> 的逐文件阅读，并在本机（GTX 1650 4GB, torch 2.5.0+cu118）复跑了诊断实验后写成。
> 所有引用的数字都可以用文末的命令复现。
>
> **更新（v2 测量口径修复已落地）**：§3 中的第 3/4/5 项（crop 语义、nonce 协议、
> 安全指标口径）与第 2 项（再生攻击代价）已在代码中修复，修复清单见 §2.5。
> 修复后的首轮结果见 §2.6 —— **其中"真裁剪"的实测结论与本文档最初的推断相反**，
> 已在原处更正。

---

## 0. 结论速览

当前管线的**机制**是通的，但**评测口径**与**工作点**存在问题，导致三类"好看的数字"
其实不成立。按严重程度排序：

| # | 问题 | 严重度 | 状态 | 影响 |
|---|---|---|---|---|
| 1 | 载密图 PSNR 9.5dB，且**同等扰动能量下换注入公式也救不回来**——瓶颈是信噪比，不是公式 | 🔴 致命 | 待办 | 论文无法投稿；"无感"前提不成立 |
| 2 | 再生攻击的"攻击代价"基线错（用 stego 而不用 cover） | 🔴 致命 | ✅ 已修（§2.5） | P3 核心结论"攻击者困境"需重算 |
| 3 | `crop` 攻击实现是循环平移，不是裁剪 | 🔴 高 | ✅ 已修（§3.3） | 实测真裁剪几乎无损，旧结论被推翻 |
| 4 | nonce 由 cover 派生并经 PNG 元数据传输 → 方案非盲 | 🟠 中 | ✅ 已修（§3.4） | 现为真盲提取 `H(key‖counter)` |
| 5 | 把槽位定位 AUC=0.79 当"攻击失败"；密钥空间 1354bit 与真实 64bit 冲突 | 🟠 中 | ✅ 已修（§3.5） | 新增盲检测 AUC、真实密钥熵 |
| 6 | "零步数失配惩罚"是信号过强的副产品，不是几何不变性 | 🟠 中 | 待验证 | 需在 30dB 工作点重测 |
| 7 | 缺少不可见性标准评测（FID/LPIPS/隐写分析）与实际 baseline 对比 | 🟠 中 | 部分（LPIPS 已接入） | FID/隐写分析网络与基线仍待做 |

**一句话**：现在的原型证明了"管线能跑通"，但它在**9.5dB 的工作点上**跑通，
所有鲁棒性/失配/攻击结论都被这个"把水印喊得足够大声"的事实污染。
下一步第一优先级不是迁移 LDM，而是**把工作点搬到 30–40dB 并重测全部结论**。

---

## 1. 复核：当前实现里正确的部分

先说清楚哪些不用改，避免推倒重来：

- **闭式 DDIM 复原路径**（`krd/schedule.py`）实现正确，采样/反演共用同一更新式，
  与提案的"公式驱动"叙事一致；`timestep_seq` 把 [0,T-1] 均匀切分，两端对齐，这点做得对。
- **密钥去旋转特征**（`pattern.ring_features` 中 `vals * exp(-jφ)`）是关键设计，方向正确。
  本机诊断也确认：不做去旋，密钥盲解码器无法学习（`inject_ratio` 组 mf clean=0.48–0.52，
  即纯幅度调制、相位不含信息时接收端完全失效，见 §3.3）。
- **逐图 nonce 打散相位**的思路正确（跨图差分平均退化为随机游走）。
- **ECC + 校验比特双通道**（消息槽 + matched-filter 校验槽）设计合理，校验槽不依赖解码器。
- **代码分层**（pattern/stego/schedule/distortions/security 与分辨率无关）确实可平移，
  这一点提案里的判断是准确的。
- 冒烟测试覆盖到位，`run_pipeline.py` 的断点续跑工程化程度不错。

---

## 2. 本机复现的诊断数据（关键证据）

诊断脚本：`scripts/diag_psnr_probe.py`（本机 CUDA 不稳定，用 `--device cpu` 跑，n=6）
输出：`results/diag_psnr_probe.json`

### 2.1 Q1：不嵌入任何信息时，DDIM 往返本身的 PSNR

| 运行 | 图像数 | 往返 PSNR | SSIM |
|---|---|---|---|
| GPU | 48 | **17.88 dB** | 0.5895 |
| CPU | 8 | 17.44 dB | 0.5710 |
| CPU | 6 | 18.26 dB | 0.6258 |

**这是整条管线的 PSNR 上限。** 也就是说：

- 基础 DDPM 只训了 60 epoch / 23.4k step（`results/logs/ddpm.log`），
  远低于 CIFAR DDPM 常用的 400k+ step，所以往返重建只有 ~18dB；
- 在这个上限之下，注入又把 PSNR 从 18dB 压到 9.5dB —— **嵌入导致的降质（8.4dB）
  比基础模型本身的往返误差还大**。

### 2.2 Q2：被选中频点到底占了多大能量

| 量 | 数值 | 含义 |
|---|---|---|
| A0 = median(\|F[k]\|)（环带内） | 0.81 | 环带系数幅度中位数 |
| A0 / RMS(x_T) | 0.865 | **环带系数幅度≈整图 RMS**，不是"高频微弱扰动" |
| 160 对频点占 x_T 总能量比例 | **35.7%** | 被写入的频点本身就携带三分之一的能量 |

提案的叙事是"往频谱里塞一个弱图案"。实测是：**往隐变量能量最大的低频区
用等幅图案整体覆盖**。`inject_pattern` 里 `scale = strength * ring_mag.median()`
把 320 个系数的幅度统一成同一个数，等于把 x_T 的频谱结构拍平。

### 2.3 Q3：三种注入方案，同等扰动能量下的对比

`pertE` = 相对 x_T 能量的扰动占比（越大越响）；`mf_*` = matched-filter 接收准确率。

| 方案 | strength | pertE | PSNR | SSIM | mf clean | mf jpeg50 | mf shift2 |
|---|---|---|---|---|---|---|---|
| replace（现状） | 0.2 | 0.42 | 15.63 | 0.291 | 0.719 | 0.750 | 0.448 |
| replace | 0.5 | 0.49 | 15.17 | 0.271 | 0.990 | 0.948 | 0.333 |
| **replace** | **1.0** | **0.71** | **9.84** | 0.092 | 1.000 | 1.000 | 0.323 |
| delta（保幅度结构，加性） | 0.2 | **0.014** | **17.38** | 0.554 | 0.781 | 0.740 | 0.469 |
| delta | 0.5 | 0.090 | 13.20 | 0.311 | 1.000 | 0.990 | 0.427 |
| delta | 1.0 | 0.359 | 9.53 | 0.106 | 1.000 | 1.000 | 0.333 |
| ratio（幅度调制） | 0.5 | 0.102 | 16.22 | 0.487 | 0.500 | 0.417 | 0.542 |
| ratio | 1.0 | 0.409 | 12.19 | 0.245 | 0.521 | 0.469 | 0.583 |

**三条结论：**

1. **准确率是"悬崖"，不是"缓坡"。** 在 pertE ≈ 0.09（PSNR 13.2dB）时准确率还是 1.000；
   掉到 pertE ≈ 0.014（PSNR 17.4dB）就崩到 0.78，再低就是 0.5。
   → 当前所有 1.000 的准确率，都是在"水印喊得极响"的区间里取得的，
   **不存在任何解码增益的余量**。这也是 P2"零失配惩罚"的真正来源（见 §3.6）。
2. **公平对比下（pertE≈0.36），保幅度结构与拍平幅度几乎一样差**（9.53 vs 9.84dB）。
   → 所以"换注入公式"本身**救不回 PSNR**。真正的瓶颈是**每个比特可用的观测样本数**。
3. **`ratio` 方案接收端完全失效**（0.5 附近）——这反过来验证了相位去旋设计的必要性，
   同时也提示：**相位不是可靠的载体**，因为循环平移会整体旋转相位
   （`shift2` 列三种方案都在 0.33–0.47，即全部失效）。

### 2.4 顺带确认的一处工程隐患

`krd/pattern.py` 的频谱取值为 `F_[:, bins[:,0], bins[:,1]]`。
对**单图 (C,H,W)** 调用时语义正确（本机 CPU/CUDA 均验证通过，且与显式
`F.permute(1,2,0)[r,c].T` 完全一致）；但若误传**批量 (B,C,H,W) 张量**，
CPU 抛 `IndexError: index 10 is out of bounds for dimension 0 with size 3`，
CUDA 直接 device-side assert —— 因为 PyTorch 会把三个高级索引解释成
对 (B,C,H,W) 的前三个维度取数。
`TrajStego` 目前逐图循环所以没踩到，但这是个静默的接口陷阱，建议改成
无歧义的扁平 gather 并加 shape 断言：

```python
def _spec_at(F, bins, W):
    """F: (C,H,W) complex spectrum, bins: (P,2) -> (C,P)"""
    assert F.dim() == 3, f"expect (C,H,W), got {tuple(F.shape)}"
    idx = (bins[:, 0] * W + bins[:, 1]).to(F.device).view(1, -1).expand(F.shape[0], -1)
    return F.reshape(F.shape[0], -1).gather(1, idx)
```

### 2.5 v2 已落地的测量口径修复

| # | 修复项 | 代码位置 | 说明 |
|---|---|---|---|
| 1 | **真裁剪** | `distortions.crop_fill` | 裁边 + 边缘回填，替代 `torch.roll` 循环平移；旧函数 `crop_shift` 删除 |
| 2 | **完整几何攻击** | `distortions`: `cropresize` / `rotate` / `translate` / `zoom` + `STANDARD_ATTACKS` | 各 eval 共用同一张攻击表，避免口径漂移 |
| 3 | **几何失真进入训练** | `distortions.random_distortion(geom_prob=0.25)` | 与评测口径对齐，消除训练/测试不一致 |
| 4 | **再生攻击代价改口径** | `eval_regen.py` | 报告 `PSNR(regen, cover)` 与 `LPIPS(regen, cover)`，同时保留 vs stego 以对照 |
| 5 | **自包含 nonce（真盲提取）** | `utils.derive_nonce_from_key` / `derive_nonces_from_keys` | `nonce = H(key \|\| counter)`，不再依赖 cover；`TrajStego.resolve_nonces` 默认走新协议 |
| 6 | **安全指标重写** | `security.watermark_presence_auc`（盲检测，含方向归一化）、`security.residual_template_correlation`、`security.effective_key_entropy_bits` | 区分"槽位定位 / 盲检测 / 密钥熵"三种口径；P1 报告不再把 AUC 0.79 说成"攻击失败" |
| 7 | **感知指标** | `krd/perceptual.py` | LPIPS 可选依赖，缺失时返回 `None` 并在报告中标注 `n/a`，不中断流水线 |
| 8 | **取数逻辑** | `scripts/eval_common.py` | 修掉 `while done < n: next(iter(loader))` 反复取同一批的旧写法，统一 CIFAR 载入与评测输入构造 |
| 9 | **测试与文档** | `tests/smoke_test.py`、README、PROPOSAL | 冒烟测试新增几何攻击语义、nonce 协议、盲检测 AUC 等检查 |

### 2.6 修复后的首轮结果（本机 CPU，小样本）

用同一套 checkpoint（未重训）在修复后的口径下重跑，
`n=16/24`、`strength=1.0`，命令见文末：

**几何鲁棒性（`results/robustness.md`，n=16/32）**

| 攻击 | 旧口径（错误） | 新口径（真几何） |
|---|---|---|
| `crop +2px`（旧=循环平移） | 0.344 | — |
| `crop2px`（真裁剪） | — | **0.992** |
| `crop4px`（真裁剪） | — | **0.992** |
| `translate2px`（零填充平移） | — | 0.355 |
| `rotate5` / `rotate15` | — | 0.887 / 0.457 |
| `zoom1.1` | — | 0.855 |
| `cropresize0.8` | — | 0.457 |

> **结论更正**：本文档 §3.3 最初推断"真裁剪会让准确率暴跌、抗裁剪是短板"，
> **这个推断是错的**。实测真裁剪（2px/4px）几乎无损（0.992），原因是裁剪在此实现里
> 表现为"内容平移 + 相位整体旋转"，幅度结构未被破坏；
> 真正的短板是**零填充平移（0.355）、旋转 15°（0.457）、裁剪+重采样（0.457）、放大（0.855）**
> ——即任何**引入重采样或空间不连续**的几何变换。
> 这同时说明：旧报告里 `crop+2px = 0.344` 测的其实是循环平移（等价于纯相位旋转），
> 用它论证"抗裁剪脆弱"是**双重错误**。

**P1 安全（`results/key_security.md`，n=32, n_wrong=100）**

| 指标 | 无 nonce 协议 | 逐图 nonce（ours） |
|---|---|---|
| 跨图残差方向一致性 mean \|corr\|（n=24 对照） | 0.186 | **0.033** |
| 槽位定位 AUC @N=16 / @N=32 | 0.968 / 0.972 | 0.777 / 0.791 |
| 盲水印检测 AUC | 1.000 | **0.812** |

- 错密钥 BER mean **0.4903** / std 0.0289；真密钥 **1.0000**；FAR@τ≤12 = 0；
- **逐图 nonce 的作用第一次被量化**：残差相干度 0.186 → 0.033（接近正交），
  这正是槽位定位 AUC 被压制的原因，比只报 AUC 更有解释力；
- **但盲检测 AUC 仍高达 0.812–0.917** —— 即"水印存在性"几乎无法隐藏。
  这与 §2.2/§2.3 一致：嵌入能量占到 x_T 的 36%+，可检测性是必然代价。
  这项指标必须与 PSNR 一起进论文表格，不能再只报"错密钥≈50%"。

**P3 再生攻击：旧"攻击者困境"叙事被新口径推翻（`results/regen.md`，n=16）**

| t_reg | BER | 攻击代价 PSNR **vs cover** | PSNR vs stego |
|---|---|---|---|
| 400 | 0.090 | 9.01 dB | 9.46 dB |
| 800 | 0.465 | **9.73 dB** | 8.71 dB |

> 载密图自身相对 cover 只有 9.65 dB。换口径后出现一个**反直觉但重要**的结果：
> 攻击越狠（t=800，BER 被推到 0.465≈随机）时，图像相对 **cover** 的 PSNR 反而
> **升高**了（9.01 → 9.73 dB），而相对 stego 的距离在变大（9.46 → 8.71）。
> 解释：低 t_reg 时扩散模型主要在"擦除我们注入的扰动"（朝自然图像流形回拉），
> 因此 Regen≈Cover+去噪；高 t_reg 时内容被重生成，与原图无关，
> 与 cover 的距离反而接近"两张无关自然图"的基线。
> **所以旧口径下的"攻击者困境"（BER↓ 必然伴随 PSNR↓）并不成立**；
> 正确的表述是：攻击者在 BER 与"相对原图的信息保真"之间做权衡，
> 且必须用 `vs cover` / LPIPS 而非 `vs stego` 来度量。这一点必须在论文里重写。

**P2 步数失配：新口径下"零失配惩罚"依旧（`results/step_mismatch.md`，n=16）**

S_rec 从 20 扫到 80，clean/jpeg50 全部 **1.000**（载密图 PSNR 9.56 dB）。
即在 9.5 dB 工作点上，失配确实完全不可见 —— 但这仍然**不能**证明"几何不变性"，
因为同一实验里 `rotate5` 只有 0.887、`translate2px` 只有 0.377：
解码器明显依赖相位/空间对齐。**必须在 30 dB 工作点重测才能定论**（见 §5 第 2 步）。

**P2 网格（`results/steps_grid.md`，n=16）**：S_hide≥25 时全 1.000；
S_hide=10（PSNR 14.41 dB，注入最弱）掉到 0.85–0.93 —— 与 §2.3 的"悬崖"一致。

### 2.7 仍未修复（下一轮要做）

- 载密图 PSNR 仍只有 **9.3–9.9 dB**（瓶颈是 §3.1 的信噪比配置，不是公式）；
- 再生攻击代价已改口径，**结论需重写**（§2.6），且尚未在 30 dB 工作点重测；
- 缺少 FID 与真实隐写分析网络（SRNet/XuNet）的对照；LPIPS 需 `pip install lpips`；
- 与 Tree-Ring / ZoDiac / MDDM 的同环境基线对比尚未开始。

> 结果文件说明：`results/*.md|csv` 为 v2 口径下的首轮结果（CPU, n=16~32, 未重训模型），
> 只用于验证口径与暴露问题，**不是论文数字**。`scripts/results/*` 是 v1（错误口径）
> 的历史存档，已标注不可引用。

---

## 3. 逐条问题与修法

### 3.1 【致命】工作点：9.5dB 的载密图

**证据**：`results/robustness.md` 首行 PSNR 9.52dB / SSIM 0.1023；
`results/steps_grid.md` 中 S_hide=50 时 9.53dB。

**为什么不能靠"迁移 LDM"一句话带过**：§2.3 已经证明，换成保幅度结构的注入
在同等能量下并没有变好。问题不是"像素空间/隐空间"，而是
**16 bit 消息 / 160 个频点对 / 单图观测** 这个配置下，
每个比特只有 2 个复数观测（ECC 展开后），要压到低信噪比就必然崩。
当前用"把幅度拉到 0.7 倍总能量"来换准确率，本质是用不可见性买准确率。

**修法（按顺序做，前两步不用换模型）**：

1. **提高每比特的观测数（真正的杠杆）**。
   隐空间 H×W×C 的可用频点数 ∝ H·W。32×32 单通道半平面只有 ~176 个可用点，
   要放 80 个槽位 → 每槽 2 点。
   64×64 有 ~2000 个可用点，**同样的 80 个槽位就是每槽 ~25 点**，
   聚合增益 10·log10(25/2) ≈ **11dB**。
   → 这就是"必须迁移 LDM"的定量理由，而不是模糊的"PSNR 会变好"。
2. **换更高效的编码**：目前 ECC 是重复码 3 次 + 组内均值。
   换成 BCH/LDPC + 软判决，在相同 SNR 下可再省 3–6dB。
   同时把 32 个校验比特从"独立槽位"改成"从消息槽派生"（或用 8–16 bit 校验），
   现在校验比特占了 40% 的嵌入槽（32/80），性价比很低。
3. **只在最后才考虑注入公式**。建议改成保结构的加性写：
   `F[k] <- F[k] + d·s_j·e^{jφ_j}`，`d` 按 `median(|F[k]|)` 定标，
   而不是替换为常数幅度。§2.3 显示它在低能量区至少不会额外恶化，
   而且天然保留幅度分布（对隐写分析更友好）。

**验收标准（写进下一轮实验）**：载密图 PSNR ≥ 35dB / SSIM ≥ 0.95 / LPIPS ≤ 0.02，
同时 jpeg50 准确率 ≥ 0.95。达不到就说明配置还是错的，不要往下走。

### 3.2 【致命】P3"攻击者困境"的测量基线错了

**证据**：`scripts/eval_regen.py:94`
```python
cost = psnr(regen, sg)   # sg 是载密图
```
报告的是"再生图 vs 载密图"，但读者/审稿人关心的攻击代价是
"**再生图 vs 原图（cover）**"或"vs 合法发布图"。
现在 stego 自己相对 cover 只有 9.5dB，所以 `cost` 的分母是个已经被毁掉的参照物，
数字（12.79/9.57/8.98/7.81 dB）没有意义。

更麻烦的是：低 t_reg 时扩散模型在把"被污染的图"往自然图像流形上拉，
**regen 相对 cover 可能比 stego 相对 cover 更接近**（即攻击者在"修图"）。
这种情况下 "BER 0.033 说明水印抗住了攻击" 的解读是错的。

**修法**：同时报告三个量，并在论文里用 cover 作参照：

| 量 | 定义 | 用途 |
|---|---|---|
| `d_cover` | PSNR(regen, cover) | **真正的攻击代价** |
| `d_stego` | PSNR(regen, stego) | 攻击对载密图的改动幅度 |
| `lpips_cover` | LPIPS(regen, cover) | 感知代价（PSNR 在这个量级已失效） |

再把 `ber` 与 `d_cover` 画成散点/曲线，那条"攻击者困境"曲线才成立。
另外 t_reg=999 那一档（`--t-regs` 默认含 999）等价于"完全重生成"，
对应的 d_cover 会接近"两张无关自然图"的水平，应作为攻击者上限单独标注。

### 3.3 【高】`crop` 攻击根本不是裁剪 —— 已修复，且原推断被实测推翻

**问题（已修复）**：`krd/distortions.py` 里
```python
def crop_shift(x, shift=2):
    return torch.roll(x, shifts=(shift, shift), dims=(2, 3))   # 循环平移
```
`torch.roll` 是**循环平移**，频谱幅度完全不变、只整体旋转相位，
因此报告里 `crop+2px: 0.344` 测的是"相位旋转"而不是裁剪，审稿人会立刻发现。

**修复**：`crop_fill`（真裁剪 + 边缘回填）替代 `crop_shift`，
并补齐 `cropresize / rotate / translate / zoom`，评测表见 §2.6。

**⚠️ 原推断更正**：本文档最初据此推断"真裁剪会让准确率暴跌、抗裁剪是短板"。
修复后实测**相反**：

| 攻击 | 准确率 | 机制 |
|---|---|---|
| `crop2px` / `crop4px`（真裁剪） | **0.992 / 0.992** | 裁剪≈内容平移+相位整体旋转，幅度结构完好；载荷在幅度上 |
| `cropresize0.8`（裁剪+重采样） | 0.457 | 插值改变幅度 |
| `rotate5` / `rotate15` | 0.887 / 0.457 | 旋转+零填充重采样 |
| `zoom1.1` | 0.855 | 缩放重采样 |
| `translate2px`（零填充） | 0.355 | 零填充造成空间不连续 |
| `cyclic2`（旧 crop 语义） | 0.30–0.45 | 纯相位旋转（信号其实挺得住，是接收端去旋后取实部吃亏） |

结论：短板不是"几何变换"本身，而是**任何引入重采样或空间不连续的变换**；
幅度域的载荷对纯平移/裁剪不敏感（这支持下面的方案 A）。
旧报告用 `crop+2px = 0.344` 论证"抗裁剪脆弱"是**双重错误**：既用错了攻击，
又用错了载体（那段 0.344 实际对应零填充/循环平移的效应）。

**下一步选择**：

- **方案 A（推荐，成本低）**：把比特编码进**幅度比**（`|F[k]|` 相对同环带中值），
  而不是相位；接收端做幅度归一化。实测 `crop2px` 已近无损，说明幅度域载荷
  对平移/裁剪天然友好，值得把接收端从"去旋取实部"改成"幅度判决"。
- **方案 B（成本高）**：空域四角放同步模板 + 仿射估计后再提取；
  会引入"额外可见标记"，削弱纯频域叙事。
- 若最终仍不抗旋转/缩放，就在 Limitations 里给出机制解释
  （全局频域载体 + 重采样改变幅度谱），这比用错误实验糊过去要好。

### 3.4 【中】nonce 协议不是盲提取

**证据**：`krd/utils.py:57` `derive_nonce(cover)` 用 **cover 内容**哈希；
`scripts/run_stego.py:53-58` 把 nonce 写进 stego PNG 的 `krd_nonce` 文本块。

问题有两层：

1. **非盲**：虽然 nonce 随图传输、复原端不需要 cover，但它**由 cover 决定**，
   所以它携带了 cover 的指纹信息（16 hex = 64bit 摘要）。方案在信息论上
   不是"只依赖密钥 + stego"。审稿人对比 Tree-Ring（完全盲）时会抓这一点。
2. **差分攻击防御有洞**：一旦 stego 元数据暴露（或攻击者拿到同一批图的 nonce），
   提案里"逐图 nonce 抗多图差分"的论证就退化——攻击者可以用 key+nonce 复现图案。
   §2.3 的 shift2 也说明图案本身可被定位。

**修法**：把 nonce 做成**自包含**的：`nonce_i = H(key || message_index || counter)`
或用 `H(key || payload_hash)`，**不依赖 cover**。这样：

- 复原端只用 key + stego 即可（真盲）；
- 攻击者即使拿到 nonce，也无法从 cover 推出任何东西；
- 多图差分攻击需要猜 counter，难度回到密钥空间。

改完后 P1 的 C 表（AUC-N 曲线）要在**新协议**下重跑，并补一个
"攻击者已知 nonce" 的对照列，明确说明已知 nonce 时还剩多少安全性。

### 3.5 【中】密钥安全性叙事要重写

三个具体问题：

1. **AUC=0.79 不是"攻击失败"。** `results/key_security.md` 的 C 表里，
   逐图 nonce 一列 N=32 时 AUC=0.790，无 nonce 是 0.971。
   0.79 意味着攻击者在 79% 的情况下能把真槽位排在随机槽位之前，
   这是**显著优势**，不能写"攻击被死死压制"。
   要么把它压到 0.5–0.55，要么承认这是个有效攻击并分析其后果。
2. **这个指标本身不是"密钥恢复"**。`security.slot_detection_auc` 测的是
   "用真密钥算出的 Δ 能否把该密钥选中的频点从其余频点里挑出来"。
   攻击者若不知道真密钥，这个检验无法执行；它更接近
   **水印存在性/载体定位**检测，而不是密钥恢复。
   建议补两个真正的威胁模型实验：
   - **盲水印检测**（隐写分析）：只给 stego，判断有无水印（AUC/FAR/FRR）；
   - **有 key 猜测的解码优势**：给攻击者一批 (stego, 明文) 对，
     测其从 Δ 中恢复消息比特的能力（这才是"载密信息泄漏"）。
3. **1354bit 与 64bit 冲突**。`token_key()` 生成 16 hex = **64 bit 熵**；
   `key_space_bounds` 把"频点选择 × 相位量化"算成 1354bit，
   但密钥到参数是 SHA256 确定性映射，攻击者枚举的是**密钥字符串**，
   有效空间 ≤ 2^64。这个数字必须在论文里改成
   "密钥派生参数空间的组合下界"，并单独声明真实密钥熵。

### 3.6 【中】"零失配惩罚"要在弱信号工作点重测

`results/step_mismatch.md`：解码器只在 S_rec=50 训练，S_rec 从 20 扫到 80，
clean/jpeg50 全部 1.000。

结合 §2.3 的悬崖曲线，这个结果最可能的解释是：
**图案信噪比极高，反演步数引起的误差根本不影响判决**，而不是
"RingDecoder 学到了扩散轨迹反演的几何不变性特征"。
一个直接的反证：`ratio` 方案（信号解不出来）在 shift 下也接近 0.5，
说明"能否解码"由信号强度单独决定，与步数无关。

**修法**：把 strength 降到工作点 PSNR≈35dB 后重跑这张表。

- 如果仍保持高准确率 → 这是**真卖点**，可以写成"步数透明性"定理级结论
  （再补一个误差传播的解析界，会非常有说服力）。
- 如果掉下来 → 它就是过强信号的副产品，应降级为普通消融，
  或者老实做 `--rec-jitter` 训练。

这一步是整个项目**风险最大、收益也最大**的实验：卖点真假在此一举。

### 3.7 【中】缺不可见性标准评测与真实基线

提案 4.3 列了 FID/隐写分析/LPIPS/基线对比，但代码里只有 PSNR/SSIM：

- 缺 **FID**（stego 分布 vs 真实图分布）与 **LPIPS**；
- 缺 **隐写分析 AUC**（SRNet/XuNet）——这是"隐秘性"这个创新点的唯一硬证据；
- 缺与 **Tree-Ring / ZoDiac / WAM / MDDM** 的同表对比（至少 Tree-Ring 有官方代码，
  在同样 32×32 的 DDPM 上复现是可行且必要的）。

**修法**：在迁移 LDM 之前，先把 Tree-Ring 复现出来做**同环境对照**。
理由：如果当前配置连 Tree-Ring 都比不过，迁移 LDM 只会把问题放大。

---

## 4. 迁移 LDM：路线与算力现实

提案把"迁移 Stable Diffusion 隐空间"当作下一步。需要先明确算力约束：

**本机是 GTX 1650 / 4GB 显存。** SD1.5 的 UNet 是 860M 参数，
fp16 权重就要 ~1.7GB，加上 64×64×4 latent 的激活与 50 步 DDIM 反演，
4GB 上极易 OOM（SD1.5 官方建议 ≥8GB）。所以有两条路：

### 路线 A：小隐空间 DDPM（本地可行，推荐先做）

- 用**冻结的 SD VAE encoder** 把 ImageNet/COCO 子集编码成 64×64×4 latent
  （encoder 前向在 4GB 上可跑，甚至可以在 CPU 上离线抽一次存盘）；
- 自己训一个 **latent DDPM**（主干沿用现有 `krd/unet.py`，输入通道 4）；
  64×64×4 的 latent 数据集比像素空间小 16 倍，本地训练完全可行；
- 载密图通过 VAE decoder 回到像素空间，感知质量由 VAE 保证
  （这是"PSNR 质变"的真正来源）。
- 优点：机制（反演 + 频域注入 + 固定步复原）**原样保留**，三大护栏可平移；
  能立刻把 §3.1 的"每比特观测数 ×12"落到实处。
- 风险：latent DDPM 的重建质量取决于训练量，但比像素空间容易得多。

### 路线 B：真实 SD1.5/SD-Turbo（需要租云卡）

- 用预训练 SD 做生成 + 反演，做 512×512 的高分辨率 demo（论文的图 1）；
- 本地只跑 smoke test，正式数字在云上（单卡 24GB 一天足够跑完所有消融）。
- 这条路能直接对标 Tree-Ring/ZoDiac 的公开设置，可比性最好。

**建议**：A 打底（拿到全部主表 + 消融），B 出惊艳 demo 与最终对比表。

### LDM 阶段必须一起改的四件事

1. **容量重设**：16bit 太寒酸。目标 256–512 bit（64×64×4 的频点预算绰绰有余），
   同时把 ECC 从重复码升级到 BCH/LDPC。
2. **信道模型重设**：VAE 编解码本身是一次有损压缩，必须把它算进信道，
   在训练解码器时就串进 VAE decoder + 可选 JPEG。
3. **空域失真映射**：JPEG/噪声在像素域，但嵌入在 latent 域，
   要在 latent 域做可微失真（decode → 失真 → re-encode），否则训练/测试不一致。
4. **不可见性指标**：FID/LPIPS + 隐写分析 AUC，与 PSNR 一起报告。

---

## 5. 建议的下一步执行顺序（含验收标准）

### 第 0 步（半天）：把测量口径修对，重跑现有实验

1. `distortions.crop_shift` → 真裁剪，并新增 rotate/scale/translate；
2. `eval_regen.py` 增加 `d_cover` / `lpips_cover` 两列；
3. `derive_nonce` 改为 `H(key || counter)`，`run_stego.py` 去掉元数据依赖；
4. 重跑 P1/P2/P3，产出一份"诚实版"结果表（预期会明显变难看，这是好事）。

**验收**：结果表里每个数字都能被一句话解释清楚"为什么此时 PSNR 是多少"。

### 第 1 步（1–2 天）：拿到 base model 的上限

5. 把 DDPM 从 60 epoch 训到至少 200–400 epoch（或用 `kdd`/EDM 的 CIFAR 预训练权重），
   目标是**无嵌入往返 PSNR ≥ 28dB**；
6. 在 strength 扫描下画出**真正的**前沿曲线（PSNR 25/30/35/40 各一点），
   记录每点的 bit accuracy。

**验收**：能在 30dB 以上工作点保持 jpeg50 ≥ 0.95；否则先解决信号处理（ECC/LDPC）、
再谈模型。

### 第 2 步（2–3 天）：重测三大护栏

7. P2 步数失配曲线（在 30dB 工作点）—— 判定"零失配惩罚"是真是假；
8. P1 在新 nonce 协议下重跑，补盲检测 AUC 与"已知 nonce"对照；
9. P3 用 d_cover 重画攻击者困境曲线。

**验收**：三大护栏里至少两个在新工作点仍然成立；不成立的降级为消融。

### 第 3 步（1–2 周）：迁移隐空间

10. 离线抽 SD VAE latents（64×64×4）；
11. 训 latent DDPM（本地）；
12. 平移 pattern/stego/security（分辨率无关，主要是 `res`/通道数参数）；
13. 容量提到 256–512 bit，ECC 换 BCH/LDPC；
14. 全套评测 + FID/LPIPS/隐写分析。

**验收**：PSNR ≥ 35dB、SSIM ≥ 0.95、jpeg50 ≥ 0.95、盲检测 AUC ≈ 0.5。
达到这一行数字，论文才有底气投 CCF-A。

### 并行事项

15. 复现 **Tree-Ring** 作为同环境基线（有官方代码，必做）；
16. 精读并写下与 **MDDM (ICML 2025)** / **Training-Free Robust Generative
    Steganography (MM 2025?)** 的差异化笔记（提案里已标为风险项）。

---

## 6. 与"其他模型分析"的差异说明

那份分析在机制解读上有不少合理之处（密钥去旋、nonce 抗差分、再生攻击预算化
都是正确的方向），但有几条结论基于错误前提，需要更正：

| 其他模型的结论 | 复核结果 |
|---|---|
| "错密钥 BER 0.4975、FAR=0 无懈可击" | BER 数据本身可信（是真密钥 64bit 熵 + SHA256 雪崩的必然结果），但**密钥空间 1354bit 与真实 64bit 冲突**，需重述 |
| "AUC 被死死压制在 0.790 = 攻击失败" | 0.79 **是有效攻击**，不是失败；且该指标不是密钥恢复而是槽位定位，威胁模型需重写 |
| "S_hide≥25 时准确率逼近 1.000，是算法护城河" | 1.000 来自"pertE≈0.5、图案占隐变量 36% 能量"的过强信号；在 30dB 工作点未知 |
| "零失配惩罚 = 学到了扩散轨迹几何不变性" | 更可能是**信号过强的副产品**（§2.3 悬崖曲线支持这一解释），必须在弱信号下重测才能确认 |
| "regeneration 中攻击者必须付出 PSNR 7.81dB 的代价" | 该 PSNR 是相对 **stego** 的，参照物已损坏；改口径后实测 t_reg=800 时**相对 cover 的 PSNR 反而升高**（9.01→9.73 dB），旧口径下的"攻击者困境"不成立，须重写 |
| "crop 脆弱是全局频域通病，写进 Limitations 即可" | 实验里 crop 是 `torch.roll` 循环平移，**根本没测裁剪**；修复后实测真裁剪几乎无损（0.992），真正的短板是重采样类几何变换（平移/旋转/缩放），见 §3.3 |
| "必须迁移 LDM，换引擎即可实现质的飞跃" | 迁移方向对，但**换空间本身不解决信噪比**；真正的杠杆是"每比特观测数 ∝ 分辨率²"+ ECC/LDPC（§3.1） |

---

## 7. 复现本文档数字的命令

```bash
# 诊断探针（推荐 CPU，本机 CUDA 在 torch 2.5.0+cu118 / GTX1650 上偶发 device assert）
python scripts/diag_psnr_probe.py --n 6 --batch 3 --device cpu \
    --strengths 0.2,0.5,1.0 --out results/diag_psnr_probe.json

# 现有三大护栏（当前工作点，用于对照）
python scripts/run_pipeline.py --stages p1 p2 p3
```

> 注：本机为 GTX 1650 4GB + torch 2.5.0+cu118，CUDA 上偶发 `device-side assert`
> （3 维 (C,H,W) 输入时库内实现 CPU/CUDA 均验证正确；**批量 4 维输入会崩**，见 §2.4）。
> 复现正式数字建议换一台环境稳定的机器。

---

## 8. v2 修复项的自检

```bash
# 冒烟测试（新增几何攻击语义 / nonce 协议 / 盲检测 AUC 等检查）
python tests/smoke_test.py

# 修复后的三大护栏（本机 CPU，样本数可按需放大）
python scripts/eval_robustness.py --n 32 --batch 16 --out results/robustness.md
python scripts/eval_key_security.py --n 32 --batch 16 --n-wrong 100 --out results/key_security.md
python scripts/eval_steps_grid.py --n 16 --batch 16 --out results/steps_grid.md
python scripts/eval_step_mismatch.py --n 16 --batch 16 --out results/step_mismatch.md
python scripts/eval_regen.py --n 16 --batch 8 --t-regs 400,800 --regen-steps-list 25 \
    --out results/regen.md
python scripts/plot_frontier.py --n 16 --batch 16 --strengths 0.5,1.0,1.5,2.0 \
    --out results/frontier.md
```

已验证的行为：
- `crop_fill` 与 `torch.roll` 语义不同（冒烟测试断言），且边缘为边缘回填、非黑边；
- `rotate/translate/zoom/cropresize` 全部落在 [-1,1] 且形状不变；
- nonce 与 cover 无关（换 counter/换 key 都会变），旧 `derive_nonce` 仅保留兼容；
- `watermark_presence_auc` 已做方向归一化（返回 ≥0.5）；
- LPIPS 缺失时返回 `None` 并在报告标注 `n/a`，流水线不中断。
